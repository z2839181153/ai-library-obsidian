# P4 打磨任务清单（2026-08-14 启动）

> 前置：P2 真实验收补跑因 ModelScope 429 余额未恢复（2026-08-14 再次探测确认）→ 按交接文档跳到 P4，隔段时间回头补跑。
> 验收方式：pytest + acceptance（Fake 离线）+ 浏览器手工验收；ModelScope 额度恢复后补 P2 --real。

## 任务分解（按序实施，每项完成 → pytest + 验收 + git commit/push）

### P4-1 🌌 占星室（星空图 Graph View）
- [ ] 后端 `/api/starmap`：返回图数据（nodes: 书/技能/主题/档案/对话；links: 关联）
  - 书节点：status（incoming/reviewing=未归位星=补书室）、入馆时间、标签、蒸馏价值
  - 技能节点：从 skills 表（reviewing/approved/installed）
  - 主题节点：rooms（房间）
  - 档案节点：archive/raw 原始副本（按书聚合）
  - 对话节点：conversations（按主题/最近）
  - links：书↔主题（房间归属）、书↔技能（distill_slug/skills.book_id）、书↔档案（raw 副本）、对话↔书（消息 refs 里的 wikilink）
  - 前端筛选：类型筛选 + 入馆时间着色（由前端基于节点 meta 完成）
- [ ] 前端 `views/StarMap.vue`：ECharts graph（echarts 已装）
  - 未归位星=补书室书（虚线圈样式/不同颜色）
  - 类型筛选（书/技能/主题/档案/对话 checkbox）
  - 入馆时间着色（时间梯度）
  - 点击书节点 → 跳转阅览室
- [ ] 路由 `/starmap` + 导航加"占星室"链接
- [ ] tests/test_starmap.py

### P4-2 🗄 档案馆
- [ ] 软删除支持：books status='deleted' + deleted_at + 原始文件移入 archive/deleted/（30 天可恢复）
  - repo: soft_delete_book / restore_book / list_deleted_books
  - API: POST /api/books/{id}/delete（主人操作，进 action ledger）、POST /api/books/{id}/restore
- [ ] 档案馆 API：
  - GET /api/archive/summary：各 tab 统计
  - GET /api/archive/raw：archive/raw 原始副本列表（+ 关联书）
  - GET /api/archive/deleted：已删除（含 30 天倒计时）
  - GET /api/archive/reports：历史日报（复用 /api/daily-reports）
  - GET /api/archive/distill-logs：archive/distill-logs 目录文件列表
  - GET /api/archive/backup：导出 zip（data + vault 打包）
- [ ] 前端 `views/Archive.vue`：Tab（原始副本/已删除+恢复/历史日报/蒸馏过程/备份导出）
- [ ] 路由 `/archive` + 导航加"档案馆"链接
- [ ] tests/test_archive.py

### P4-3 📖 阅览室增强
- [ ] 章节导航：BookDetail 左侧/顶部章节列表（book_content sections → 点击滚动定位）
- [ ] 原文渲染增强：markdown-it 渲染（当前是纯文本）
- [ ] 高亮：搜索词高亮（从 URL ?hl= 或检索定位传入）
- [ ] 右侧"相关技能/相关笔记"联动：GET /api/books/{id}/related
  - 相关技能：skills 表同书/同主题（distill 关联 + 描述相似）
  - 相关笔记：同房间/同主题其他书 + 向量检索相似 chunk
- [ ] tests/test_related.py

### P4-4 🪪 借书证画像（可视化 + 可编辑）
- [ ] 后端 GET /api/profile/stats：藏书分布（按楼层/房间/标签）、方向池、收藏/拒绝历史（recommendations 表按状态统计）
- [ ] 前端：设置页画像 tab（或独立视图）：
  - 藏书分布饼图（ECharts pie，按楼层/主题）
  - 方向池列表（可编辑增删权重）
  - 收藏/拒绝历史可视化（按日期的条形图/列表）
- [ ] tests/test_profile_stats.py

### P4-5 💬 流式聊天（/ws/chat 逐字输出）
- [ ] ChatClient 支持 stream（OpenAI stream=True）或后端逐 token 转发
- [ ] ws.py：ask_stream 消息 → 逐字/逐段推送 chat_token → chat_done
  - FakeLLM 也要支持流式（测试用）
  - 无 key 降级一次性返回
- [ ] 前端 Admin.vue：逐字渲染（游标效果）
- [ ] tests/test_ws_stream.py

### P4-6 ⚡ 性能/细节
- [ ] 虚拟书架标签视图：Floors.vue 按标签聚合（books tags → 虚拟书架切换）
- [ ] 健康度趋势图：Hall.vue 本周入馆趋势用 ECharts 折线
- [ ] WS 重连：指数退避重连（当前固定 5s）
- [ ] 移动端适配：响应式 CSS（grid 单列/导航折叠）
- [ ] tests/test_virtual_shelf.py（若后端支持）或前端验收

### 收尾
- [ ] 全量 pytest + acceptance_p3（回归）+ git commit/push
- [ ] 更新交接文档.md（P4 完成状态、P2 真实验收仍待补）
