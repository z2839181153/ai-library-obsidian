# AI图书馆 · 项目交接文档

> 生成日期：2026-08-15（P2 真实验收推进更新：LLM 切 DeepSeek 官方，`--real-llm` 预验收通过）
> 开发根目录：`D:\qwenpaw的工作空间\ai图书馆`
> 状态：**P4 打磨全部完成 + P5 全部 5 项完成**（188 测试全绿，git `41cb581` 已 push，详见下方里程碑）。**P2 真实验收补跑进行中（2026-08-15）**：LLM 已切换 **DeepSeek 官方 API**（`deepseek-v4-flash`，chat/distill，secrets.deepseek_api_key）；embedding 保持 ModelScope 免费（独立 `embed_base_url`/`embed_api_key` 配置）。**混合预验收 `--real-llm` 已通过**（真实 DeepSeek 蒸馏 16 技能，10 步全绿，1798.9s）；**全真 `--real` 待 ModelScope embedding 账户充值/额度恢复（实测为 429 insufficient balance，非每日限流）后补跑一条命令即可**。P5 主人新需求已确认（2026-08-14）：桌面壳 / 批量入馆（≤10 本/次、同格式）/ 无额度读原文 / 阅览室顶层入口 / 星图书-书语义边。**2026-08-15 代码实查追加：设计缺口盘点——「设计偏差与替代方案」见 §7 末、「设计已定义但未实现功能清单」见 §8 末**。**2026-08-15 本次会话新增：P6 发布版规划已写入 §10（主人新需求：① 一键安装软件包 + 桌面快捷方式，② 傻瓜式配置向导 选供应商→选模型→填 key；并确认了与 Obsidian 关联的可实施方案与楼层/书架自由增删改名的现状核实）——规划稿未动工，新会话开工第一件事见 §10**。**2026-08-15 下午：修复"补书室无法生成分类建议"bug（embedding 429 致单书索引无词法兜底 + LLM JSON 截断不重试），补书室 12 本书全部恢复分类，188 测试全绿，详见 §11**。**2026-08-15 新需求已完成：补书室/书卡操作按钮——已上架书卡「删除」、补书室建议区「送回待定区」、待定区「删除」，194 测试全绿 + 浏览器实测通过，详见 §12**。**2026-08-15 晚间：P6-3 傻瓜式配置向导已完成**（供应商预设库 config/providers.json + 三步向导组件 + 三个新端点 + 设置页「当前生效配置」健康卡 + 首次启动自动弹向导，207 测试全绿 + 浏览器实测通过，详见 §13）。**2026-08-18 顺手待办收尾**：P6-3 提交已 push（`b750dfe`+`1c3605d`，origin/main 同步）；验收测试产物/日志全部清理（§8 第 8 点）；vault 根文档 README.md + schema.md 补齐 + .gitignore 白名单写法修复（git `3d62bb0` 已 push）。**2026-08-18：P6-4 首次启动向导「🏢 自定义楼层」可选步骤已完成**（SetupWizard.vue 第 4 步：保存配置后自动进入，默认 4 标准楼层可增删改名，可跳过；顺带修 .gitignore `dist/` 误忽略 web/dist 改 `/dist/`；207 测试全绿 + 浏览器实测通过，git `cc37520` 已 push，详见 §14）。剩余主线：**P6-2 Obsidian 关联 → P6-1 打包 → 全流程验收**；P2 全真 `--real` 验收与向量回填仍待 ModelScope embedding 额度恢复。本文档用于新会话无缝接续开发。

---

## 1. 项目目标

用 AI + Obsidian 搭建**"图书馆式"个人知识库**：Obsidian vault 是图书馆本体，AI agent 是图书管理员/采购员/档案员。

核心能力闭环：
- **入馆**：多格式素材（PDF/EPUB/技术文档/网页/公众号/聊天记录/视频转写）→ 清洗 → 不可变副本
- **编目分类**：楼层（按来源媒介）→ 房间（按语义主题）→ 书架 → 书，层级可自定义
- **蒸馏技能**：借仓颉 skill 把方法论类书蒸馏成可执行 SKILL.md（人审确认）
- **检索问答**：混合检索定位书 + 概要 + 自动调用相关技能作答，带 `[[wikilink]]` 引用
- **每日采购**：无订阅清单，采集方向 = 历史提问方向 + 热门源；配额按藏书比重由 AI 动态计算
- **主人主权**：所有 AI 操作是建议，主人确认才生效

### 空间地图（最终版，界面设计定稿）

```
┌────────────────────────────────────────────────────────────┐
│  🏛 大厅 │ 📖 楼层 │ 🌌 占星室 │ 📖 阅览室 │ 💬 管理员 │ 🛒 采购员 │ 🗄 档案馆 │ ⚙ 设置 │
├────────────────────────────────────────────────────────────┤
│  · 建筑鸟瞰/健康度    · 今日日报    · 待办（分类/审阅）      │
│  · 快捷：扔书入馆 → 补书室 → 管理员取书分类 → 你确认 → 上架  │
│                                                           │
│  楼层（第一层=来源媒介，可自定义增删）                       │
│    ├ 一楼 PDF/电子书 ── 房间（语义主题）── 书架 ── 书        │
│    ├ 二楼 网页/公众号 ── 房间 ── 书架 ── 书                 │
│    ├ 三楼 聊天记录   ── 房间（按人）── 书架 ── 书           │
│    ├ 四楼 视频转写   ── 房间 ── 书架 ── 书                  │
│    └ 补书室（悬浮于楼层外）——建议区/待定区                  │
│  占星室：书/技能/主题/档案/对话 的星空图，未归位的星=补书室   │
│          （P5：新增书↔书 内容相关连线）                     │
│  管理员：对话（历史）+ 索引（按时间/书名/内容/语义）         │
│  采购员：对话 + 每日推荐 + 配额理由 + 历史日报               │
│  档案馆：原始副本/已删除(可恢复)/历史日报/蒸馏过程记录/备份   │
│  设置：API / 采集规则 / 托管程度滑杆 / 楼层房间编辑 / 画像    │
└────────────────────────────────────────────────────────────┘
```

## 2. 当前目录结构

```
D:\qwenpaw的工作空间\ai图书馆\
    ├─ 详细设计文档.md        ← 设计定稿（v1.0，约1000行，第8节定义 P0–P4）
    ├─ 交接文档.md            ← 本文件
    ├─ main.py                ← FastAPI 入口（127.0.0.1:8800，静态托管 web/dist + SPA fallback）
    ├─ config.py / state.py   ← 配置加载（.env / settings.json）+ 全局状态（含 ConnectionManager）
    ├─ app/
    │   ├─ api/               ← health.py、index.py、books.py（+软删除 delete）、actions.py、floors.py（+CRUD）、ask.py、distill.py、skills.py
    │   │                       + P3 新增：ingest.py（入馆上传 + P5-2 批量入馆 batch）、dashboard.py（健康度）、purchase.py（采购员）、
    │   │                       conversations.py（对话落库/历史/归档）、settings.py（设置读写）、ws.py（/ws/chat WebSocket）
    │   │                       + P4 新增：starmap.py（占星室图数据）、archive.py（档案馆：软删除/恢复/原始副本/日报/蒸馏日志/备份 zip）
    │   ├─ ws/                ← manager.py（ConnectionManager：线程安全广播，call_soon_threadsafe）
    │   ├─ db/                ← repo.py（P0-P4 CRUD：+soft_delete_book/restore_book/list_deleted_books，单写入锁）、schema.py（P0-P4：+books.deleted_at 迁移）
    │   ├─ ingest/            ← parsers.py（md/txt/html/pdf）、cleaner.py（清洗+sha256 不可变副本）
    │   ├─ llm/               ← embeddings.py（Qwen3-Embedding-0.6B）、chat.py（DeepSeek-V4-Flash chat + chat_json 容错，重试 8 次可配）
    │   ├─ retrieval/         ← chunker.py、tokenizer.py、indexer.py（+index_book 单书索引）、searcher.py、vector_store.py
    │   ├─ core/              ← card_generator.py、shelving.py、qa.py
    │   ├─ distill/           ← artifacts.py、pipeline.py、executor_fake.py、executor_llm.py
    │   ├─ router/            ← skill_index.py、skill_router.py
    │   └─ vec/               ← vector_store.py（LanceDB）
    ├─ web/                   ← P3/P4 前端：Vue 3 + Vite SPA（Pinia + vue-router + markdown-it + echarts）
    │   ├─ src/               ← App.vue（布局+toast+WS）、views/（Hall 大厅、Floors 楼层+补书室、BookDetail 阅览室、
    │   │                       Admin 管理员、Purchaser 采购员、Settings 设置+技能审阅+蒸馏确认、
    │   │                       StarMap 占星室、Archive 档案馆）、stores/library.js、
    │   │                       components/（BookCard、ModalDialog 自定义对话框、BatchIngestDialog 批量入馆对话框）、api.js、router.js、echarts.js（按需引入）
    │   ├─ dist/              ← 构建产物（已提交，FastAPI 直接托管，开箱即用）
    │   └─ package.json       ← dev 5173（proxy /api /ws → 8800）；build 出 dist；依赖含 echarts
    ├─ scripts/               ← acceptance.py（P0）、acceptance_p1.py、acceptance_p2.py、acceptance_p3.py（P3 全鼠标验收）、setup_secrets.py
    ├─ tests/                 ← 34 个测试文件（207 个测试：P0/P1 49 + P2 22 + P3 34 + UX 7 + P4 starmap 4 + archive 6 + related 5 + P4-4~6 13 + P5-4 reading 3 + P5-3 offline_reading 9 + P5-2 ingest_batch 14 + P5-5 starmap 7 + P5-1 desktop_shell 15 + P6-3 setup_wizard 13）
    ├─ config/settings.json   ← 运行参数（vault 路径、模型名、chunk 参数、distill 参数、采集规则）
    ├─ P3任务清单.md          ← P3 任务分解（T1–T16 全勾选，含设计取舍与验收步骤）
    ├─ P4任务清单.md          ← P4 任务分解（P4-1~6 全部完成 ✅，含验收步骤）
    ├─ P5任务清单.md          ← P5 任务分解（P5-4/3/2/5/1 全部完成 ✅，含验收步骤与残留数据说明）
    ├─ desktop/               ← P5-1 桌面壳（launcher/backend/tray/webview2/build.py）
    └─ 桌面壳.bat             ← P5-1 双击启动脚本（装依赖 → launcher.py）
C:\Users\Z\.qwenpaw\workspaces\yh7ECT\research\   ← 调研产物（obsidian-ai-survey.md 等）
```

## 3. 已经完成的功能

- ✅ GitHub 同类项目调研（30+ 项目、五条架构路线，结论见 `obsidian-ai-survey.md`）
- ✅ 需求澄清 + 形态确认：**形态 C** = 独立 Python 后端 + Obsidian 联动（薄插件二期）+ Agent Skills 内核 + MCP 接口
- ✅ 详细设计文档 v1.0（约1000 行：架构/数据模型/流程状态机/UI/技术选型/MVP P0–P4/隐私边界）
- ✅ **M0 脚手架**：FastAPI 骨架 + 配置加载 + 启动脚本（health 200 实测）
- ✅ **P0 索引管线**（git `4dd88de`，真实验收通过）：
  - 解析 md/txt/html/pdf → 清洗 → sha256 不可变副本
  - 标题分节 chunker + jieba 预分词（FTS5 词法索引）
  - ModelScope Qwen3-Embedding-0.6B embedding + SQLite 向量缓存（避免重复 API 调用）
  - LanceDB 向量存储（chunk_id merge-insert）+ FTS5 混合检索 RRF 融合
  - copy-on-write 增量索引 + 完整性检测（`/api/index/check`）
  - API：`/api/index/run|status|check`、`/api/search`
  - 验收（100 篇真实文档）：首建 **7.82s**、增量 **0.33s**、召回 **100%**、重建 3.27s；pytest 22 个全绿
- ✅ **P1 编目闭环**（本次会话，49 测试全绿）：
  - schema 扩展：floors/rooms/shelves/catalog_cards/actions/conversations/messages + 默认 4 楼层种子（幂等）
  - `app/llm/chat.py`：OpenAI 兼容 chat 客户端（`chat`/`chat_json` 容错解析、无 key 抛 `LLMUnavailable`、失败重试）
  - `app/core/card_generator.py`：图书卡片生成（书名/一句话简介/摘要/章节/关键概念/标签 + 蒸馏价值评估）+ 分类建议（楼层=媒介固定映射、房间/书架=LLM 建议优先匹配已有）；落盘 `catalog/bk_<id>.md`；私密书跳过 LLM；幂等 + force
  - `app/core/shelving.py`：确认上架（房间/书架不存在自动创建 + `.json`/README.md + `book.md`）、撤销上架（移回补书室）、撤销分类（清建议）
  - `app/core/qa.py`：基础问答（定位→概览→作答带 `[[catalog/bk_<id>]]` 引用→refs），无 key 降级返回原文片段
  - API：`/api/books`（列表/详情/classify/confirm/content）、`/api/actions`（+undo）、`/api/floors`、`/api/ask`
  - action ledger：classify/shelve 全记录 + 可撤销；repo 单写入锁（threading.RLock + busy_timeout）
  - 验收：`scripts/acceptance_p1.py` 7 步端到端通过（离线 Fake / `--real` 真 API）；49 pytest 全绿
- ✅ **P1 真实验收**（2026-08-14，真实 ModelScope API，全流程 **30.4s**）：
  - 卡片质量基线：蒸馏价值分 65-70（真实评估，Fake 固定 82 仅测试用）、房间建议"机器学习"贴合主题、摘要/章节/概念结构完整
  - 问答：能回答"神经网络是什么"并带 `[[catalog/bk_xxx]]` 引用定位原文
  - 修复：ModelScope 免费 API **间歇性空响应**（choices=None 不抛异常）→ ChatClient 显式检测 + 重试 5 次（间隔 1.5s→12s）+ max_tokens=1024
- ✅ **P2 蒸馏闭环**（本次会话，71 测试全绿）：
  - schema：skills 表（sk_* 注册表：draft/reviewing/approved/rejected/installed/blocked + reject_count + last_reject_reason + test_prompts）+ books 补 distill_slug/distill_status；旧库 CREATE IF NOT EXISTS 迁移
  - `app/distill/artifacts.py`：扫描 `vault/skills/<slug>/`（cangjie 规范产物），解析 SKILL.md frontmatter + 六段 R/I/A1/A2/E/B，校验 test-prompts.json（darwin 兼容、缺诱饵 → warning）
  - `app/distill/pipeline.py`：状态机 idle→stage0→awaiting→stage1→1.5→awaiting→stage2→3→4→5→done/failed/blocked；PIPELINE_STATE.md 断点续跑；阶段 0/1.5 主人确认（continue/skip/cancel）；审计复制到 archive/distill-logs/；action ledger（distill_start/stage/block/register…）；触发校验（已上架+methodology+distill_value≥60，force 可绕过）
  - `app/distill/executor_fake.py`：离线 FakeDistiller（确定性产物，可注入失败场景）
  - `app/distill/executor_llm.py`：LLMDistiller（模型=settings.modelscope.distill_model，现为 DeepSeek `deepseek-v4-flash`；串行 5 阶段）；prompt/模板单一事实源 = cangjie-skill 目录（extractors/*.md + templates/*.md）；三重验证 V1/V2/V3；RIA++ 构造；LLM 盲测标注 fallback
  - `app/router/skill_index.py` + `skill_router.py`：技能库 LanceDB 向量索引（cosine，阈值可配 0.6）；问答命中注入 SKILL.md（截断 8000 字符）→ system prompt；多技能 LLM 裁决（无 key 降级 top-1）
  - API：`/api/distill/{id}/start|status|confirm-stage`、`/api/skills`（列表/详情/approve/reject/unblock + `/api/skills/review` 极简 HTML 审阅页）
  - 阻塞逻辑：reject_count≥5 → skill blocked + 书 distill_status=blocked + start 409 拦截；unblock 重置
  - 修复（真实验收中发现）：chat_json max_tokens 默认 1024 → 4096（图书卡片 JSON 被截断）；ChatClient 空响应重试 5 次 → 8 次（间隔 3s→60s）；LLMDistiller verified.md 加 YAML 块（stage2 解析用，否则 0 技能注册）
  - **DeepSeek 接入修复（2026-08-15）**：① 配置分离（chat/distill → DeepSeek 官方，embedding → ModelScope，见 §7 配置说明）；② stage1_5 三重验证分批（每批 ≤3）+ JSON 截断容错解析 + 未判定候选保守放行（DeepSeek 长 JSON 易截断/慢，单批 15 候选实测 429 字符即停 → 0 技能）；③ SKILL.md 格式清洗（剥离 ```markdown 围栏、六段标题扫描放宽、frontmatter 漏闭合容错）+ prompt 硬性约束；④ LLMDistiller timeout 60→150s（DeepSeek 约 8 字符/s，长输出易撞超时）
  - 验收：`scripts/acceptance_p2.py` 10 步端到端通过（Fake 离线 1.3s）；**混合预验收 `--real-llm` 通过（2026-08-15，真实 DeepSeek 蒸馏：16 个技能注册、批准+索引、路由命中、拒绝 5 次阻塞、解除阻塞，10 步全绿，总耗时 1798.9s）**；全真 `--real` 待 ModelScope embedding 额度恢复后补跑（命令：`.venv\Scripts\python.exe scripts\acceptance_p2.py --real`）
- ✅ **P3 Web UI MVP**（本次会话，105 测试全绿 + 端到端验收通过）：
  - **后端补齐**（T1-T8）：schema +recommendations/daily_reports/profile 三表；repo CRUD；`/api/ingest`（multipart 文件/text 上传 + sha256 重复检测 + 入馆即索引 + WS 广播）；`/api/dashboard`（健康度：待分类/待审技能/索引状态/配额执行率/本周趋势/楼层鸟瞰/今日日报）；`/api/purchase`（今日配额+推荐清单[内置热门源种子+方向池启发式，无搜索 key]、收藏→入馆、反馈 ignore/not_interested 写回画像、日报）；`/api/conversations`（ask 落库 + 历史 + 归档为书[media_type=chat 走 ingest]）；`/api/settings`（GET/PUT，key 掩码、空值不改）；floors 扩展 CRUD（有书的楼层禁删 + action ledger）；`app/ws/manager.py` ConnectionManager（线程安全广播：记录 loop + call_soon_threadsafe）；`/ws/chat`（ping/pong、chat/ask_stream、notice 广播 book_ingested/distill_progress/skill_review_ready）；main.py 静态托管 web/dist + SPA fallback
  - **关键修复**：ingest 不能直接调 `indexer.run`（会把不在目录的书删除）→ Indexer 新增公开 `index_book()` 单书索引；FakeEmbed/FakeLLM 下建议区/待定区流程可测
  - **前端**（T9-T14）：Vue 3 + Vite + Pinia + vue-router + markdown-it；页面：大厅（健康度/鸟瞰/扔书入馆/日报）、楼层+补书室（建议区确认上架/待定区、书详情抽屉）、管理员（对话历史+聊天+索引搜索+操作账本撤销）、采购员（配额/推荐/收藏/反馈/日报）、设置（API 配置/采集规则/托管滑杆/楼层编辑/技能审阅并入/蒸馏确认并入）；WS 连接 + toast 通知
  - **验收**：`scripts/acceptance_p3.py` 10 步端到端（扔书→WS 通知→分类建议→确认上架→提问落库→生成推荐→收藏入馆→反馈→设置改楼层→健康度）全程 HTTP 无命令行，通过；105 pytest 全绿（旧 71 + P3 34）
  - **前端构建**：`web/` 内 `npm install`（淘宝镜像）+ `npm run build` 出 dist；dist 已提交（FastAPI 直接托管开箱即用）
- ✅ **P3.5 手工验收 3 个 UX 问题修复**（本次会话，112 测试全绿 + 浏览器实测通过）：
  - **上架楼层下拉选择 + 后端按 name 匹配**：BookDetail.vue 楼层输入框改为 `<select>`（加载 `/api/floors`，显示"电子书（1F）"）；后端 `shelving.confirm_shelve` 支持 code → floor_id → **name** 三级匹配（repo 新增 `floor_by_name`），自由文本填"电子书"也能上架
  - **设置页内联编辑 + 自定义对话框**：新增 `web/src/components/ModalDialog.vue`（Teleport 弹窗，支持单行/多行输入与 danger 确认）；Settings.vue 移除全部原生 `prompt()`/`confirm()`——改名改内联编辑（行内 input + ✓保存/✗取消，楼层/房间/书架均支持）、新建用对话框、删除用 danger 确认框、拒绝技能原因用 textarea 对话框
  - **LLM 重试可配 + 429 立即降级 + 前端超时/取消**：`config.modelscope` 新增 `chat_retries`(8)/`chat_retry_base`(3.0)/`chat_retry_max`(60.0)/`chat_retry_on_429`(False)，settings.json 与 `/api/settings` GET/PUT 均可读写；ChatClient 检测 401/403/404/**429** 视为不可重试立即抛 LLMUnavailable（默认不重试 429）；api.js request 支持 `{timeout, signal}`，管理员提问 180s 超时 + "✖ 取消"按钮、BookDetail 分类 120s 超时 + 取消
  - **回归测试**：`tests/test_ux_fixes.py` +7（楼层 name 匹配、重试参数加载、重试次数生效、429/401 不重试、retry_on_429 开关）
- ✅ **P4-1 占星室**（本次会话，116 测试全绿 + 浏览器实测通过）：
  - `app/api/starmap.py`：`GET /api/starmap` 返回星空图节点/边——节点 = 书（含"未归位"的补书室书）+ 技能 + 主题房间 + 档案对话 + 历史对话；边 = 书→房间、书→技能（蒸馏来源）、书→档案、对话→书（引用）；全部只读 SQL 聚合，无 LLM
  - `web/src/views/StarMap.vue`：echarts 力导向图（graph force），按类型着色（书=琥珀/技能=青/房间=蓝/对话=灰/档案=红），节点可拖拽、悬停 tooltip 显示书名与类型、点击跳转（书→阅览室，房间→楼层页）；筛选下拉（全部/未归位/技能/对话）；顶部标题"占星室 · 星空图"
  - `web/src/router.js` 注册 `/starmap`；`package.json` 加 echarts 依赖
  - 测试：`tests/test_starmap.py` +4（图数据含 5 类节点、补书室书标记未归位、对话-书引用边、技能-书蒸馏边）
- ✅ **P4-2 档案馆**（本次会话，122 测试全绿 + 浏览器实测通过）：
  - schema：books 表加 `deleted_at` 列（CREATE IF NOT EXISTS 迁移，旧库兼容）
  - `app/api/archive.py`：`GET /api/archive`（总览：待恢复已删除书/原始副本/历史日报/蒸馏过程记录/备份状态）、`POST /api/archive/restore/{book_id}`（恢复软删除书，30 天窗口）、`GET /api/archive/raw`（原始不可变副本文件列表 + 路径）、`GET /api/archive/reports`（历史日报）、`GET /api/archive/distill-logs`（蒸馏过程记录）、`GET /api/archive/backup`（vault 全量 zip 打包下载，含 books/catalog/pending/skills/archive/profile.md）
  - repo 新增 `soft_delete_book`（写 deleted_at + 记录 action ledger `book_deleted`）/`restore_book`/`list_deleted_books`（仅 deleted_at 非空且在 30 天窗口内）
  - books.py 新增 `DELETE /api/books/{id}`（软删除，不删磁盘文件，主人在档案馆恢复）；删除后索引不再返回该书（revision +1）
  - `web/src/views/Archive.vue`：档案馆页面（总览卡片 + 已删除书列表 [可恢复/恢复按钮] + 原始副本列表 + 日报列表 + 蒸馏日志 + 备份 zip 下载按钮）；`/archive` 路由 + 导航加入口
  - 测试：`tests/test_archive.py` +6（软删除→列表隐藏→恢复、30 天窗口过滤、raw/reports/distill-logs 列表、备份 zip 生成且含关键目录、重复删除 404）
  - 浏览器实测（headed Chrome，127.0.0.1:8800）：删除《机器学习基础》→ 导航"档案馆"红点 → 档案馆页"已删除"区出现 → 点"恢复"→ 回到楼层页该书重新上架；备份 zip 下载成功（~170KB，含 archive/ 下目录）
  - **注意**：`app/api/archive.py` 里把演示用 `server_log.txt` 的写入临时禁用（改由 PowerShell 重定向），避免每次启动服务都往根目录写日志文件
- ✅ **P4-3 阅览室增强**（本次会话，127 测试全绿 + 浏览器实测通过）：
  - 后端 `GET /api/books/{id}/related`：相关技能（same_book 本书蒸馏 / same_room 同房间 / similar 内容相似，语义匹配 skills 表）+ 相关笔记（same_room / similar，用 Searcher 对全库检索）；`top_n` 参数
  - **related 性能修复**：embedding 429 时原实现会卡 47.7s（前端无超时一直等→面板"暂无"）；Searcher 新增 `search_lexical`（纯词法 FTS5 兜底）+ embed 3s 探测（不可用则 similar 走词法路）→ related 稳定 3.6s 返回；前端 api.js `bookRelated` 加超时兜底
  - `web/src/views/BookDetail.vue` 重写为阅览室三栏：左章节导航（TOC 点击平滑滚动 + IntersectionObserver active 跟随）/ 中原文 markdown 渲染（markdown-it，代码块/表格/引用样式）+ 图书卡片 / 右相关技能+相关笔记面板（技能可展开 description，笔记可跳转）
  - **高亮 `?hl=`**：管理员搜索/问答定位跳转传词，正文词高亮 `mark.hl`（金色）；**关键修复（Vue 3.5 坑）**：模板 ref 赋值晚于 nextTick 与 `flush:'post'` watcher，且 `content` 赋值时 `loading` 仍 true（DOM 未渲染书详情）→ 用 `watch([content, loading], ..., {flush:'post'})` + `getContentRoot()` 用 `contentEl.value || querySelector('.book-content')` 兜底 + applyHighlight 先清旧 mark 防嵌套
  - `web/src/views/Admin.vue`：搜索结果/问答跳转带 `?hl=` 高亮定位
  - 测试：`tests/test_related.py` +5（相关技能 same_book/same_room/similar、相关笔记、embed 失败词法兜底、无相关时空列表）
  - 浏览器实测（127.0.0.1:8801，8800 被旧进程占用）：三栏布局、markdown 渲染、`?hl=机器学习` 高亮 7 处、章节导航 active 跟随滚动、相关面板有数据/清理后"暂无"空态均正常；验收测试数据已清理（测试书软删进档案馆、测试 skill 删除）
- ✅ **P4-4 借书证画像**（本次会话，133 测试全绿）：
  - 后端 `GET /api/profile/stats`：藏书分布（按楼层/房间/标签/主题）、方向池（distill.direction_pool）、收藏/拒绝历史（recommendations 按日期统计）+ 最近推荐明细
  - 后端 `PUT /api/profile/direction-pool`（编辑方向池权重/来源）+ `PUT /api/profile/themes`（手动调主题）
  - 前端 Settings.vue 新增"🪪 借书证"tab：ECharts 饼图（楼层/标签/主题切换）+ 收藏拒绝历史堆叠条形图 + 方向池行内编辑（增删改权重）+ 最近推荐明细表
  - 测试：`tests/test_profile_stats.py` +6
- ✅ **P4-5 流式聊天**（本次会话，137 测试全绿）：
  - ChatClient.chat_stream（OpenAI stream=True）+ FakeLLM.chat_stream 逐段输出（重试/429 降级策略同 chat）
  - QAService.ask_stream 生成器：chat_start（带 refs）→ chat_token* → chat_done；无 key 降级原文片段
  - ws.py：同步生成器线程包装（`_iter_sync_gen`）不阻塞事件循环；支持 cancel 消息；完成后落库 conversations 并回传 cv_id
  - 前端 Admin.vue：WS ask_stream 逐字渲染 + 闪烁光标 + 取消按钮 + 会话续聊（store.library.js onWSEvent 订阅）
  - 测试：`tests/test_ws_stream.py` +4
- ✅ **P4-6 性能/细节**（本次会话，140 测试全绿）：
  - 虚拟书架标签视图：后端 /api/books 支持 tag 过滤 + 列表返回 tags 字段；Floors.vue 侧栏标签 chips 聚合（计数）+ 点击筛选虚拟书架
  - 健康度趋势图：Hall.vue 本周入馆趋势改 ECharts 折线（平滑+面积+均值线+resize）
  - WS 重连：指数退避（1s→2s→…→30s 上限，成功重置）
  - 移动端适配：grid-main/grid-half 可折叠布局 + @media 820px 单列/导航换行/聊天区自适应
  - 测试：`tests/test_virtual_shelf.py` +3
- ✅ **P4 续优化：echarts 按需引入 + 图表 nextTick 时序修复**（本次会话，140 测试全绿 + 浏览器实测通过）：
  - 新增 `web/src/echarts.js` 共享模块（echarts/core 按需注册 Graph/Line/Bar/Pie + Grid/Tooltip/Legend/MarkLine + CanvasRenderer）；StarMap/Hall/Settings 三个视图从全量 `import * as echarts` 改为按需模块 → echarts chunk **1126.9KB → 623.2KB（-45%）**，独立异步 chunk（gzip 213.8KB）
  - **修复预存在时序 bug**：StarMap/Settings 图表容器在 v-if/v-else 分支，render() 在 DOM 未挂载时同步调用导致图表永不渲染 → `nextTick(render)` / `await nextTick()` 修复（P4-1/P4-4 手工验收时被掩盖，浏览器实测暴露）
  - 浏览器实测：占星室 / 大厅趋势 / 借书证画像 三页图表均正常渲染；git `9f9e2e8` 已 push
- ✅ **P5-4 阅览室顶层入口**（本次会话，143 测试全绿 + 浏览器实测通过，git `625bb78`）：
  - 后端：books 表加 `last_read_at` 列（旧库 ALTER 迁移）；`GET /api/books?recent_read=true&limit=N` 按最近阅读倒序（排除删除书）；`GET /api/books/{id}/content` 读取即记录阅读时间（`repo.mark_book_read`，毫秒精度保证排序稳定，不刷新 updated_at）
  - 前端：顶部导航加"📖 阅览室"入口（`/reading`，位于占星室后）；新建 `Reading.vue`（最近阅读 + 最近入馆两栏，点书进入三栏阅读页）；`BookCard.vue` 加"📖 阅读"按钮（仅已上架书显示，click.stop 不干扰卡片跳转）；大厅加"继续阅读"模块（最近阅读 chip 点击直达 + "全部 →" 进阅览室页）
  - 测试：`tests/test_reading.py` +3（读取标记 last_read_at / 连续阅读时间排序 / 排除删除书 + limit）
  - 浏览器实测（headed Chrome 127.0.0.1:8800）：导航"阅览室"入口、楼层页书卡"阅读"按钮跳转原文、读后大厅"继续阅读"出现该书、/reading 最近阅读+最近入馆列表均正常
- ✅ **P5-3 无额度读原文**（2026-08-15 本次会话，152 测试全绿）：
  - 后端：`content` 端点三级兜底——vault `book.md`（已上架）→ chunks 拼接（未上架已索引）→ **`archive/raw` 原始副本**（未上架/未编目无 chunks 时，按 media_type 路由解析器 pdf/markdown/html/text 从原始文件提取正文）；返回 `source`（vault|chunks|raw）+ `raw_available`；新增 `GET /api/books/{id}/raw-file` 返回原始副本二进制（media_type→MIME 推断 + `inline` 内嵌，浏览器内置 viewer 渲染 PDF）；纯本地读取不调 LLM
  - 前端：`BookDetail.vue`——无卡片时卡片区明示"⚠️ 模型不可用或尚未生成 —— 卡片/摘要暂缺。正文离线照常可读"；`source=raw` 时正文标题旁显示"📦 离线原文"徽标；"📄 查看原文件"模态 iframe（PDF 浏览器原生渲染）+ 新标签页链接
  - 测试：`tests/test_offline_reading.py` +9（vault/chunks/raw 三级来源、PDF raw 兜底解析、content 不调 LLM、raw-file PDF/markdown MIME+字节、无 raw 404）
  - 浏览器实测（headed Chrome 127.0.0.1:8800）：已上架《机器学习基础》读 vault 正文照常（7 章节）；PDF 未上架《小蘑菇》"📦 离线原文"徽标 + 全文从原始副本逐页读出 + "查看原文件"模态内 Chrome PDF viewer 原生渲染（embed）；无卡片书显示"模型不可用"提示
- ✅ **P5-2 批量入馆**（2026-08-15 本次会话，166 测试全绿 + 浏览器实测通过，git `07fcf7f`）：
  - 后端 `POST /api/ingest/batch`（multipart：files 多文件 + format + private）：文件数 **>10 或混格式（文件扩展名与所选 format 不符）→ 整批 400 拒绝**；逐本清洗 + sha256 去重（重复标 `duplicate`，不阻断其余）；逐本返回 `{book_id, title, status, duplicate, error}`（status = registered | duplicate | error）
  - **异步索引**：清洗登记完成后立即返回（HTTP 不等待索引）；后台线程执行批量索引——批量 embedding（embed_batch，API 批 64 条/次，N 次 API 往返降为 N/64）+ LanceDB 批量 upsert + FTS5 批量写（indexer 新增 `index_batch` + `BatchIndexResult`）；完成后 WS 广播 `batch_ingested`（progress 0/2 全量/部分索引）→ `batch_index_done`（含 index_counts）
  - **429/额度兜底**：embedding 失败（LLMUnavailable/超时/无 key）的书**先落词法索引（FTS5）**，置 `vector_pending=1`，向量待后台补（`POST /api/index/run` 后续可补），不阻断入馆；`/api/ingest/batch` 返回 `vector_pending` 计数
  - 前端 `BatchIngestDialog.vue`（大厅"📦 批量入馆（≤10 本）"按钮打开）：**格式下拉必选**（未选格式上传文件被拒）→ 多文件选择/拖拽（>10 本提示截断只取前 10）→ **同格式校验提交前完成**（不符文件标红 + 提交禁用）→ 提交后逐本结果列表（✅ 已入馆 / 🔁 已存在 / ❌ 失败）+ 私密书 checkbox（内容不发送 API）+ WS 进度 toast（"批量入馆：x/y 已完成"）；Hall.vue 挂载按钮 + 拖拽区
  - 测试：`tests/test_ingest_batch.py` +14（2 本正常 / 5 本含重复（3 duplicate）/ 混格式 400 / >10 本 400 / 无 format 400 / 空文件 error 不阻断 / private 标记 / batch 入馆后 WS 广播 / 索引失败词法兜底 vector_pending / duplicate 不重复入库）
  - 浏览器实测（headed Chrome 127.0.0.1:8800）：对话框开 → 选格式 → 拖入 5 个 .md → 提交 → 2 本"已入馆" + 3 本"已存在"逐本结果；WS toast 两条（批量入馆进度 → 索引完成）；健康度 13 本书 / 119 chunks 刷新；同格式校验（选 markdown 传 .txt → 标红 + 提交禁用）均通过
  - **顺手修复预存在 flaky 测试**：`test_recent_read_ordered_by_time` 在 Windows 时钟粒度（~15.6ms tick）下偶发失败（同毫秒并列 → recent_read 排序不稳定）→ `repo.mark_book_read` 加**单调推进守卫**（同进程内时间戳严格递增，`_last_read_dt` 上次值 +1ms），隔离跑 5/5 稳定；全量 166 全绿

## 4. 正在开发的功能

- ✅ **P2 蒸馏闭环已完成**（见 §3；git `cb85bdb`）
- ✅ **P3 Web UI MVP 已完成**（见 §3；git `6f71f27`，已 push 至 origin/main）
- ✅ **P3 浏览器手工验收已完成**（2026-08-14 本次会话，结论见 §3.1）
- ✅ **P3.5 手工验收 3 个 UX 问题已修复**（2026-08-14 本次会话，见 §3 末尾；浏览器实测通过，112 测试全绿）
- ✅ **P4-1 占星室已完成**（2026-08-14 本次会话，见 §3 末尾；git `435fcb8`）
- ✅ **P4-2 档案馆已完成**（2026-08-14 本次会话，见 §3 末尾；git `435fcb8`，已 push）
- ✅ **P4-3 阅览室增强已完成**（2026-08-14 本次会话，见 §3 末尾；git `5d34062`，已 push）
- ✅ **P4-4 借书证画像已完成**（2026-08-14 本次会话，见 §3 末尾；git `2798d8b`）
- ✅ **P4-5 流式聊天已完成**（2026-08-14 本次会话，见 §3 末尾；git `6a00891`）
- ✅ **P4-6 性能/细节已完成**（2026-08-14 本次会话，见 §3 末尾；git `9b016f2`）
- ✅ **P4 续优化：echarts 按需引入 + 图表 nextTick 时序修复已完成**（2026-08-14 本次会话，见 §7；git `9f9e2e8` 已 push）
- ✅ **P5-4 阅览室顶层入口已完成**（2026-08-14 本次会话，见 §3 末尾；git `625bb78`）
- ✅ **P5-3 无额度读原文已完成**（2026-08-15 本次会话，见 §3 末尾）
- ✅ **P5-2 批量入馆已完成**（2026-08-15 本次会话，见 §3 末尾；git `07fcf7f` 已 push）
- ✅ **P5-5 星图语义边已完成**（2026-08-15 本次会话，见 §3 末尾；173 测试全绿，git 见本次提交）
- ✅ **P5-1 桌面壳已完成**（2026-08-15 本次会话，见 §3 末尾；188 测试全绿，git `4607b3e` 已 push）——**P5 全部 5 项完成，P5 收尾**
- 仅剩 **P2 全真 `--real` 验收**（LLM=DeepSeek 真 + embedding=ModelScope 真）：混合预验收 `--real-llm` 已通过（2026-08-15，16 技能 10 步全绿），全真待 ModelScope embedding 额度恢复（每日 0 点重置）后执行一条命令
- ✅ **P6-3 傻瓜式配置向导已完成**（2026-08-15 本次会话，见 §13；207 测试全绿 + 浏览器实测通过）——供应商预设库 + 三步向导 + 测试连接 + 健康卡 + 首次启动自动弹
- ✅ **P6-4 首次启动向导「🏢 自定义楼层」步骤已完成**（2026-08-18，见 §14；207 测试全绿 + 浏览器实测通过）——SetupWizard.vue 第 4 步（可选）：保存配置后自动进入，默认 4 标准楼层可增删改名后进入，可跳过
- 🚧 **P6 剩余项**（§10 实施顺序）：**P6-2 Obsidian 关联 A+B** → **P6-1 Inno Setup 打包**（含路径 CWD 坑修复）→ 全流程验收

## 3.1 P3 浏览器手工验收结论（2026-08-14 本次会话）

服务用 `start.ps1`（.venv python -m uvicorn 127.0.0.1:8800）启动，headed Chrome 逐页走查：

| 流程 | 结果 | 说明 |
|---|---|---|
| 大厅健康度/楼层鸟瞰/趋势/日报 | ✅ | 索引 revision 11·100 chunks 显示；WS 显示"已连接" |
| 扔书入馆（文件上传） | ✅ | `manual_accept_book.md` 上传 → toast"已入馆《机器学习基础》" + WS 广播"📥 新书入馆" + 健康度更新（待分类 1、索引 100→106 chunks） + 导航"楼层"红点徽标 |
| 补书室待定区 | ✅ | 待定区显示书 + "生成分类建议"按钮 |
| 分类建议（真实 LLM） | ⚠️ 受阻 | ModelScope 429 insufficient balance（账户余额，非代码问题，见 §7）；Ollama 也未运行 → 无法真实生成卡片；acceptance_p3 用 Fake 已验证该链路 |
| 手动确认上架 | ✅ | 书详情页填楼层/房间/书架 → 确认上架 → 状态"已上架" + vault 路径 `books/1F-电子书/机器学习/入门/机器学习基础`；楼层页房间/书架层级正确显示；ledger 记录 |
| 管理员提问 | ⚠️ 受阻 | 对话落库成功（cv_09ab22b5c18e），但 LLM 429 重试 8 次（约 10 分钟）才降级，**实测 50 分钟未返回**（见 §7 新问题）；无 LLM 降级路径本身由 acceptance 验证 |
| 采购员生成推荐 | ✅ | 3/7 条（arxiv/hn/zhihu），评分理由透明（基准55+方向池命中10+主题命中5=55），日报生成 |
| 收藏入馆 | ✅ | arXiv/HN 两条 collected，执行率 0%→67%，"已处理"区显示，新书进补书室 |
| 反馈 | ✅ | not_interested 200（curl 验证），写回画像 |
| 设置页基础 tab | ✅ | API 配置（key 掩码 ms-f****d000）、采集规则、托管滑杆渲染正常 |
| 设置页楼层编辑 | ⚠️ | 楼层/房间/书架层级树渲染正常（上架自动创建的房间/书架可见）；但点"改名"触发原生 prompt 后**页面崩溃**（自动化环境可复现，见 §7 新问题） |

**结论**：P3 核心 UI 流程（入馆→补书室→上架→推荐→收藏→反馈→设置）基本可用，WS 广播/toast/健康度联动正常；受阻项全部源于 ModelScope 429 与浏览器原生 prompt 的自动化限制，非 P3 代码缺陷（acceptance_p3 已覆盖逻辑链路）。发现 2 个真实 UX 问题待 P4 修复（见 §7）。

**P3.5 修复后复验（2026-08-14，浏览器实测）**：

| 项目 | 结果 | 说明 |
|---|---|---|
| 上架楼层下拉 | ✅ | BookDetail 楼层为 `<select>`（电子书（1F）/网页公众号（2F）/聊天记录（3F）/视频转写（4F））；选中 1F + 房间/书架 → 确认上架成功，vault `books/1F-电子书/密码学/入门/…` |
| 设置页改名内联编辑 | ✅ | 点"改名"→ 行内 input + ✓保存/✗取消（无原生 prompt）；改名保存生效（1F→电子书厅→改回）；新建楼层/删除楼层走自定义对话框，无页面崩溃 |
| LLM 429 快速降级 | ✅ | 管理员提问返回"馆内暂无相关内容"（秒级，不再挂起 50 分钟）；发送中显示"✖ 取消"按钮，点击后显示"（已取消/超时，未获得回答）" |
| 重试参数可配 | ✅ | `/api/settings` 返回 chat_retries=8 / chat_retry_base=3.0 / chat_retry_max=60.0 / chat_retry_on_429=false |

## 5. 关键技术线

| 层 | 选型 |
|---|---|
| 后端 | Python 3.11+ / FastAPI（本地服务，127.0.0.1:8800） |
| 存储 | SQLite FTS5（词法）+ LanceDB（向量），全部本机 |
| LLM/Embedding | **chat/distill = DeepSeek 官方 API**（`deepseek-v4-flash`，2026-08-15 起；key 在 secrets.json 的 `deepseek_api_key`）；**embedding = ModelScope 免费**（Qwen3-Embedding-0.6B，独立 `embed_base_url`/`embed_api_key` 配置；key 见 MEMORY.md，key 本身不记录）。GLM-5.2/Qwen3.5-27B 已不用于主链路 |
| 检索 | 混合检索：FTS5 词法 + LanceDB 向量 → RRF 融合 → 按书聚合 |
| 蒸馏 | 复用 `C:\Users\Z\.qwenpaw\workspaces\yh7ECT\skills\cangjie-skill` |
| 前端 | Vue 3 + Vite Web UI（空间隐喻界面；P4 起含 echarts 星空图） |
| Agent 集成 | QwenPaw agent-skills（主入口）+ MCP server（其他 agent）+ Obsidian 薄插件（二期） |
| 调度 | 每日定时采集（QwenPaw cron 或后端内置） |
| 参考架构 | ObsidianRAG（混合检索+copy-on-write 索引+引用溯源）、claude-obsidian（skills 化+单写入者+溯源账本）、SwarmVault（raw/wiki/schema 三层） |

**多角色 agent 设计**：管理员（编目/索引/问答）、采购员（采集/推荐）、档案员（归档/备份/原始副本）、蒸馏师（调仓颉产技能）。

**vault 目录规划**：`books/楼层/房间/书架/` + `catalog/`（图书卡片）+ `skills/`（蒸馏产物）+ `pending/`（采购候选）+ `archive/`（档案馆）+ `profile.md`（兴趣画像/借书证）。

## 6. 重要文件说明

- `交接文档.md`（本文件）：新会话第一份要读的文件
- `详细设计文档.md`：设计定稿，实现时的唯一依据；第 8 节是 MVP P0–P4 分解
- `main.py` / `app/`：P0 代码主体（见第 2 节目录树）
- `scripts/acceptance.py`：P0 验收脚本（`--real` 用真实 ModelScope API，需 secrets 就位）
- `scripts/acceptance_p1.py`：P1 验收脚本（7 步端到端：入馆→索引→编目→补书室→上架→问答→撤销；默认 Fake 离线，`--real` 走真 API）
- `scripts/acceptance_p2.py`：P2 验收脚本（10 步端到端：蒸馏→注册→批准+索引→路由命中→拒绝 5 次阻塞→解除；默认 Fake 离线 1.3s，`--real-llm` 混合模式=真实 LLM（DeepSeek）+Fake embedding 约 30 分钟，`--real` 全真=真实 LLM + 真实 ModelScope embedding）
- `scripts/acceptance_p3.py`：P3 验收脚本（10 步全鼠标流程：扔书→WS 通知→分类→上架→提问→推荐→收藏→反馈→设置→健康度；Fake 离线，无需 key；注意 TestClient 在 WS 连接期间主线程不能发 HTTP——后台线程触发广播 + timeout 兜底）
- `scripts/setup_secrets.py`：把 ModelScope key 写入 `secrets.json`（不入 git）
- `tests/`：207 个测试（P0 22 + P1 27 + P2 22 + P3 34 + UX 修复 7 + P4 starmap 4 + archive 6 + related 5 + ws_stream 4 + virtual_shelf 3 + profile_stats 6 + reading 3 + offline_reading 9 + ingest_batch 14 + P5-5 starmap 7 + desktop_shell 15 + P6-3 setup_wizard 13），改动后跑 `pytest` 防回归
- `P1任务清单.md` / `P2任务清单.md` / `P3任务清单.md` / `P4任务清单.md` / `P5任务清单.md`：各阶段任务分解（P1-P3 全勾选；P4 全完成；P5-4/3/2 已完成，含设计取舍与验收步骤）
- 调研依据：`C:\Users\Z\.qwenpaw\workspaces\yh7ECT\research\obsidian-ai-survey.md`
- 蒸馏 skill：`C:\Users\Z\.qwenpaw\workspaces\yh7ECT\skills\cangjie-skill\SKILL.md`
- 环境记忆：`C:\Users\Z\.qwenpaw\workspaces\yh7ECT\MEMORY.md`（ModelScope key 存放位置、归档阈值 60% 等）

## 7. 已知的问题 / 风险

- **无搜索 API key**：采集主要靠公开热门源 + 提问方向提取，无法主动全网搜索
- **免费 API 隐私**：ModelScope 调用时文本经过阿里云——私密内容需标记并走本地 Ollama 隔离
- **多格式解析成本**：EPUB/聊天导出/视频转写需额外解析或 ASR；视频转写成本高，默认不主动抓
- **PDF 解析**：当前用简单文本抽取，复杂版式（扫描件/多栏）质量有限，P1 后可按需升级
- **选型雷区**：Reor/obsidian-mcp-tools 已归档、smart-composer 半停滞——不作为依赖；claude-obsidian 宿主绑定不采用
- **P1 依赖 LLM 摘要质量**：图书卡片生成质量取决于免费模型表现；离线验收用 FakeLLM 已验证结构正确，**P1 真实验收已跑**（30.4s，卡片质量基线见 §3）
- **ModelScope 免费 API 额度/限流**（P2 真实验收遇到，重要）：GLM-5.2 蒸馏时偶发**空响应**（已重试 8 次缓解）与 **429 insufficient balance**（账户级限额，非代码问题）；真实验收全流程曾受此影响，代码链路已用 stub LLM 验证（7 阶段 + 注册 ≥2 技能）。**2026-08-15 已绕过**：chat/distill 切 DeepSeek 官方（见下方替代方案），embedding 仍用 ModelScope 免费——`--real-llm` 混合预验收通过（LLM=DeepSeek 真 / embedding=Fake），全真 `--real` 待 embedding 额度恢复后补跑记录质量基线
  - **额度机制（2026-08-14 晚调研确认）**：ModelScope 免费 API（`api-inference.modelscope.cn`）为**账户级每日免费额度**——注册即送每日 2000 次调用（宣传值；单模型 500 次，社区大量反馈实际 200~500 次就触发 429）；**按自然日重置（北京时间 0 点后自动刷新）**，无需任何操作
  - **实测确认**（2026-08-14 21:30 探测）：DeepSeek-V4-Flash / GLM-5.2 / Qwen3.5-27B / Qwen3-Embedding 全部统一 429 `insufficient balance` → 账户级耗尽（非单模型额度，换模型无效）
  - **恢复预期**：最迟 2026-08-15 0 点后自动恢复；若次日仍 429 则非每日配额问题，需登录 `modelscope.cn/my/myaccesstoken` 控制台查账户状态
  - **替代方案**（2026-08-15 已实际采用：**chat/distill 切 DeepSeek 官方 API** `https://api.deepseek.com/v1` / `deepseek-v4-flash`，key 在 `secrets.json` 的 `deepseek_api_key`；embedding 保持 ModelScope 免费，通过新增独立配置 `modelscope.embed_base_url` / `embed_api_key` 分离——见 `app/config.py` / `app/llm/embeddings.py`）。其他未采用备选：① **Ollama 本地**（settings.json 已配置 `qwen2.5:7b` enabled=true 但服务未运行；零额度、数据不外传、质量稍差）② **智谱开放平台**（GLM 系列赠 2000 万 tokens 永久）③ **硅基流动**（2000 万 tokens 永久，开源模型全）④ **火山引擎**（每天 200 万 tokens 按天重置）⑤ 阿里云百炼
  - **DeepSeek 官方 API 要点**：无 embedding 端点（/embeddings 404）；生成速度约 8 字符/s（长 JSON 判定输出 70s+，蒸馏执行器已放宽 timeout=150s）；偶发空响应（chat.py 重试兜底）；付费按量计费（key 充值后可用）
  - **当前状态（2026-08-15）**：`--real-llm` 混合预验收通过（LLM=DeepSeek 真 / embedding=Fake，16 技能，10 步全绿 1798.9s）；**全真 `--real` 待 ModelScope embedding 额度恢复**（每日 0 点重置）后补跑：`.venv\Scripts\python.exe scripts\acceptance_p2.py --real`
- **真实蒸馏耗时**：GLM-5.2（已弃用）单次响应 20-75s，整本书约 8-10 分钟（502.9s 实测）；DeepSeek（现用）生成约 **8 字符/s**，`--real-llm` 实测整本蒸馏 **1798.9s ≈ 30 分钟**（16 技能，含三重验证与压力测试）；长书会更久——P3 已实现进度推送（WS 广播）与后台任务管理
- **技能路由阈值 0.6 待校准**：FakeEmbed 余弦尺度与真实 embedding 不同（离线测试用 description 本身作查询命中）；真实验收通过后按实际命中情况校准 `settings.distill.route_threshold`
- **FakeEmbed 语义尺度**：词袋哈希 64 维，自然问法余弦普遍 <0.3——离线测试/验收中"命中"用技能 description 本身（cos=1.0），仅验证链路而非语义；语义命中靠 --real 校准
- **单写入锁为进程内锁**：`threading.RLock + busy_timeout` 只保证单进程串行；多进程场景需升级（当前本地单进程可接受）。WS ConnectionManager 已做线程安全（loop + call_soon_threadsafe），同步端点广播不再丢消息
- ~~conversations/messages 未落库~~：**P3 已实现**（/api/ask 落库 + 历史 + 归档为书）
- **前端手工浏览器验收未做**：acceptance_p3 走 TestClient 全 HTTP 已通过，但真实浏览器 127.0.0.1:8800 的视觉/交互验收（拖拽上传、toast、路由跳转）未人工确认，P4 开工前建议开服务点一遍 → **本次会话已补做，结论见 §3.1**
- **P2 真实蒸馏基线已补一半**：`--real-llm`（LLM=真实 DeepSeek / embedding=Fake）已通过（2026-08-15，16 技能 10 步全绿 1798.9s）——DeepSeek 蒸馏链路端到端验证完成；**全真 `--real` 基线**（embedding 也真实）待 ModelScope embedding 额度恢复（每日 0 点重置）后执行 `acceptance_p2.py --real` 记录最终基线

### P4 新增观察（2026-08-14，本次会话）

1. **echarts 按需引入已完成**（P4 续优化，2026-08-14）：三个视图（Hall/Settings/StarMap）原 `import * as echarts` 全量引入（dist ~1.1MB）；新增 `web/src/echarts.js` 共享模块（`echarts/core` + 只注册 Graph/Line/Bar/Pie + Grid/Tooltip/Legend/MarkLine + CanvasRenderer），三个视图改用该模块 → echarts chunk 从 **1126.9KB → 623.2KB（-45%）**，且为独立异步 chunk（gzip 213.8KB）。构建 4.8s，140 pytest 全绿，浏览器实测三页图表正常渲染
2. **预存在时序 bug 修复**（P4 续优化，2026-08-14，浏览器实测暴露）：StarMap.vue / Settings.vue 的图表容器在 `v-if`/`v-else` 条件分支里，但渲染函数在 `loading=false` / `profileStats` 赋值后**同步**调用 → DOM 未挂载时 `chartEl.value` 为 null，`render()` 直接 return，图表永不渲染（P4-1/P4-4 手工验收时被掩盖）。修复：`nextTick(render)` / `await nextTick()` 后再渲染。Hall.vue 的 weekChartEl 是常驻 div 无此问题
3. **server_log.txt 不再由 main.py 写入**：`app/api/archive.py` 中把 `server_log.txt` 的写入临时禁用（改由 PowerShell 重定向），避免每次启动服务都往根目录写日志文件；如需恢复日志功能，注意该处被注释的代码
4. **软删除不删磁盘文件**：`DELETE /api/books/{id}` 只写 `deleted_at`（索引 revision +1 隐藏），vault 原始副本与 catalog 卡片仍在磁盘；恢复即撤销 deleted_at（重新索引）。这是"AI 永不删书"约束的实现——主人若要从磁盘彻底删除需手动操作

### P5 新增观察（2026-08-14，本次会话，P5-4 实现）

1. **last_read_at 需要毫秒精度**：最初用秒级时间戳（与 created_at 同格式），同秒连续读两本书排序不稳定（测试暴露：`[b2,b1]` 断言失败）→ `repo.mark_book_read` 改为毫秒精度 ISO（`2026-08-14T22:18:00.914`），字典序=时间序，排序稳定；且不刷新 `updated_at`（阅读不该改变"最近更新"语义）
2. **"读即标记"设计**：阅读标记挂在 `GET /api/books/{id}/content` 上，无额外埋点端点——打开书详情页（BookDetail onMounted → api.bookContent）即自动记录，管理员搜索跳转/占星室跳转/书卡"阅读"按钮/大厅 chip 全部天然覆盖；注意浏览器会话恢复的旧标签页也会触发（实测小蘑菇 22:17:35 被标记即此来源），属预期行为
3. **8800 服务重启方式**：P5-4 浏览器实测前杀掉旧 uvicorn（PID 21968）重启加载新代码（`start cmd /c .venv\Scripts\python -m uvicorn ... > server_p5_8800.log 2>&1`），实测完保持新服务运行（等同用户日常状态）；`server_p5_8800.log` 为启动日志重定向（同 §7 P4 观察第 3 条纪律：不往根目录写日志，重定向文件可删）
4. **BookDetail 相关面板加载 bookContent 的副作用**：书详情页除原文外还会调 `/api/books/{id}/related`（embedding 3s 探测 + 词法兜底），无额度时稳定降级，不影响 last_read_at 记录

### P5 新增观察（2026-08-15，本次会话，P5-3 实现）

1. **raw 原始副本无扩展名**：`archive/raw/<h2>/<hash>` 是内容寻址文件（不带后缀），`parsers.parse()` 按扩展名路由会抛"不支持的格式"→ content 兜底必须按 `books.media_type`（ingest 时写入的 parse format）路由解析器（pdf/markdown/html/text），不能复用 `parse()`
2. **FileResponse 默认 attachment**：`FileResponse(raw, filename=...)` 默认 `Content-Disposition: attachment` → 浏览器会下载而非内嵌渲染；PDF iframe/新标签页查看必须显式 `content_disposition_type="inline"`（实测第一版 attachment 下 iframe 触发下载，改 inline 后 Chrome 内置 viewer 原生渲染 `<embed>`）
3. **raw_path 可能是相对路径**：正常入馆写绝对路径，但历史数据/手写记录可能存相对 data_dir 的路径（starmap 已按 `data_dir/raw` 兼容）→ `_resolve_raw_path` 三路查找：raw_path 绝对 → data_dir/raw_path → content_hash 定位，任一命中即视为 `raw_available`
4. **content 纯本地不调 LLM**：测试断言 `state.llm.calls == []` 固化该约束——无额度/断网场景正文照常是 P5-3 验收核心
5. **server_p53_8800.log**：P5-3 实测用 `start cmd /c .venv\Scripts\python -m uvicorn ... > server_p53_8800.log 2>&1`（8800 端口旧进程 PID 9528 已杀），实测完保持新服务运行（等同用户日常状态）；日志重定向文件可删

### P5 新增观察（2026-08-15，本次会话，P5-2 实现）

1. **embedding API 批量接口是必要优化**：P0-P3 单书索引 `index_book` 逐 chunk 调 embed（embed_one），批 10 本书 × 每本 ~4-8 chunk = 40-80 次 API 往返；P5-2 新增 `embed_batch`（批 64 条/次）→ 全批 1-2 次往返，API 成本与耗时都降一个量级。FakeEmbed 的 `embed_batch` 用 `embed_one` 逐条垫底（测试共用），真实实现（llm/embeddings.py）用 OpenAI 兼容 `inputs` 数组一次请求
2. **批量索引失败语义按书粒度**：`index_batch` 返回 `BatchIndexResult{indexed, failed, skipped}`；单书 embedding 失败（429/超时）**不抛异常**——该书跳过向量、落词法（FTS5 已写入）、`vector_pending=1`，其余书照常索引；HTTP 层把 failed 计数带进 `batch_index_done` 广播，前端"批量入馆完成（部分索引）"提示
3. **API 批 64 是保守值**：ModelScope/OpenAI 兼容 embedding 常见单请求上限 1024 条，64 是"一次 API 往返"的安全批；若日后换服务商可调 `_BATCH_EMBED_SIZE`
4. **curl 直传乱码教训**：用 `curl -F "files=@xxx.md"` 传中文内容文件时，cmd 默认把文件按本地代码页（GBK）读取 → 服务端收到乱码正文 → 标题乱码。浏览器/表单工具（multipart 按 UTF-8 传输）无此问题。实测产生的 3 本乱码书已软删（档案馆可恢复 30 天）
5. **WS 广播消息复用 P3 通道**：`batch_ingested` / `batch_index_done` 走 `ConnectionManager.broadcast`（已有 call_soon_threadsafe 线程安全），前端 store.library.js `onWSEvent` 统一分发，无需新通道；toast 文案与"入馆即索引"单书流程一致
6. **pre-existing flaky 测试修复**：`test_recent_read_ordered_by_time` 全量跑偶发失败——Windows `datetime.now()` 粒度 ~15.6ms，同 tick 内两次 `mark_book_read` 得同毫秒时间戳 → `ORDER BY last_read_at DESC` 并列乱序。修复：repo 实例加 `_last_read_dt` 单调守卫（`now <= last` 则 `last + 1ms`），同进程内阅读时间严格递增（这也是 P5-4 recent_read 功能的正确语义）；隔离跑 5/5 稳定
7. **server_p52_8800.log**：P5-2 实测用 `start cmd /c .venv\Scripts\python -m uvicorn ... > server_p52_8800.log 2>&1`（8800 端口旧进程已杀），实测完保持新服务运行（等同用户日常状态）；日志重定向文件可删

### P5-5 星图语义边新增观察（2026-08-15，本次会话）

1. **embed_many 批量是语义边的成本关键**：115 本书的卡片级向量一次 `embed_many`（批 64）≈ 2 次 API 往返；之后全部命中 SQLite `embedding_cache`（本地缓存），后续加载零 API。另加模块级签名缓存（books 关键字段 + 卡片文本变化才重算），实测二次请求 11ms
2. **无额度时词法兜底自然聚类系列书**：演示库 115 本（阳台园艺笔记 / 尤克里里笔记等系列、无卡片）→ jieba 词袋余弦给同名前缀系列书 0.75~0.8 高分，形成正确聚类；top-k（语义每本 ≤5）截断后 224 条边 / 115 本 ≈ 2 条/本，无毛线球。`semantic_source=lexical` 前端图例明示"词法兜底（离线）"
3. **规则边正确性由 pytest 覆盖**：演示库 0 张卡片（无标签）、仅 1 本上架书 → 同房间/同标签/引用边为 0 属正常；三种规则边 + 已删除书排除均有确定性测试（tests/test_starmap.py +7）
4. **embedding 路径与词法路径共用同一 top-k 贪心**：`_semantic_edges` 按相似度降序连边、每本度数 ≤ top_k，embedding 传 list（余弦）与词法传 dict（词袋）都走同一函数，行为一致
5. **server_p55_8800.log / server_p55_8800_err.log**：P5-5 实测用 PowerShell `Start-Process` 重启 8800（入口为 `app.main:app`，非根 `main.py`），实测完保持新服务运行；日志可删

### P5-1 桌面壳新增观察（2026-08-15，本次会话）

1. **桌面壳 = 薄壳，后端不打包**：exe 只封装 launcher + pywebview + 托盘（PyInstaller 单文件 31MB 无控制台）；FastAPI/LanceDB 等 native 依赖仍由项目 .venv python 运行——避免 PyInstaller 收集 native 库（lance 等）的打包风险，也保证 Web 版与桌面版共用同一后端进程
2. **BackendManager 探测-复用语义**：启动先健康检查 `http://127.0.0.1:8800/api/health`（超时 1s）→ 已运行则复用（退出**不**误杀主人手动启动的服务）；未运行则用 `_resolve_python` 找到 .venv python 拉起 `uvicorn app.main:app`（CREATE_NO_WINDOW 无黑窗 + 日志重定向到 `desktop/backend.log`），退出只 taskkill /T 自己拉起的进程树
3. **关窗 ≠ 退出（托盘常驻）**：pywebview 窗口 `closing` 事件里 `window.hide()` + `return False` 取消关闭 → 窗口隐藏进程存活；托盘"显示窗口" `window.show()`；托盘"退出"才 `destroy()` → `webview.start()` 返回 → 停后端 → 退出。这个 destroy 后 start 返回的机制是 pywebview 正确的退出通道（实测验证）
4. **WebView2 缺失降级**：`desktop/webview2.py` 查注册表 `HKLM\SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}` → 缺失时提示下载链接 + `webbrowser.open` 浏览器打开 + 托盘常驻保持后端；`--fallback-browser` 参数强制降级
5. **exe 路径无关**：frozen 模式下 `_resolve_root` 从 exe 位置向上找含 `web/dist` 的项目根，`_resolve_python` 找 `.venv/Scripts/python.exe`——exe 放项目根或 dist/ 均可运行；pytest 用 `sys.executable` 垫底（CI/开发环境无 .venv 也能测）
6. **测试 15 个全绿但无 UI 断言**：tests/test_desktop_shell.py 覆盖后端探测/拉起/复用/停止/进程树清理 + WebView2 注册表 mock + launcher 参数/降级路径/后端必停；pywebview 窗口本身靠浏览器/桌面实测验证（源码模式 + exe 模式均跑通：标题"AI 图书馆"、`/` + assets + WS + dashboard API 全 200）
7. **desktop/icon.ico 运行时生成**：`build.py` 的 `_ensure_icon` 用 `tray._default_icon()`（PIL 画书本图标）生成，已 gitignore——仓库不存二进制图标，打包时自动生成

### P5 需求现状核实（2026-08-14，主人确认追加，设计见详细设计文档 v0.2 §11 P5）

1. **批量上传不存在**：`POST /api/ingest` 只收单个 `file` 或文本；每本书入馆后单独 `index_book()` → 逐 chunk 调 embedding API（Qwen3-Embedding-0.6B），**速度瓶颈 = 逐 chunk API 往返 + 逐本同步等索引**，429 时更慢。P5-2 方案：`/api/ingest/batch`（≤10 本、同格式）+ 批量 embedding（批 64）+ 异步索引 + WS 进度
2. **无额度读原文的现状**：`GET /api/books/{id}/content` 本身纯本地（读 vault 文件 / chunks 表，不调 LLM），但①未上架/未编目书无 chunks 读不了；②读的是清洗后 markdown，无原文件（PDF/EPUB）查看；③入口不显眼（阅览室不是顶层导航，点书卡才进）——**③已解决（P5-4：顶层导航"阅览室" + 书卡"阅读"按钮 + 大厅"继续阅读"）**；①②属 P5-3 解决
3. **星图无书↔书边**：`/api/starmap` 边只有 book→room（shelved_in/suggested）、book→archive（raw_copy，即"同名书与档案相连"的来源）、book→skill（distilled）、cv→book（archived/referenced），**无书与书之间的内容相关边**。P5-5 方案：卡片级向量两两相似 + 规则边（同房间/同标签/引用）+ top-k 截断
4. **形态**：目前 Web UI 靠浏览器访问 `127.0.0.1:8800`；主人确认 P5-1 先做 pywebview 桌面壳（Obsidian 薄插件二期再议）

### P3 手工验收新发现（2026-08-14，**已修复**，见 §3 末尾 P3.5）

1. **确认上架楼层名不匹配**（UX bug）：~~后端 `shelving.confirm_shelve` 只按楼层 `code` 匹配，而 BookDetail.vue 楼层输入框是自由文本框，用户填名称"电子书"会报"楼层不存在: 电子书"~~ → **已修复**：前端改为 `/api/floors` 下拉选择 + 后端支持按 name 匹配。
2. **设置页"改名"触发页面崩溃**（自动化环境可复现）：~~Settings.vue 用原生 `prompt()`/`confirm()`，Playwright/CDP 下点击即导致 browser context 关闭~~ → **已修复**：全部换成内联编辑 + 自定义对话框组件（ModalDialog.vue）。
3. **LLM 429 时 ask/classify 长时间挂起**（体验问题）：~~ChatClient 重试 8 次，真实 429 场景下前端"思考中"无超时/取消，实测 50 分钟未返回~~ → **已修复**：重试参数可配（settings.json）+ 429 立即降级 + 前端 ask/classify 超时与取消。

### 设计偏差与替代方案（2026-08-15 代码实查确认，非功能缺口）

以下为详细设计文档 §5.1/§6.8/§3.3 定义、实现时刻意改用替代方案的项——**功能有等价实现，仅存放位置/机制不同**（vault 当前只实际存在 `books/`，其余目录因替代方案不落盘或暂无数据）：

| 设计（详细设计文档） | 实际实现 | 说明 |
|---|---|---|
| `vault/archive/raw`（原始不可变副本） | `data/archive/raw/<h2>/<hash>` | `cleaner.py:58` 写入；`archive.py _raw_scan` 扫描此路径（实测 100+ 哈希目录存在） |
| `vault/profile.md`（借书证） | DB `profile` 表 | 借书证画像功能完整（/api/profile/stats、direction-pool、themes），不落盘 md |
| `vault/archive/deleted/`（物理目录） | DB `books.deleted_at` 软删除 | "AI 永不删书"约束的刻意设计（见 §7 P4 观察 4：软删除不删磁盘文件） |
| `vault/conversations/`（对话归档目录） | 对话归档直接走 ingest 成为书 | `conversations.py` archive 端点导出 md → tmp → ingest，不单独建目录 |
| `archive/backups/`（周期落盘备份） | `GET /api/archive/backup` 即时下载 zip | 备份即点即下（zip 打包 data+vault），不落盘 backups/ 目录 |
| `vault/.meta/index_state.json` + `vault_watch.db` | DB `index_state` 表 + SQLite 缓存 | 索引修订状态在 SQLite，不在 vault 文件系统 |
| 前端 UI 库（Element Plus / Naive UI）+ TypeScript（设计 §3.3） | 手写组件（ModalDialog 等）+ JavaScript | 功能等价，未引入组件库/TS |
| 阅览室原文件查看用 PDF.js（设计 §9.1） | 浏览器内置 viewer（`Content-Disposition: inline` embed） | P5-3 实测 Chrome 原生渲染 PDF |

## 8. 下一步要做什么

1. ✅ **P3.5 手工验收 3 个 UX 问题已修复**（2026-08-14 完成，见 §3 末尾 + §7；git `P3.5 UX 修复`）
2. ✅ **P4-1 占星室 / P4-2 档案馆 / P4-3 阅览室增强 已完成**（2026-08-14 完成，见 §3 末尾；git `435fcb8` → `5d34062` 已 push）
3. ✅ **P4-4 借书证画像 / P4-5 流式聊天 / P4-6 性能细节 已完成**（2026-08-14 完成，见 §3 末尾；git `2798d8b` → `9b016f2`，push 随本次文档更新提交一并执行）——**P4 打磨全部完成**
4. **P2 真实验收补跑**（2026-08-15 推进中）：LLM 已切 **DeepSeek 官方**（`deepseek-v4-flash`，secrets.deepseek_api_key），配置分离完成（chat/distill=DeepSeek / embedding=ModelScope）。**混合预验收 `--real-llm` 已通过**（16 技能全链路，10 步全绿，1798.9s）；**全真 `--real` 待 ModelScope embedding 额度恢复**（0 点自动重置）后执行：`.venv\Scripts\python.exe scripts\acceptance_p2.py --real`，记录耗时/通过率/技能质量基线，据此校准 `settings.distill.route_threshold`（当前 0.6 为默认值）
5. **P4 打磨续**（设计文档 §11 P4，已全部完成 ✅）：
   - ✅ ~~占星室~~（P4-1，见 §3）
   - ✅ ~~档案馆~~（P4-2，见 §3）
   - ✅ ~~阅览室增强~~（P4-3，见 §3；git `5d34062`）
   - ✅ ~~借书证画像~~（P4-4，见 §3；git `2798d8b`）
   - ✅ ~~流式聊天~~（P4-5，见 §3；git `6a00891`）
   - ✅ ~~性能/细节~~（P4-6，见 §3；git `9b016f2`）
6. 每完成一个 P 阶段：跑 pytest + 真实验收，git commit 推送 GitHub（ai-library-obsidian，公开）
7. ✅ **P4 续优化：echarts 按需引入 + 图表 nextTick 时序修复**（2026-08-14 完成，见 §7；echarts chunk 1126.9KB→623.2KB；git `9f9e2e8` 已 push）
8. **验收残留数据说明**（2026-08-15 P5-2 实测后更新；**2026-08-18 已清理测试产物**）：库中演示数据——已上架《机器学习基础》（1F 电子书/机器学习/入门）；补书室待定区《小蘑菇》《悉达多》《Hacker News 热榜》《arXiv 最新论文精选》《批量测试书 A-E》（5 本，P5-2 批量入馆验收产物，可直接删）；已删除《机器学习进阶》+ **3 本乱码标题书**（P5-2 curl 直传 GBK 误编码产物，已软删，档案馆可恢复 30 天）；最近阅读记录 机器学习基础 22:18 / 小蘑菇 22:17（P5-4 实测触发）；档案馆备份 zip 若干。**演示书保留作演示（AI 不删书，清理需主人自行操作）**。测试产物已全部清理（2026-08-18）：`data/manual_accept_book.md`、`data/p52_test/`、`data/_p43_demo_skill.py`、`data/_probe_glm.py`、`data/_probe_llm.py`、`data/新建 文本文档.txt`、`data/*.png` 验收截图、根目录全部 `server*.log`/`server_log.txt`、`desktop/*.log`——以上均非 git 跟踪文件（.gitignore 已忽略），删除无 git 影响。
  9. **P5 主人新需求（2026-08-14 确认，设计已更新至详细设计文档 v0.2 §11 P5）**：
     - ✅ **P5-4 阅览室顶层入口已完成**（2026-08-14，见 §3 末尾；git `625bb78`）：顶部导航 + 书卡片"阅读"按钮 + 大厅"继续阅读"（last_read_at 记录）
     - ✅ **P5-1 桌面壳已完成**（2026-08-15，见 §3 末尾；188 测试全绿，git `4607b3e` 已 push）：pywebview 封装 `web/dist` → Windows 桌面程序（双击启动 + 自动拉起/关闭后端 + 托盘常驻；WebView2 缺失降级浏览器）——`desktop/launcher.py` + `desktop/backend.py`（健康检查→未运行则 .venv python 拉起 uvicorn，已运行则复用、退出不误杀）+ `desktop/tray.py`（pystray 托盘：显示窗口/浏览器打开/退出，关窗隐藏不退出）+ `desktop/webview2.py`（注册表检测缺失→提示下载+浏览器降级）+ `桌面壳.bat` + `desktop/build.py`（PyInstaller → `dist/AI图书馆.exe` 31MB 单文件无控制台）；**P5 全部 5 项完成**
     - ✅ **P5-2 批量入馆已完成**（2026-08-15，见 §3 末尾；git `07fcf7f` 已 push）：`POST /api/ingest/batch`（≤10 本/次、同格式 400 拒绝、批量 sha256 去重、批量 embedding 批 64、异步索引 + WS 进度、429 词法先行 vector_pending）；前端对话框（格式下拉必选 + 多文件拖拽 >10 截断 + 同格式校验提交前）
     - ✅ **P5-3 无额度读原文已完成**（2026-08-15，见 §3 末尾）：content 端点 vault→chunks→`archive/raw` 三级兜底；`raw-file` 原文件查看（浏览器内置 viewer 渲染 PDF）；UI 明示模型降级（卡片/摘要标"模型不可用"，正文照常）
     - ✅ **P5-5 星图书↔书语义边已完成**（2026-08-15，见 §3 末尾；173 测试全绿，git 见本次提交）：`/api/starmap` 新增书↔书边——卡片级向量两两相似（`embed_many` 批量 + SQLite embedding_cache 本地缓存 + 4s 超时）+ 规则边（same_room 同房间 / same_tag 同标签 / references 正文 `[[catalog/bk_*]]` 引用）；top-k 截断（语义每本 ≤5、规则每本 ≤8）防毛线球；embedding 不可用时 jieba 词袋余弦词法兜底（`semantic_source: embedding|lexical`）；模块级签名缓存（数据未变秒回）；前端按关系类型着色 + semantic 线宽映射相似度 + 边 tooltip + "书↔书边"开关 + 图例
     - ✅ 实施顺序已全部完成：P5-4（阅览室入口，✅）→ P5-3（离线读原文，✅）→ P5-2（批量入馆，✅）→ P5-5（星图增强，✅）→ P5-1（桌面壳，✅）——**P5 全部 5 项完成（2026-08-15）**
10. **设计已定义但未实现的功能清单**（2026-08-15 代码实查确认——`app/`、`web/src/`、`parsers.py`、`purchase.py`、`archive.py`、`main.py` 均无对应实现；**与"项目未开始使用"无关，属真缺口，供后续排期**）：
    - **入口层**：MCP server（详细设计 §8.3 的 `kb_*` 工具，main.py 无 MCP 路由）、QwenPaw agent-skills 入口（§8.2 `/kb-*` 命令，项目无 `kb_skills/`）、多角色 agent 调度层（`agent/roles.py`，§8.1）
    - **解析**：EPUB（ebooklib）、聊天记录导出解析器（douyin-chat-export 等 JSON/TXT）、SRT/VTT 视频转写（§3.4；parsers.py 仅 md/txt/html/pdf）
    - **采购/调度**：每日 08:00 定时采购（无 APScheduler/cron 代码）、RSS 订阅源（feedparser，现仅内置热门源种子+方向池启发式）、自动模式采购（score≥85 自动入馆；purchase.py 仅返回 `auto_mode` 显示布尔，无自动执行）
    - **检索/问答**：rerank（Qwen3-Reranker-0.6B，P4 清单唯一未勾项）、检索范围过滤（楼层/房间/补书室；SearchRequest 仅 `top_k`/`book_ids`）、问答"执行技能产出文档"（§6.5 第⑤步；qa.py 仅注入 SKILL.md 当上下文，无框架产出执行）
    - **归档**：建议归档（AI 检测长期未读/重复；archive.py 无）、备份导入（现仅导出 zip）、30 天到期自动清理/提示（现仅倒计时显示）
    - **隐私/安全**：本地 Ollama 通道（`app/llm/__init__.py` 明写"占位"，仅 OllamaConfig 配置无 client）、免费 API 明示横幅（UI 无"经手 ModelScope"提示条）、敏感词检测、主人操作端点同源校验（main.py 仅 CORS）、日志脱敏（app 层无 logging 配置）
    - **UI/WS**：管理员 @书/#技能 输入、右侧悬浮"待审阅技能队列"（现并入设置页 tab）、WS `subscribe` 定向订阅推送（后端仅回 `subscribed` 确认，无频道管理；前端未用）、托管滑杆行为落地（`default_mode` 仅配置+显示，无任何行为分支——全托管/全手动与半托管无行为差异）
    - **vault 文档**：根 `schema.md`、根 `README.md`（图书馆导览）未创建（仅 books/ 下楼层 README.md 自动生成）——**2026-08-18 已补齐**（git `3d62bb0`；顺带修 `.gitignore`：`vault/` 排除父目录导致 `!vault/README.md` 白名单不生效，改 `vault/*` + 白名单写法）
    - **备注（勿误判为缺口）**：`vault/catalog/`、`vault/skills/`、`vault/archive/distill-logs/` 当前不存在属"无数据/未蒸馏"——`card_generator.py`/`pipeline.py` 有创建逻辑，真实入馆分类/蒸馏后会出现
11. 🚧 **P6 发布版规划（2026-08-15 本次会话确认，方案见 §10）**：主人新需求——① 把项目做成软件（下载软件包一键安装 + 桌面快捷方式，Inno Setup 方案）；② 傻瓜式 agent 配置（选供应商→选模型→填 API key，三步向导）；③ 与 Obsidian 关联（已确认可实施：vault 指向任意 Obsidian vault + "在 Obsidian 中打开"按钮 + 二期薄插件）；④ 楼层/书架自由增删改名（核实已实现，P6 仅需在首次启动向导暴露）。**2026-08-15 晚间：P6-3 配置向导已完成（见 §13）**；**2026-08-18：P6-4 自定义楼层步骤已完成（见 §14）**；剩余 P6-2 → P6-1

## 9. 不能改动 / 需要注意的约束

- **主人权利（硬约束）**：AI 永不删书；分类/蒸馏/采购全部是"建议"，主人确认才生效；AI 不覆盖主人手动摆放；每个 AI 操作有记录 + 可撤销；删除仅限主人，进档案馆可恢复 30 天
- **保守模式默认**：采购员只出推荐清单，主人点"收藏"才入馆
- **分类层级**：楼层=来源媒介、房间=语义主题、层级依次下降（楼层→房间→书架→书），全部可增删改名；支持虚拟书架（按标签自动聚合）
- **补书室**：所有新书先到补书室，管理员提分类建议 → 主人确认 → 上架
- **隐私边界**：私密内容标记后永不发送 API；存储/索引全部本机；免费 API 数据会经手阿里云（UI 明示）
- **多 agent 支持**：标准 SKILL.md + MCP，QwenPaw 为主入口
- **界面**：可视化窗口为主（Web UI 优先，P5 升级为 pywebview 桌面壳），命令行仅后备；Obsidian 薄插件二期
- **批量入馆（P5 硬约束）**：每次最多 10 本、上传前必须先选格式、一次只能上传同一格式（混格式拒绝）
- **采集规则**：每日约 5 条、不主动抓视频、配额按藏书比重由 AI 思考、方向来自提问历史 + 热门源
- **成本**：优先零成本路线（ModelScope 免费模型、本地存储、公开热门源）
- **蒸馏质量**：SKILL.md 必须人工审阅（拒绝附原因 → 下次重蒸）
- **环境**：Windows 11 + WSL2/Docker 可用；ModelScope key 在 `secrets.json`（不入 git）；git 仓库 `ai-library-obsidian`（GitHub 公开，GCM 已存 token）
- **归档纪律**：本窗口上下文进度到 ~60%（出现 `[context compressed]` 信号或会话很长）时，立即用本模板重写 `交接文档.md`，勿等提示

## 10. P6 发布版规划（2026-08-15 新增，主人新需求；P6-3 已于 2026-08-15 完成 ✅ 见 §13，P6-4 已于 2026-08-18 完成 ✅ 见 §14）

P5 全部完成后（2026-08-15），主人提出两个新需求：① 把项目做成**软件**——别人下载软件包一键安装、装完有桌面快捷方式；② 优化 agent 配置程序——**选供应商 → 选模型 → 填 API key** 三步傻瓜式完成配置。本节约定了推荐方案与实施要点，供新会话直接开工（已确认：P6-2 与 Obsidian 关联有方案且可实施）。**2026-08-15 晚间已按第 1 项实施顺序完成 P6-3 配置向导（§13）；2026-08-18 完成 P6-4「🏢 自定义楼层」可选步骤（§14）；剩余 P6-2、P6-1 未动工。**

### P6-1 一键安装软件包（推荐：Inno Setup 安装程序，保留薄壳架构）

**现状差距**：`desktop/build.py` 打出的 `dist/AI图书馆.exe` 是**薄壳**——必须依赖旁边的项目 `.venv\python.exe` 拉起后端（P5-1 决策：避免 PyInstaller 收集 LanceDB native 库的打包风险）。因此 exe 无法独立发给别人，"一键安装"无从谈起。

**推荐方案**：把**整个可运行环境**用 Inno Setup（免费、Windows 原生、成熟）打成 `AI图书馆-Setup.exe`：

```
安装目标 %LOCALAPPDATA%\AI图书馆\        ← 用户级安装（免管理员权限，可写）
├─ AI图书馆.exe          ← 桌面壳（现有薄壳，几乎不改）
├─ web\dist\             ← 前端构建产物（npm build 已产出）
├─ app\ + config\ + scripts\   ← 后端代码
├─ python\python.exe     ← 精简 Python 运行时（venv --copies 复制 + 删缓存/__pycache__/tests/.git）
└─ Lib\site-packages\    ← 全部依赖（含 lance native dll）
```

- **桌面快捷方式**：Inno Setup `[Icons]` 段创建 `{autodesktop}\AI图书馆.lnk` + 开始菜单 + 卸载入口（原生能力）
- **保留薄壳架构**：exe 不碰 native 库（LanceDB 风险不引入）；安装目录结构里 exe 同级即有 `web/dist`，现有 `_resolve_root()` 无需改
- **数据隔离**：用户数据（SQLite/LanceDB/secrets/archive）放 `%APPDATA%\AI图书馆\data`（或安装目录内），卸载时保留；vault 内容归 Obsidian 关联（见 P6-2）
- **实现要点（新会话必须处理）**：
  1. `desktop/backend.py::_resolve_python` 需多认一个 `python\python.exe`（便携运行时）位置
  2. **路径 CWD 坑（隐蔽）**：`app/config.py` 的 `paths` 相对路径是 `Path("data")` 相对**当前工作目录**解析，不是 ROOT_DIR——安装版快捷方式启动 CWD 不定，必须改为相对 ROOT_DIR 解析，否则数据目录错乱
  3. WebView2：Win11 自带；Win10 安装脚本静默装离线包（现有注册表检测逻辑已具备，补静默安装步骤）
  4. 体积预估 150–300MB，安装包可用 UPX 压缩；构建流程做成一条命令（npm build → 精简 venv → PyInstaller 薄壳 → Inno Setup）
- **备选（不作主方案）**：① PyInstaller 全量打包后端（单 exe 体验最好，但 LanceDB native 收集失败率高、体积更大、冷启动解压慢——仅当探索项）；② 绿色 zip + 安装.bat（零成本兜底，体验不够"软件"）

### P6-2 与 Obsidian 关联（已确认：有方案且可实施，分三层）

项目定位"vault 是图书馆本体"，安装版若只用封闭内置 vault 就失去 Obsidian 联动价值。方案：

- **A. vault 指向任意 Obsidian vault（一期必做，实施成本低）**：
  - 现有 `paths.vault_dir` 已支持任意路径——安装版首次启动/设置页提供"选择 Obsidian vault"目录选择器，写入 `paths.vault_dir`
  - 图书馆只在自身子目录操作：`books/`（楼层/房间/书架）、`catalog/`、`skills/`、`pending/`——与用户已有笔记共存，不碰用户文件（天然符合"AI 不覆盖主人手动摆放"硬约束）
  - 需校验：所选目录下 `books/` 若已存在且非本项目结构 → 提示而非静默覆盖；切换 vault 时提供"复制内置 vault 内容到所选 vault"**可选**项（默认不迁移，尊重 AI 不删书/不覆盖）
  - 职责分离：软件数据（SQLite/LanceDB/secrets）留 %APPDATA%，只有知识内容（书/卡片/技能）进用户 vault
- **B. "📂 在 Obsidian 中打开"一键入口（一期必做，成本极低）**：
  - Obsidian 官方支持 `obsidian://open?path=<url-encoded绝对路径>` URI（Windows 注册 obsidian:// 协议）
  - 后端一个端点返回 vault 绝对路径 + obsidian URI；前端大厅/设置页一个按钮；桌面壳 `webbrowser.open(uri)`
  - 未装 Obsidian → 降级 `explorer` 打开 vault 文件夹 + 提示安装
- **C. Obsidian 薄插件（二期，形态 C 已定，P6 不做）**：安装包设置页留插件安装指引即可，不排期

### P6-3 傻瓜式配置向导（选供应商 → 选模型 → 填 key）【已完成 ✅ 2026-08-15，详见 §13】

**现状差距**：设置页 API tab 是平铺表单——base_url/chat_model/distill_model/embed_model/api_key 五框分开填，还混 Ollama 与重试参数；用户须懂"base_url/蒸馏模型/嵌入模型"才能配好，不傻瓜。

**推荐方案**：三步配置向导 + 供应商预设库
- **供应商预设库**（一份 JSON，前后端共用，可扩展）：deepseek（`https://api.deepseek.com/v1`、无 embedding→自动配 ModelScope 免费）/ modelscope（免费额度标签）/ zhipu / siliconflow / openai / ollama（本地，key 留空）；每项含 `chat_models`、`distill_models`、`embed_models`（可为空）、`free` 标签、`key_url` 获取链接、`hint` 说明
- **前端三步**：① 选供应商（卡片式，免费/付费/本地标签）→ ② 选模型（聊天/蒸馏/嵌入，默认推荐组合；供应商无 embedding 自动落 ModelScope 免费或 Ollama）→ ③ 填 API key（带"去获取 key"链接，保存后立即**测试连接**，把 401/404/429/超时翻译成大白话"key 无效/模型名错/额度不足/网络不通"）
- **后端三个新端点**：`GET /api/providers`（返回预设库）、`POST /api/settings/apply-provider`（一步写 settings.json + secrets.json）、`POST /api/settings/test-connection`（最小请求实测）
- **关键体验**：首次启动检测未配置 key → 自动弹向导；设置页顶部"当前生效配置"健康卡（供应商 + 模型 + key 掩码 + 上次测试时间）；现有平铺表单降级为"高级设置"保留
- **约束延续**：key 继续存 `data/secrets.json`（不入 settings.json、不入 git）；Ollama 作为"本地供应商"选项；免费 API 数据经手提示保留

### P6-4 楼层/书架自由增删改名（**已实现，非新需求**，P6 只需增强暴露）

主人 2026-08-15 问询后核实：该能力 P3/P3.5 已完成，安装版开箱即得，无需后端开发：
- **后端已具备**（`app/api/floors.py` 全套 CRUD + action ledger 可撤销）：`POST/PUT/DELETE /api/floors`（新建自动 code F{n}；**有书的楼层拒绝 409**）、`POST/PUT/DELETE /api/rooms`、`POST/PUT/DELETE /api/shelves`；上架按楼层 name 匹配（P3.5）
- **前端已具备**（设置页「楼层编辑」tab）：＋ 新建（自定义对话框）、改名（内联编辑 ✓/✗）、删除（danger 确认框；有书层级后端拒绝）
- **约束延续**（§9）：楼层=来源媒介、房间=语义主题、层级依次下降（楼层→房间→书架→书），全部可增删改名；有书的楼层/房间/书架禁删（须先移走书）；虚拟书架（按标签自动聚合，P4-6）
- **P6 增强点（唯一工作项）**：首次启动向导加"🏢 自定义楼层"步骤（可选）——默认展示 4 个标准楼层（PDF/电子书、网页/公众号、聊天记录、视频转写），用户可增删改名后进入；设置页「楼层编辑」tab 保持为高级入口不动。**2026-08-18 已完成 ✅（见 §14，git `cc37520`）**

### 实施顺序建议（P6 内部）

1. **P6-3 配置向导 + P6-4 首次启动向导**（别人装完能不能用的前提；供应商库 + 三步 + 测试连接 + 自定义楼层步骤；两者合并为同一"首次启动向导"流程）——**已完成**：P6-3 全部（2026-08-15，见 §13）+ P6-4「🏢 自定义楼层」可选步骤（2026-08-18，SetupWizard.vue 加第 4 步，复用 /api/floors CRUD，见 §14）
2. **P6-2 Obsidian 关联 A+B**（依赖小：settings 路径 + 一个 URI 端点 + 前端按钮）
3. **P6-1 Inno Setup 打包**（把前两者固化进安装体验；含路径 CWD 修复、便携运行时、桌面快捷方式）
4. 全流程验收：全新 Windows 机器（或虚拟机）下载 Setup.exe → 安装 → 桌面快捷方式 → 首次启动向导（配置 DeepSeek + 自定义楼层）→ 关联 Obsidian vault → 扔书/问答冒烟；pytest 防回归

### P6 风险 / 注意

- 安装包体积 150–300MB（精简 venv + lance native），UPX 压缩后可接受；若嫌大再评估 PyInstaller 全量
- 路径 CWD 坑必须修（见 P6-1 实现要点 2），否则安装版数据目录错乱
- 关联 Obsidian vault 时的目录冲突校验与"可选复制"必须做，尊重 AI 不覆盖硬约束
- P6-3 测试连接端点要覆盖无 key / 错 key / 错模型 / 网络不通 / 429 全分支，给出可读错误 —— ✅ 已完成（`app/providers.py::translate_error` 覆盖 401/403/404/429/超时/网络不通/未填 key，单测 + 真实实测通过，见 §13）

---

## 11. 补书室"无法生成分类建议"修复记录（2026-08-15 实测已修）

**现象**：补书室里的书点"生成分类建议"全部失败，返回"书无可用 chunk（未索引或为空）"。

### 根因（两层）

1. **embedding 429 余额不足 + 单书索引缺词法兜底（主因）**：ModelScope embedding（Qwen3-Embedding-0.6B）账户 `insufficient balance`（HTTP 429）。入馆登记成功 → `index_book` 调 embedding 失败 → **旧逻辑先 embed 后写 chunk，embedding 失败直接抛异常被 `_register_and_index` 的 try/except 吞掉** → 书留在 incoming 但 chunks=0。P5-2 批量路径 `index_books` 已有"词法先行"兜底，**单书 `index_book` 漏了**。classify 依赖 chunks → 报"书无可用 chunk"。
2. **LLM 输出截断致 JSON 解析失败（次因，大书触发）**：`chat_json` 默认 `max_tokens=4096`，大书卡片（长 summary）输出被截断成不完整 JSON → 抛 `LLMOutputError` 且**不重试**（实测弗洛姆 312 chunks 触发，summary 写到一半断掉）。另 `parse_json_loose` 用 `rfind('}')` 截断，字符串值内含 `{` `}` 时会误切。

### 代码修复（已生效，需重启后端加载）

- `app/retrieval/indexer.py`：`index_book` / `_index_book` / `run()` 三处 embedding 失败改为**词法先行**（chunks + FTS5 照写，书标 `vector_pending=1`，向量由 `backfill_vectors()` 后台补）——与批量路径行为一致。
- `app/llm/chat.py`：`chat_json` 解析失败**自动重试最多 3 次**，第 2 次起 `max_tokens≥8192`（防截断）；`parse_json_loose` 改用**花括号配对深度扫描**（正确处理字符串内含 `{}` 与尾随杂质）。
- `app/core/card_generator.py`：classify 调用显式 `max_tokens=8192`。
- `tests/conftest.py`：`FakeLLM.chat_json` 补 `max_tokens` 参数（适配新签名）。

### 数据修复（已执行）

7 本 chunks=0 的 incoming 书（弗洛姆三部曲、我与地坛、小蘑菇、悉达多、Hacker News 热榜、arXiv 最新论文精选、1 本乱码标题 md）按 `media_type` 从 raw 副本重新解析 → chunk → 词法索引，全部标 `vector_pending=1`。本质做法：`parser(raw) → IngestedBook → indexer.index_book()`（临时脚本已删除）。

### 验证（已实测）

- 补书室全部 12 本 classify 成功：弗洛姆→心理学/入门、小蘑菇→科幻文学、悉达多→文学、我与地坛→文学、HN→技术热点、arXiv→论文资讯、批量测试书 A–E→机器学习/入门、乱码书→密码学/入门。
- 完整 pytest **188 全绿**；后端已重启加载新代码（uvicorn 8800）。

### 遗留事项（新会话注意）

- **ModelScope embedding 账户余额不足（429 insufficient balance，非每日限流）**：7 本新书 + 以后入馆的书只落词法索引，**语义搜索对这 7 本退化**。额度恢复/换 key 后跑 `state.indexer.backfill_vectors()` 补向量，自动清 `vector_pending`。P5「全真 `--real` 验收」也被此卡住（文档顶部状态原先写"每日 0 点重置"，实测为账户余额不足，需充值或换 key）。
- **raw 副本是无扩展名的 sha256 文件名**：`indexer.scan_files` 按扩展名过滤捡不到它们，`run()` 全量重建救不了补书室历史书——补索引必须按 `media_type` 路由 parser 或走 `index_book`（本次脚本做法）。

---

## 12. 补书室/书卡操作按钮（2026-08-15 新需求，已完成 ✅）

主人新需求三项 UI 操作按钮（本次会话实现，浏览器实测通过，194 pytest 全绿）：

1. **已上架的书**：书卡「📖 阅读」按钮旁新增「🗑 删除」按钮（软删除 → 档案馆，30 天可恢复）
2. **补书室建议区**：每本书新增「↩️ 送回待定区」按钮（清空分类建议，状态 reviewing → incoming）
3. **补书室待定区**：每本书新增「🗑 删除」按钮（软删除，同上）

### 后端

- `app/core/shelving.py`：新增 `Shelver.move_to_pending(book_id)`——校验 status=reviewing → 清 `suggest_*` + `card_path`（解除卡片关联，catalog_cards 记录与 vault/catalog/bk_*.md 文件保留作参考）→ status='incoming' → action ledger 记录（`action_type=unclassify`，agent=owner）。已上架（shelved）或待定区（incoming）书调用返回 400。
- `app/api/books.py`：新增 `POST /api/books/{book_id}/unclassify`（书不存在 → 404）；删除复用既有 `POST /api/books/{book_id}/delete`（P4-2 软删除，任意非 deleted 状态可删，含 incoming/reviewing）。

### 前端

- `web/src/api.js`：新增 `unclassify(id)`、`deleteBook(id)` 封装。
- `web/src/components/BookCard.vue`：shelved 书卡「阅读」旁加「🗑 删除」（`.danger-btn` 红色系，style.css 新增），点击弹 `ModalDialog` danger 确认（"将移入档案馆，30 天内可恢复"）→ `api.deleteBook` → `emit('deleted')` 通知父组件刷新。BookCard 现引入 `useLibraryStore` 做 toast。
- `web/src/views/Floors.vue`：建议区卡片按钮行加「↩️ 送回待定区」（调 `api.unclassify` → 刷新补书室+楼层+健康度）；待定区卡片加「🗑 删除」（`pendingDelete` + ModalDialog 确认 → `api.deleteBook` → 刷新）；`classifyBook` 改为 `api.classify(id, true)`（force——被送回待定区的书保留旧卡片，重新生成建议需强制覆盖，正常 incoming 书无卡片不受影响）；BookCard `@deleted` 监听刷新。
- `web/src/views/Reading.vue`：BookCard `@deleted="load"` 刷新最近阅读/最近入馆。

### 设计取舍

- **删除 = 软删除**（沿用 P4-2「AI 永不删书」约束）：只写 `deleted_at`，磁盘原始副本/catalog 卡片保留，档案馆 30 天可恢复；不是物理删除。
- **送回待定区 vs 撤销分类**：管理员账本 `undo classify` 只清 `suggest_*`、不改 status（书留建议区）；新 `unclassify` 额外把 status 改回 `incoming`（真正回到待定区），并解除 `card_path` 关联。
- **分类建议 force=true**：因卡片保留（`get_card` 命中会幂等 skip），送回待定区后再点「生成分类建议」统一 `force=true` 重新生成；顺带规避了旧 `undo classify` 后无法重新生成的既存小问题。

### 测试

- `tests/test_pending_actions.py` +6：unclassify reviewing→incoming（建议清空/卡片解除/账本记录）；shelved 书 unclassify 400；incoming 书 unclassify 400；不存在书 404；incoming+reviewing 书删除均软删除进档案馆；unclassify 后 force 重新 classify 成功。
- 全量 pytest **194 全绿**（188 + 6）。

### 浏览器实测（headed Chrome 127.0.0.1:8800）

- 楼层页 4 本已上架书卡均显示「📖 阅读 + 🗑 删除」；点删除弹 danger 确认框（取消未删）。
- 补书室建议区 9 本均显示「✅ 确认上架 / ↩️ 送回待定区 / 📖 查看」；点《批量测试书A》送回待定区 → toast「↩️ 已送回待定区」+ 该书移到待定区。
- 待定区《批量测试书A》显示「🔖 生成分类建议 / 🗑 删除 / 📖 查看」；点删除弹确认框（取消未删）。
- 重新「生成分类建议」→《批量测试书A》回到建议区（force=true 重蒸成功，💎75→80），演示库状态还原。
- 阅览室页 BookCard 同样显示「阅读 + 删除」（仅 shelved，符合需求）。

### 遗留事项

- 本需求未涉及 P6 规划（配置向导/打包/Obsidian 关联），P6 开工顺序不变（见 §10）。
- git：本次功能提交 `e695d74`（§11 遗留修复独立提交 `eb7b4f1`，文档补号 `2715f8d`）。

---

## 13. P6-3 傻瓜式配置向导（2026-08-15 已完成 ✅）

按 §10「实施顺序建议」第 1 项完成的第一个 P6 工作项。目标：别人装完软件后，**选供应商 → 选模型 → 填 key** 三步完成配置，无需懂 base_url/蒸馏模型/嵌入模型。

### 交付内容

**供应商预设库（一份 JSON，可扩展）**
- `config/providers.json`：6 个预设供应商——`deepseek`（官方 API，无嵌入 → 自动落 ModelScope 免费）、`modelscope`（免费额度标签）、`zhipu`、`siliconflow`、`openai`、`ollama`（本地、key 留空）。每项含 `name/logo/base_url/chat_models/distill_models/embed_models(可空)/tags/key_url/hint/embed_fallback/local`。
- 改库即可扩展供应商；`embed_models` 为空（如 deepseek）时嵌入自动配 ModelScope 免费（Qwen3-Embedding-0.6B）。

**后端三个新端点**
- `GET /api/providers`（`app/api/providers.py`）：返回预设库 + `current`（按 base_url 反查当前生效供应商/模型/key 掩码/上次测试时间）+ `needs_setup`（聊天 key 未配且 Ollama 未启用 → 首次需配置）。
- `POST /api/settings/apply-provider`（`app/api/settings.py`）：一步写 settings.json + secrets.json。embed_model 为空 → 自动 ModelScope 免费；**key 留空 = 保留已配置 key**（既不填、也没已配置才拒绝）；非本地配置**不误关** Ollama 本地通道（只启用不禁用）。
- `POST /api/settings/test-connection`：最小请求实测 chat（OpenAI 兼容补全）/ embed（embeddings）/ ollama（`/api/tags` 探测）；**key 留空 → 用已配置 key 实测**（不写盘）；成功任一路时写 settings.json 顶层 `last_conn_test`。
- 错误翻译（`app/providers.py::translate_error`）：401/403→"key 无效或无权限"、404→"模型名不存在"、429→"额度不足或请求过于频繁"、超时→"连接超时"、连接失败→"网络不通"。

**前端**
- `web/src/components/SetupWizard.vue`：三步向导（全屏遮罩，不用原生 prompt/confirm）——① 供应商卡片（免费/付费/本地/隐私标签 + hint）→ ② 模型下拉（默认推荐组合；无嵌入接口显示"自动 ModelScope 免费"fallback 提示）→ ③ key 输入（带"去获取 key"链接 + "key 只存本机 secrets.json"提示）+ 测试连接 + 保存并应用。
- `web/src/views/Settings.vue`：顶部新增「🩺 当前生效配置」健康卡（供应商 + 模型 + key 掩码 + Ollama 状态 + 上次测试时间）+「⚙️ 打开配置向导」按钮；原平铺表单降级为「🔧 高级设置」折叠保留。
- `web/src/App.vue`：首次启动检测 `needs_setup=true` → 自动弹出向导。
- `web/src/api.js`：新增 `providers() / applyProvider() / testConnection()`。

### 测试

- `tests/test_setup_wizard.py` +13：预设库存在性与当前识别；needs_setup True/False；apply-provider 各分支（deepseek 嵌入 fallback / 显式嵌入 / ollama 本地 / 未知供应商 400 / 缺模型 400 / 缺 key 400 / key 留空保留旧 key）；test-connection（成功写 last_conn_test / 失败不写 / ollama 探测 / key 留空用已配置）；`translate_error` 全分支；`detect_current`。
- 配置文件隔离：monkeypatch `app.api.settings.DEFAULT_CONFIG_PATH` / `app.api.providers.DEFAULT_CONFIG_PATH` 到 tmp_path，不污染真实 config/settings.json。
- 全量 pytest **207 全绿**（194 + 13）。

### 浏览器实测（headed Chrome 127.0.0.1:8800，真实配置）

- 设置页健康卡正确显示 DeepSeek / deepseek-v4-flash / Qwen3-Embedding-0.6B / key 掩码 / "Ollama 本地已启用" / 测试时间。
- 三步向导：DeepSeek 卡片选中 → 模型下拉预填当前配置 → fallback 提示"嵌入将自动使用 ModelScope 免费额度" → 第三步 placeholder"已配置 sk-8***b114（留空不修改）"。
- 测试连接（key 留空 → 用已配置 key）：chat **真实成功**（"聊天模型 deepseek-v4-flash：连接正常"）、embed **429 翻译**（"嵌入模型：额度不足或请求过于频繁（HTTP 429）"——ModelScope 账户余额不足的真实状态）、Ollama 未运行 → "网络不通（无法连接服务）"；未填 key → "未填 API key"。
- `last_conn_test` 已写入 config/settings.json（2026-08-15T18:44:31+08:00），健康卡显示"上次测试连接：2026/8/15 18:44:31"。
- 保存并应用（DeepSeek 相同配置 + key 留空）：toast「✅ 配置已保存（DeepSeek）」、向导关闭、**settings.json 保持原配置**（chat_model 不变、embed fallback 不变、**ollama.enabled 仍 true** 未被误关）。
- 高级设置折叠展开正常，保存按钮可用。

### 设计取舍

- **key 留空 = 保留**：向导允许"已配置 key 时留空保存"（改供应商/模型不重输 key）；后端同规则兜底。
- **Ollama 只启用不禁用**：非本地供应商配置不关 Ollama 本地隐私通道（此前实现会误关，已修）。
- **test-connection 不写盘 key**：只实测并记录 `last_conn_test` 时间；key 只在 apply-provider 时写 data/secrets.json。
- **首次启动弹向导的条件**：`needs_setup = 聊天 key 未配置 且 Ollama 未启用`（纯本地 Ollama 用户不被打扰）。

### 遗留 / 下一步

- **P6-4 已完成**：自动弹向导（App.vue）基础上在 SetupWizard.vue 加了"🏢 自定义楼层"可选步骤（保存配置后自动进入第 4 步，默认 4 标准楼层可增删改名后进入，可跳过），详见 §14（2026-08-18，git `cc37520`）。
- **P6-2 Obsidian 关联 A+B**、**P6-1 Inno Setup 打包**（含路径 CWD 坑修复）未动工，见 §10。
- git：本次功能提交 `b750dfe`（含 config/providers.json、app/providers.py、app/api/providers.py、app/api/settings.py、app/main.py、tests/conftest.py、tests/test_setup_wizard.py、web 前端 6 文件 + web/dist 重建产物）。
- 提示：ModelScope embedding 账户余额不足（429）仍卡住 P2 全真验收与语义索引回填，见 §11 遗留事项。
## 14. P6-4 首次启动向导「🏢 自定义楼层」步骤（2026-08-18 已完成 ✅）

按 §10「实施顺序建议」第 1 项收尾：P6-3 三步向导（§13）基础上，在 `web/src/components/SetupWizard.vue` 增加第 4 步「🏢 自定义楼层」（**可选**）。目标：别人装完软件配置好 API 后，顺手按来源媒介把默认楼层改造成自己的藏书结构再进入。

### 实现

- **流程**：第 3 步「保存并应用」成功后（`apply()`）不再直接关向导，而是 `step=4` + `loadWizardFloors()`；第 4 步提供「跳过」「🚀 完成，开始使用」两个出口（行为相同，文案区分）。
- **UI**：步骤指示器加 `④ 自定义楼层 <span class="wz-opt">可选</span>`；楼层行 = code（如 1F）+ 名称 + 「改名」「删除」按钮；顶部「＋ 新建楼层」行内输入（placeholder「新楼层名称（如：扫描件）」）；底部提示「有书的楼层不能删除；房间/书架等更细层级在设置页管理」。
- **交互纪律延续（P3.5）**：不用原生 prompt/confirm——新建/改名用行内输入（Enter 保存 / Esc 取消）；删除用**二次确认**（点「删除」→ 按钮变「确认删除？」→ 再点执行，点击他处自动取消）。
- **数据**：全部复用既有 `GET/POST/PUT/DELETE /api/floors`（`api.floors() / createFloor / updateFloor / deleteFloor`），新建自动编号 code F{n}、有书楼层删除 409 → toast 报错；后端零改动。
- **状态重置**：向导每次打开（`visible` watch）重置 `step=1`、清空楼层编辑状态，避免残留。
- **顺带修复 `.gitignore`**：原 `dist/` 规则同时忽略根目录 PyInstaller 产物**和** `web/dist/`（后者需入库供 FastAPI 托管开箱即用），导致新构建产物无法 add——改为 `/dist/` 仅忽略根目录。

### 测试

- 全量 pytest **207 全绿**（28.07s；本项纯前端，无新增测试文件）。
- 浏览器实测（headed Chrome 127.0.0.1:8800，真实配置）：
  - 设置页打开向导 → 三步走完（DeepSeek 预选 + key 留空）→ 点「保存并应用」→ toast「✅ 配置已保存（DeepSeek）」→ **自动进入第 4 步**，步骤指示器亮起「④ 自定义楼层 可选」；
  - 4 个标准楼层齐全：1F 电子书 / 2F 网页公众号 / 3F 聊天记录 / 4F 视频转写；
  - 「＋ 新建楼层」输入「测试楼层」→ toast「✅ 已新建楼层」→ 列表出现 **5F 测试楼层**（自动编号）；
  - 删除该楼层：点「删除」→ 变「确认删除？」→ 再点 → toast「✅ 已删除楼层」→ 回到 4 层；
  - 改名：2F 网页公众号 → 改名输入「网页」→ ✓ 保存 → toast「✅ 已改名」→ 再改回「网页公众号」（演示数据已还原）；
  - 「🚀 完成，开始使用」→ 向导关闭，回到设置页。

### 设计取舍

- **第 4 步只做楼层级操作**（增/删/改名），房间/书架保持设置页「楼层编辑」tab 管理（高级入口不动）——向导目标是把"来源媒介"定好，不引入层级复杂度。
- **保存配置后才进第 4 步**：保证"配置可用"是第一步；楼层是可选项，跳过零成本。
- **删除用二次确认而非确认框**：向导组件保持自包含（不引 ModalDialog），行内二段式最轻。

### 遗留 / 下一步

- **P6-2 Obsidian 关联 A+B**、**P6-1 Inno Setup 打包**（含路径 CWD 坑修复）未动工，见 §10；全流程验收（全新机器安装 → 首次启动向导含自定义楼层 → 关联 vault → 冒烟）待 P6-1 完成后执行。
- git：本次功能提交 `cc37520`（web/src/components/SetupWizard.vue + web/dist 重建产物 + .gitignore 修复），已 push origin/main（`66ab115..cc37520`）。
