# AI图书馆 · 项目交接文档

> 生成日期：2026-08-13
> 开发根目录：`D:\qwenpaw的工作空间\ai图书馆`
> 状态：**设计讨论已完成，尚未写代码**。本文档用于新会话无缝接续开发。

---

## 1. 项目目标

用 AI + Obsidian 搭建**"图书馆式"个人知识库**：Obsidian vault 是图书馆本体，AI agent 是图书管理员/采购员/档案员。

核心能力闭环：
- **入馆**：多格式素材（PDF/EPUB/技术文档/网页/公众号/聊天记录/视频转写）→ 清洗 → 不可变副本
- **编目分类**：楼层（按来源媒介）→ 房间（按语义主题）→ 书架 → 书，层级可自定义
- **蒸馏技能**：借仓颉 skill 把方法论类书蒸馏成可执行 SKILL.md（人审确认）
- **检索问答**：混合检索定位书 + 概要 + 自动调用相关技能作答，带 `[[wikilink]]` 引用
- **每日采购**：无订阅清单，采集方向 = 历史提问方向 + 热门源；配额按藏书比重由 AI 动态计算
- **主人主权**：所有 AI 操作是建议，主人确认才生效

### 空间地图（最终版，界面设计定稿）

```
┌────────────────────────────────────────────────────────────┐
│  🏛 大厅 │ 📖 楼层 │ 🌌 占星室 │ 💬 管理员 │ 🛒 采购员 │ 🗄 档案馆 │ ⚙ 设置 │
├────────────────────────────────────────────────────────────┤
│  · 建筑鸟瞰/健康度    · 今日日报    · 待办（分类/审阅）      │
│  · 快捷：扔书入馆 → 补书室 → 管理员取书分类 → 你确认 → 上架  │
│                                                           │
│  楼层（第一层=来源媒介，可自定义增删）                       │
│    ├ 一楼 PDF/电子书 ── 房间（语义主题）── 书架 ── 书        │
│    ├ 二楼 网页/公众号 ── 房间 ── 书架 ── 书                 │
│    ├ 三楼 聊天记录   ── 房间（按人）── 书架 ── 书           │
│    ├ 四楼 视频转写   ── 房间 ── 书架 ── 书                  │
│    └ 补书室（悬浮于楼层外）——建议区/待定区                  │
│  占星室：书/技能/主题/档案/对话 的星空图，未归位的星=补书室   │
│  管理员：对话（历史）+ 索引（按时间/书名/内容/语义）         │
│  采购员：对话 + 每日推荐 + 配额理由 + 历史日报               │
│  档案馆：原始副本/已删除(可恢复)/历史日报/蒸馏过程记录/备份   │
│  设置：API / 采集规则 / 托管程度滑杆 / 楼层房间编辑 / 画像    │
└────────────────────────────────────────────────────────────┘
```

## 2. 当前目录结构

```
D:\qwenpaw的工作空间\ai图书馆\        ← 开发根（当前为空，后续代码都放这里）
C:\Users\Z\.qwenpaw\workspaces\yh7ECT\research\   ← 调研产物（当前工作区）
    ├─ obsidian-ai-survey.md   调研报告（30+ 项目分析、架构结论）
    ├─ gh_search.py / gh_meta.py / gh_readme.py / gh_readme2.py  GitHub 调研脚本
    ├─ readmes.txt / readmes2.txt  头部项目 README 原文
```

## 3. 已经完成的功能

- ✅ GitHub 同类项目调研：30+ 项目、五条架构路线（插件内嵌 AI / Agent 维护 vault / LLM Wiki / 独立 RAG / MCP server），结论见 `obsidian-ai-survey.md`
- ✅ 需求澄清（多轮）：内容来源、蒸馏对象（AI 识别）、支持多 agent、免费 API+数据本机、可视化窗口
- ✅ 形态确认：**形态 C** = 独立 Python 后端服务 + Obsidian 联动（薄插件二期）+ Agent Skills 内核 + MCP 接口
- ✅ 界面设计：图书馆空间模型（见第 5 节与交接要点）
- ✅ 关键设计决策：补书室、主人权利机制、托管滑杆、对话归档、虚拟书架、健康度仪表盘

## 4. 正在开发的功能

- ⏳ **详细设计文档**（下一步立即执行，用户已等确认）
- 尚未开始写任何代码；MVP 范围已定义（P0–P4，见第 8 节）

## 5. 关键技术线

| 层 | 选型 |
|---|---|
| 后端 | Python 3.11+ / FastAPI（本地服务，一个端口 127.0.0.1:8800） |
| 存储 | SQLite FTS5（词法）+ LanceDB（向量），全部本机 |
| LLM/Embedding | ModelScope 免费 API（OpenAI 兼容）：DeepSeek V4 Flash、GLM-5.2、Qwen3.5-27B；embedding 用 Qwen3-Embedding-0.6B |
| 蒸馏 | 复用工作区现有 skill：`C:\Users\Z\.qwenpaw\workspaces\yh7ECT\skills\cangjie-skill` |
| 前端 | Vue 3 + Vite Web UI（空间隐喻界面） |
| Agent 集成 | QwenPaw agent-skills（主入口）+ MCP server（其他 agent）+ Obsidian 薄插件（二期） |
| 调度 | 每日定时采集（QwenPaw cron 或后端内置） |
| 参考架构 | ObsidianRAG（混合检索+copy-on-write 索引+引用溯源）、claude-obsidian（skills 化+单写入者+溯源账本）、SwarmVault（raw/wiki/schema 三层） |

**多角色 agent 设计**：管理员（编目/索引/问答）、采购员（采集/推荐）、档案员（归档/备份/原始副本）、蒸馏师（调仓颉产技能）。

**vault 目录规划**：`inbox/`（弃用，由补书室取代）→ `books/楼层/房间/书架/` + `catalog/`（图书卡片）+ `skills/`（蒸馏产物）+ `pending/`（采购候选）+ `archive/`（档案馆）+ `profile.md`（兴趣画像/借书证）。

## 6. 重要文件说明

- `交接文档.md`（本文件）：新会话第一份要读的文件
- `D:\qwenpaw的工作空间\ai图书馆\`：后续所有开发代码、文档的根目录
- `C:\Users\Z\.qwenpaw\workspaces\yh7ECT\research\obsidian-ai-survey.md`：调研结论与"值得复用/应避免"清单，写设计文档时的依据
- `C:\Users\Z\.qwenpaw\workspaces\yh7ECT\skills\cangjie-skill\SKILL.md`：蒸馏流水线要调用的 skill
- 环境记忆：`C:\Users\Z\.qwenpaw\workspaces\yh7ECT\MEMORY.md`（ModelScope 模型配置、WSL/Docker 经验）

## 7. 已知的问题 / 风险

- **选型雷区**：Reor 已归档停更、obsidian-mcp-tools 已归档、smart-composer 半停滞、mcp-obsidian 3 个月未推送——不作为依赖
- **Copilot 不可复用**：AGPL + 云服务分层 + 索引管线架构缺陷（RangeError/库损坏）
- **claude-obsidian 宿主绑定**：强依赖 Claude Code（Anthropic），大陆受限——只参考理念（skills 化/溯源/单写入者），宿主换成 QwenPaw
- **免费 API 隐私**：ModelScope 调用时文本经过阿里云服务器——私密内容需标记并走本地 Ollama 隔离
- **无搜索 API key**：采集主要靠公开热门源 + 提问方向提取，无法主动全网搜索
- **多格式解析成本**：EPUB/聊天导出/视频转写需额外解析或 ASR；视频转写成本高，默认不主动抓
- **暂无代码**：所有设计尚未实现，无代码层面的已知 bug

## 8. 下一步要做什么

1. **写详细设计文档**（用户已确认要"先出设计文档"）：架构、数据模型（楼层/房间/书架/书/技能/档案/对话/画像）、流程状态机（入馆→分类→蒸馏→问答→采购→归档）、窗口布局、技术选型、MVP 范围、开发顺序、隐私边界
2. 用户审阅设计文档 → 确认后进入实现
3. **P0 地基**：索引管线（扫描→chunk→embedding→LanceDB+FTS5，增量+copy-on-write），验收=100 篇笔记秒级索引、可重建
4. **P1 编目闭环**：图书卡片生成 + 基础问答带引用
5. **P2 蒸馏闭环**：接 cangjie-skill 自动蒸馏 + 人工审阅 + 技能路由
6. **P3 Web UI MVP**：大厅 + 楼层（馆藏）+ 补书室 + 管理员（对话/索引）+ 采购员（推荐/日报）
7. **P4 打磨**：占星室（星空图）、档案馆、阅览室、设置页、性能优化

## 9. 不能改动 / 需要注意的约束

- **主人权利（硬约束）**：AI 永不删书；分类/蒸馏/采购全部是"建议"，主人确认才生效；AI 不覆盖主人手动摆放；每个 AI 操作有记录 + 可撤销；删除仅限主人，进档案馆可恢复 30 天
- **保守模式默认**：采购员只出推荐清单（新书展示台），主人点"收藏"才入馆
- **分类层级**：楼层=来源媒介、房间=语义主题、层级依次下降（楼层→房间→书架→书），全部可增删改名；支持虚拟书架（按标签自动聚合）
- **补书室**：所有新书（主人扔的、采购员采购的、对话归档的）先到补书室，管理员提分类建议 → 主人确认 → 上架
- **隐私边界**：私密内容标记后永不发送 API；存储/索引全部本机；免费 API 数据会经手阿里云（需在 UI 明示）
- **多 agent 支持**：标准 SKILL.md + MCP，QwenPaw 为主入口
- **界面**：可视化窗口为主（Web UI 优先），命令行仅后备；Obsidian 薄插件二期
- **采集规则**：每日约 5 条、不主动抓视频（除非很热门）、配额按藏书比重由 AI 思考（新书是否影响权重由 AI 判断）、方向来自提问历史 + 热门源
- **成本**：优先零成本路线（ModelScope 免费模型、本地存储、公开热门源）
- **蒸馏质量**：SKILL.md 必须人工审阅（拒绝附原因 → 下次重蒸，参考 obsidian-llm-wiki 反馈环）
- **环境**：Windows 11 + WSL2/Docker 可用；ModelScope key 已配置（见 MEMORY.md，key 本身不记录）
