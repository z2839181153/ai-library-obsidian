# AI图书馆 · 项目交接文档

> 生成日期：2026-08-14（P3 完成更新）
> 开发根目录：`D:\qwenpaw的工作空间\ai图书馆`
> 状态：**P3 Web UI MVP 已完成**（105 测试全绿 + acceptance_p3 端到端验收通过，git `P3 提交后`）。本文档用于新会话无缝接续开发。

---

## 1. 项目目标

用 AI + Obsidian 搭建**"图书馆式"个人知识库**：Obsidian vault 是图书馆本体，AI agent 是图书管理员/采购员/档案员。

核心能力闭环：
- **入馆**：多格式素材（PDF/EPUB/技术文档/网页/公众号/聊天记录/视频转写）→ 清洗 → 不可变副本
- **编目分类**：楼层（按来源媒介）→ 房间（按语义主题）→ 书架 → 书，层级可自定义
- **蒸馏技能**：借仓颉 skill 把方法论类书蒸馏成可执行 SKILL.md（人审确认）
- **检索问答**：混合检索定位书 + 概要 + 自动调用相关技能作答，带 `[[wikilink]]` 引用
- **每日采购**：无订阅清单，采集方向 = 历史提问方向 + 热门源；配额按藏书比重由 AI 动态计算
- **主人主权**：所有 AI 操作是建议，主人确认才生效

### 空间地图（最终版，界面设计定稿）

```
┌────────────────────────────────────────────────────────────┐
│  🏛 大厅 │ 📖 楼层 │ 🌌 占星室 │ 💬 管理员 │ 🛒 采购员 │ 🗄 档案馆 │ ⚙ 设置 │
├────────────────────────────────────────────────────────────┤
│  · 建筑鸟瞰/健康度    · 今日日报    · 待办（分类/审阅）      │
│  · 快捷：扔书入馆 → 补书室 → 管理员取书分类 → 你确认 → 上架  │
│                                                           │
│  楼层（第一层=来源媒介，可自定义增删）                       │
│    ├ 一楼 PDF/电子书 ── 房间（语义主题）── 书架 ── 书        │
│    ├ 二楼 网页/公众号 ── 房间 ── 书架 ── 书                 │
│    ├ 三楼 聊天记录   ── 房间（按人）── 书架 ── 书           │
│    ├ 四楼 视频转写   ── 房间 ── 书架 ── 书                  │
│    └ 补书室（悬浮于楼层外）——建议区/待定区                  │
│  占星室：书/技能/主题/档案/对话 的星空图，未归位的星=补书室   │
│  管理员：对话（历史）+ 索引（按时间/书名/内容/语义）         │
│  采购员：对话 + 每日推荐 + 配额理由 + 历史日报               │
│  档案馆：原始副本/已删除(可恢复)/历史日报/蒸馏过程记录/备份   │
│  设置：API / 采集规则 / 托管程度滑杆 / 楼层房间编辑 / 画像    │
└────────────────────────────────────────────────────────────┘
```

## 2. 当前目录结构

```
D:\qwenpaw的工作空间\ai图书馆\
    ├─ 详细设计文档.md        ← 设计定稿（v1.0，约1000行，第8节定义 P0–P4）
    ├─ 交接文档.md            ← 本文件
    ├─ main.py                ← FastAPI 入口（127.0.0.1:8800，静态托管 web/dist + SPA fallback）
    ├─ config.py / state.py   ← 配置加载（.env / settings.json）+ 全局状态（含 ConnectionManager）
    ├─ app/
    │   ├─ api/               ← health.py、index.py、books.py、actions.py、floors.py（+CRUD）、ask.py、distill.py、skills.py
    │   │                       + P3 新增：ingest.py（入馆上传）、dashboard.py（健康度）、purchase.py（采购员）、
    │   │                       conversations.py（对话落库/历史/归档）、settings.py（设置读写）、ws.py（/ws/chat WebSocket）
    │   ├─ ws/                ← manager.py（ConnectionManager：线程安全广播，call_soon_threadsafe）
    │   ├─ db/                ← repo.py（P0-P3 CRUD，单写入锁）、schema.py（P0-P3 数据模型：+recommendations/daily_reports/profile）
    │   ├─ ingest/            ← parsers.py（md/txt/html/pdf）、cleaner.py（清洗+sha256 不可变副本）
    │   ├─ llm/               ← embeddings.py（Qwen3-Embedding-0.6B）、chat.py（DeepSeek-V4-Flash chat + chat_json 容错，重试 8 次）
    │   ├─ retrieval/         ← chunker.py、tokenizer.py、indexer.py（+index_book 单书索引）、searcher.py、vector_store.py
    │   ├─ core/              ← card_generator.py、shelving.py、qa.py
    │   ├─ distill/           ← artifacts.py、pipeline.py、executor_fake.py、executor_llm.py
    │   ├─ router/            ← skill_index.py、skill_router.py
    │   └─ vec/               ← vector_store.py（LanceDB）
    ├─ web/                   ← P3 前端：Vue 3 + Vite SPA（Pinia + vue-router + markdown-it）
    │   ├─ src/               ← App.vue（布局+toast+WS）、views/（Hall 大厅、Floors 楼层+补书室、BookDetail 阅览室、
    │   │                       Admin 管理员、Purchaser 采购员、Settings 设置+技能审阅+蒸馏确认）、stores/library.js、api.js
    │   ├─ dist/              ← 构建产物（已提交，FastAPI 直接托管，开箱即用）
    │   └─ package.json       ← dev 5173（proxy /api /ws → 8800）；build 出 dist
    ├─ scripts/               ← acceptance.py（P0）、acceptance_p1.py、acceptance_p2.py、acceptance_p3.py（P3 全鼠标验收）、setup_secrets.py
    ├─ tests/                 ← 30 个测试文件（105 个测试：P0/P1 49 + P2 22 + P3 34）
    ├─ config/settings.json   ← 运行参数（vault 路径、模型名、chunk 参数、distill 参数、采集规则）
    └─ P3任务清单.md          ← P3 任务分解（T1–T16 全勾选，含设计取舍与验收步骤）
C:\Users\Z\.qwenpaw\workspaces\yh7ECT\research\   ← 调研产物（obsidian-ai-survey.md 等）
```

## 3. 已经完成的功能

- ✅ GitHub 同类项目调研（30+ 项目、五条架构路线，结论见 `obsidian-ai-survey.md`）
- ✅ 需求澄清 + 形态确认：**形态 C** = 独立 Python 后端 + Obsidian 联动（薄插件二期）+ Agent Skills 内核 + MCP 接口
- ✅ 详细设计文档 v1.0（约1000 行：架构/数据模型/流程状态机/UI/技术选型/MVP P0–P4/隐私边界）
- ✅ **M0 脚手架**：FastAPI 骨架 + 配置加载 + 启动脚本（health 200 实测）
- ✅ **P0 索引管线**（git `4dd88de`，真实验收通过）：
  - 解析 md/txt/html/pdf → 清洗 → sha256 不可变副本
  - 标题分节 chunker + jieba 预分词（FTS5 词法索引）
  - ModelScope Qwen3-Embedding-0.6B embedding + SQLite 向量缓存（避免重复 API 调用）
  - LanceDB 向量存储（chunk_id merge-insert）+ FTS5 混合检索 RRF 融合
  - copy-on-write 增量索引 + 完整性检测（`/api/index/check`）
  - API：`/api/index/run|status|check`、`/api/search`
  - 验收（100 篇真实文档）：首建 **7.82s**、增量 **0.33s**、召回 **100%**、重建 3.27s；pytest 22 个全绿
- ✅ **P1 编目闭环**（本次会话，49 测试全绿）：
  - schema 扩展：floors/rooms/shelves/catalog_cards/actions/conversations/messages + 默认 4 楼层种子（幂等）
  - `app/llm/chat.py`：OpenAI 兼容 chat 客户端（`chat`/`chat_json` 容错解析、无 key 抛 `LLMUnavailable`、失败重试）
  - `app/core/card_generator.py`：图书卡片生成（书名/一句话简介/摘要/章节/关键概念/标签 + 蒸馏价值评估）+ 分类建议（楼层=媒介固定映射、房间/书架=LLM 建议优先匹配已有）；落盘 `catalog/bk_<id>.md`；私密书跳过 LLM；幂等 + force
  - `app/core/shelving.py`：确认上架（房间/书架不存在自动创建 + `.json`/README.md + `book.md`）、撤销上架（移回补书室）、撤销分类（清建议）
  - `app/core/qa.py`：基础问答（定位→概览→作答带 `[[catalog/bk_<id>]]` 引用→refs），无 key 降级返回原文片段
  - API：`/api/books`（列表/详情/classify/confirm/content）、`/api/actions`（+undo）、`/api/floors`、`/api/ask`
  - action ledger：classify/shelve 全记录 + 可撤销；repo 单写入锁（threading.RLock + busy_timeout）
  - 验收：`scripts/acceptance_p1.py` 7 步端到端通过（离线 Fake / `--real` 真 API）；49 pytest 全绿
- ✅ **P1 真实验收**（2026-08-14，真实 ModelScope API，全流程 **30.4s**）：
  - 卡片质量基线：蒸馏价值分 65-70（真实评估，Fake 固定 82 仅测试用）、房间建议"机器学习"贴合主题、摘要/章节/概念结构完整
  - 问答：能回答"神经网络是什么"并带 `[[catalog/bk_xxx]]` 引用定位原文
  - 修复：ModelScope 免费 API **间歇性空响应**（choices=None 不抛异常）→ ChatClient 显式检测 + 重试 5 次（间隔 1.5s→12s）+ max_tokens=1024
- ✅ **P2 蒸馏闭环**（本次会话，71 测试全绿）：
  - schema：skills 表（sk_* 注册表：draft/reviewing/approved/rejected/installed/blocked + reject_count + last_reject_reason + test_prompts）+ books 补 distill_slug/distill_status；旧库 CREATE IF NOT EXISTS 迁移
  - `app/distill/artifacts.py`：扫描 `vault/skills/<slug>/`（cangjie 规范产物），解析 SKILL.md frontmatter + 六段 R/I/A1/A2/E/B，校验 test-prompts.json（darwin 兼容、缺诱饵 → warning）
  - `app/distill/pipeline.py`：状态机 idle→stage0→awaiting→stage1→1.5→awaiting→stage2→3→4→5→done/failed/blocked；PIPELINE_STATE.md 断点续跑；阶段 0/1.5 主人确认（continue/skip/cancel）；审计复制到 archive/distill-logs/；action ledger（distill_start/stage/block/register…）；触发校验（已上架+methodology+distill_value≥60，force 可绕过）
  - `app/distill/executor_fake.py`：离线 FakeDistiller（确定性产物，可注入失败场景）
  - `app/distill/executor_llm.py`：LLMDistiller（GLM-5.2，串行 5 阶段）；prompt/模板单一事实源 = cangjie-skill 目录（extractors/*.md + templates/*.md）；三重验证 V1/V2/V3；RIA++ 构造；LLM 盲测标注 fallback
  - `app/router/skill_index.py` + `skill_router.py`：技能库 LanceDB 向量索引（cosine，阈值可配 0.6）；问答命中注入 SKILL.md（截断 8000 字符）→ system prompt；多技能 LLM 裁决（无 key 降级 top-1）
  - API：`/api/distill/{id}/start|status|confirm-stage`、`/api/skills`（列表/详情/approve/reject/unblock + `/api/skills/review` 极简 HTML 审阅页）
  - 阻塞逻辑：reject_count≥5 → skill blocked + 书 distill_status=blocked + start 409 拦截；unblock 重置
  - 修复（真实验收中发现）：chat_json max_tokens 默认 1024 → 4096（图书卡片 JSON 被截断）；ChatClient 空响应重试 5 次 → 8 次（间隔 3s→60s）；LLMDistiller verified.md 加 YAML 块（stage2 解析用，否则 0 技能注册）
  - 验收：`scripts/acceptance_p2.py` 10 步端到端通过（Fake 离线 1.3s）；真实 API 链路跑通至 stage3（502.9s 完成整轮蒸馏，GLM-5.2 响应 20-75s/次，受 429 余额限制未能完成全流程重跑）
- ✅ **P3 Web UI MVP**（本次会话，105 测试全绿 + 端到端验收通过）：
  - **后端补齐**（T1-T8）：schema +recommendations/daily_reports/profile 三表；repo CRUD；`/api/ingest`（multipart 文件/text 上传 + sha256 重复检测 + 入馆即索引 + WS 广播）；`/api/dashboard`（健康度：待分类/待审技能/索引状态/配额执行率/本周趋势/楼层鸟瞰/今日日报）；`/api/purchase`（今日配额+推荐清单[内置热门源种子+方向池启发式，无搜索 key]、收藏→入馆、反馈 ignore/not_interested 写回画像、日报）；`/api/conversations`（ask 落库 + 历史 + 归档为书[media_type=chat 走 ingest]）；`/api/settings`（GET/PUT，key 掩码、空值不改）；floors 扩展 CRUD（有书的楼层禁删 + action ledger）；`app/ws/manager.py` ConnectionManager（线程安全广播：记录 loop + call_soon_threadsafe）；`/ws/chat`（ping/pong、chat/ask_stream、notice 广播 book_ingested/distill_progress/skill_review_ready）；main.py 静态托管 web/dist + SPA fallback
  - **关键修复**：ingest 不能直接调 `indexer.run`（会把不在目录的书删除）→ Indexer 新增公开 `index_book()` 单书索引；FakeEmbed/FakeLLM 下建议区/待定区流程可测
  - **前端**（T9-T14）：Vue 3 + Vite + Pinia + vue-router + markdown-it；页面：大厅（健康度/鸟瞰/扔书入馆/日报）、楼层+补书室（建议区确认上架/待定区、书详情抽屉）、管理员（对话历史+聊天+索引搜索+操作账本撤销）、采购员（配额/推荐/收藏/反馈/日报）、设置（API 配置/采集规则/托管滑杆/楼层编辑/技能审阅并入/蒸馏确认并入）；WS 连接 + toast 通知
  - **验收**：`scripts/acceptance_p3.py` 10 步端到端（扔书→WS 通知→分类建议→确认上架→提问落库→生成推荐→收藏入馆→反馈→设置改楼层→健康度）全程 HTTP 无命令行，通过；105 pytest 全绿（旧 71 + P3 34）
  - **前端构建**：`web/` 内 `npm install`（淘宝镜像）+ `npm run build` 出 dist；dist 已提交（FastAPI 直接托管开箱即用）

## 4. 正在开发的功能

- ✅ **P2 蒸馏闭环已完成**（见 §3；git `cb85bdb`）
- ✅ **P3 Web UI MVP 已完成**（见 §3；git 待提交，本次会话完成后 push）
- 尚未开始：P4 打磨（占星室/档案馆/阅览室增强/流式聊天/性能优化）

## 5. 关键技术线

| 层 | 选型 |
|---|---|
| 后端 | Python 3.11+ / FastAPI（本地服务，127.0.0.1:8800） |
| 存储 | SQLite FTS5（词法）+ LanceDB（向量），全部本机 |
| LLM/Embedding | ModelScope 免费 API（OpenAI 兼容）：DeepSeek V4 Flash、GLM-5.2、Qwen3.5-27B；embedding 用 Qwen3-Embedding-0.6B（key 已配置，见 MEMORY.md，key 本身不记录） |
| 检索 | 混合检索：FTS5 词法 + LanceDB 向量 → RRF 融合 → 按书聚合 |
| 蒸馏 | 复用 `C:\Users\Z\.qwenpaw\workspaces\yh7ECT\skills\cangjie-skill` |
| 前端 | Vue 3 + Vite Web UI（空间隐喻界面） |
| Agent 集成 | QwenPaw agent-skills（主入口）+ MCP server（其他 agent）+ Obsidian 薄插件（二期） |
| 调度 | 每日定时采集（QwenPaw cron 或后端内置） |
| 参考架构 | ObsidianRAG（混合检索+copy-on-write 索引+引用溯源）、claude-obsidian（skills 化+单写入者+溯源账本）、SwarmVault（raw/wiki/schema 三层） |

**多角色 agent 设计**：管理员（编目/索引/问答）、采购员（采集/推荐）、档案员（归档/备份/原始副本）、蒸馏师（调仓颉产技能）。

**vault 目录规划**：`books/楼层/房间/书架/` + `catalog/`（图书卡片）+ `skills/`（蒸馏产物）+ `pending/`（采购候选）+ `archive/`（档案馆）+ `profile.md`（兴趣画像/借书证）。

## 6. 重要文件说明

- `交接文档.md`（本文件）：新会话第一份要读的文件
- `详细设计文档.md`：设计定稿，实现时的唯一依据；第 8 节是 MVP P0–P4 分解
- `main.py` / `app/`：P0 代码主体（见第 2 节目录树）
- `scripts/acceptance.py`：P0 验收脚本（`--real` 用真实 ModelScope API，需 secrets 就位）
- `scripts/acceptance_p1.py`：P1 验收脚本（7 步端到端：入馆→索引→编目→补书室→上架→问答→撤销；默认 Fake 离线，`--real` 走真 API）
- `scripts/acceptance_p2.py`：P2 验收脚本（10 步端到端：蒸馏→注册→批准+索引→路由命中→拒绝 5 次阻塞→解除；默认 Fake 离线 1.3s，`--real` 走真 API 约 8-10 分钟）
- `scripts/acceptance_p3.py`：P3 验收脚本（10 步全鼠标流程：扔书→WS 通知→分类→上架→提问→推荐→收藏→反馈→设置→健康度；Fake 离线，无需 key；注意 TestClient 在 WS 连接期间主线程不能发 HTTP——后台线程触发广播 + timeout 兜底）
- `scripts/setup_secrets.py`：把 ModelScope key 写入 `secrets.json`（不入 git）
- `tests/`：105 个测试（P0 22 + P1 27 + P2 22 + P3 34），改动后跑 `pytest` 防回归
- `P1任务清单.md` / `P2任务清单.md` / `P3任务清单.md`：各阶段任务分解（均已全勾选，含设计取舍与验收步骤）
- 调研依据：`C:\Users\Z\.qwenpaw\workspaces\yh7ECT\research\obsidian-ai-survey.md`
- 蒸馏 skill：`C:\Users\Z\.qwenpaw\workspaces\yh7ECT\skills\cangjie-skill\SKILL.md`
- 环境记忆：`C:\Users\Z\.qwenpaw\workspaces\yh7ECT\MEMORY.md`（ModelScope key 存放位置、归档阈值 60% 等）

## 7. 已知的问题 / 风险

- **无搜索 API key**：采集主要靠公开热门源 + 提问方向提取，无法主动全网搜索
- **免费 API 隐私**：ModelScope 调用时文本经过阿里云——私密内容需标记并走本地 Ollama 隔离
- **多格式解析成本**：EPUB/聊天导出/视频转写需额外解析或 ASR；视频转写成本高，默认不主动抓
- **PDF 解析**：当前用简单文本抽取，复杂版式（扫描件/多栏）质量有限，P1 后可按需升级
- **选型雷区**：Reor/obsidian-mcp-tools 已归档、smart-composer 半停滞——不作为依赖；claude-obsidian 宿主绑定不采用
- **P1 依赖 LLM 摘要质量**：图书卡片生成质量取决于免费模型表现；离线验收用 FakeLLM 已验证结构正确，**P1 真实验收已跑**（30.4s，卡片质量基线见 §3）
- **ModelScope 免费 API 额度/限流**（P2 真实验收遇到，重要）：GLM-5.2 蒸馏时偶发**空响应**（已重试 8 次缓解）与 **429 insufficient balance**（账户级限额，非代码问题）；真实验收全流程受此影响，代码链路已用 stub LLM 验证（7 阶段 + 注册 ≥2 技能），待额度恢复后重跑 `scripts/acceptance_p2.py --real` 记录质量基线
- **真实蒸馏耗时**：GLM-5.2 单次响应 20-75s，整本书蒸馏约 8-10 分钟（502.9s 实测）；长书会更久——P3 需考虑进度推送与后台任务管理
- **技能路由阈值 0.6 待校准**：FakeEmbed 余弦尺度与真实 embedding 不同（离线测试用 description 本身作查询命中）；真实验收通过后按实际命中情况校准 `settings.distill.route_threshold`
- **FakeEmbed 语义尺度**：词袋哈希 64 维，自然问法余弦普遍 <0.3——离线测试/验收中"命中"用技能 description 本身（cos=1.0），仅验证链路而非语义；语义命中靠 --real 校准
- **单写入锁为进程内锁**：`threading.RLock + busy_timeout` 只保证单进程串行；多进程场景需升级（当前本地单进程可接受）。WS ConnectionManager 已做线程安全（loop + call_soon_threadsafe），同步端点广播不再丢消息
- ~~conversations/messages 未落库~~：**P3 已实现**（/api/ask 落库 + 历史 + 归档为书）
- **前端手工浏览器验收未做**：acceptance_p3 走 TestClient 全 HTTP 已通过，但真实浏览器 127.0.0.1:8800 的视觉/交互验收（拖拽上传、toast、路由跳转）未人工确认，P4 开工前建议开服务点一遍
- **P2 真实蒸馏基线未补**：ModelScope 429 余额限制未恢复，`acceptance_p2.py --real` 全流程未重跑

## 8. 下一步要做什么

1. **浏览器手工验收 P3**（建议最先做）：`start.ps1` 起服务 → 打开 127.0.0.1:8800 → 鼠标走一遍"扔书入馆→补书室 WS toast→分类建议→确认上架→管理员提问→采购员生成推荐→收藏→反馈→设置改楼层"，确认视觉与交互无问题；发现小问题直接在 P4 一并修
2. **P2 真实验收补跑**（额度恢复后）：`python scripts/acceptance_p2.py --real`（GLM-5.2 蒸馏 1 本真实方法论书），记录耗时/通过率/技能质量基线；据此校准 `settings.distill.route_threshold`（当前 0.6 为默认值）
3. **P4 打磨**（设计文档 §11 P4）：
   - 占星室：书/技能/主题/档案/对话星空图（Graph View），未归位的星=补书室书，可按类型筛选/按入馆时间着色
   - 档案馆：原始不可变副本/已删除(可恢复30天)/历史日报/蒸馏过程记录/备份导出
   - 阅览室增强：原文渲染+章节导航+高亮+右侧"相关技能/相关笔记"联动
   - 设置完善：借书证=兴趣画像可视化（藏书分布饼图+方向池+收藏/拒绝历史，可编辑）
   - 流式聊天：/ws/chat 逐字输出（当前 ask_start→ask_done 完整回答）
   - 性能/细节：虚拟书架标签视图、健康度趋势图、WS 重连、移动端适配
4. 每完成一个 P 阶段：跑 pytest + 真实验收，git commit 推送 GitHub（ai-library-obsidian，公开）

## 9. 不能改动 / 需要注意的约束

- **主人权利（硬约束）**：AI 永不删书；分类/蒸馏/采购全部是"建议"，主人确认才生效；AI 不覆盖主人手动摆放；每个 AI 操作有记录 + 可撤销；删除仅限主人，进档案馆可恢复 30 天
- **保守模式默认**：采购员只出推荐清单，主人点"收藏"才入馆
- **分类层级**：楼层=来源媒介、房间=语义主题、层级依次下降（楼层→房间→书架→书），全部可增删改名；支持虚拟书架（按标签自动聚合）
- **补书室**：所有新书先到补书室，管理员提分类建议 → 主人确认 → 上架
- **隐私边界**：私密内容标记后永不发送 API；存储/索引全部本机；免费 API 数据会经手阿里云（UI 明示）
- **多 agent 支持**：标准 SKILL.md + MCP，QwenPaw 为主入口
- **界面**：可视化窗口为主（Web UI 优先），命令行仅后备；Obsidian 薄插件二期
- **采集规则**：每日约 5 条、不主动抓视频、配额按藏书比重由 AI 思考、方向来自提问历史 + 热门源
- **成本**：优先零成本路线（ModelScope 免费模型、本地存储、公开热门源）
- **蒸馏质量**：SKILL.md 必须人工审阅（拒绝附原因 → 下次重蒸）
- **环境**：Windows 11 + WSL2/Docker 可用；ModelScope key 在 `secrets.json`（不入 git）；git 仓库 `ai-library-obsidian`（GitHub 公开，GCM 已存 token）
- **归档纪律**：本窗口上下文进度到 ~60%（出现 `[context compressed]` 信号或会话很长）时，立即用本模板重写 `交接文档.md`，勿等提示
