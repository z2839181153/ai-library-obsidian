# AI图书馆 · 详细设计文档

> 版本：v0.2（2026-08-14 更新：P5 桌面化 + 批量入馆 + 离线阅读 + 星图增强，主人确认追加；v0.1 为原设计稿）
> 日期：2026-08-13（v0.1）→ 2026-08-14（v0.2）
> 关联：`交接文档.md`（第 8 节要求本文件）；调研依据 `research/obsidian-ai-survey.md`
> 开发根目录：`D:\qwenpaw的工作空间\ai图书馆\`

---

## 0. 阅读指引

本文档是**实现蓝图**：实现阶段按章节直接落地。阅读顺序建议：

1. 第 1 章（目标回顾，确认范围）
2. 第 2 章（总体架构，理解系统全貌）
3. 第 5 章（vault 目录 + 数据模型，理解"东西放哪、长什么样"）
4. 第 6 章（核心流程状态机，理解"东西怎么流转"）
5. 第 7 章（索引与检索，理解"AI 怎么找到东西"）
6. 第 8、9 章（多角色 agent + 交互协议，理解"入口怎么写"）
7. 第 10、11 章（UI 布局 + 隐私边界）
8. 第 12、13 章（MVP 范围 + 开发顺序 + 风险）

**约定**：
- 用词：`主人`=用户本人；`管理员/采购员/档案员/蒸馏师`=AI 角色；`上架`=主人确认后书进入楼层正式馆藏。
- 所有 AI 动作都是"建议 + 记录 + 可撤销"，主人确认才生效（硬约束，见交接文档 §9）。
- 表中字段带 `★` 表示该字段是主键或唯一键。

---

## 1. 项目目标回顾

用 AI + Obsidian 搭建"图书馆式"个人知识库。能力闭环（同交接文档 §1）：

**入馆 → 编目分类 → 蒸馏技能 → 检索问答 → 每日采购 → 主人主权**

关键设计原则（本文档所有章节不得违背）：

| 原则 | 含义 | 落点 |
|---|---|---|
| P1 主人主权 | AI 永不删书、只建议、可撤销 | 第 6.10 节 action ledger + 事务写入 |
| P2 保守默认 | 采购只出清单，主人点"收藏"才入馆 | 第 6.7 节 |
| P3 本地优先 | 存储/索引全在本机；私密内容永不发 API | 第 10 章 |
| P4 零成本 | ModelScope 免费模型 + 公开源 | 第 3 章 |
| P5 蒸馏人审 | SKILL.md 必须人工审阅，拒绝附原因可重蒸 | 第 6.5 节 |
| P6 混合检索溯源 | FTS5 词法 + LanceDB 向量，回答带 `[[wikilink]]` 引用 | 第 7 章 |

---

## 2. 总体架构

### 2.1 形态回顾

**形态 C**：独立 Python 后端服务 + 桌面壳（P5）+ Obsidian 联动（薄插件二期）+ Agent Skills 内核 + MCP 接口。

```
┌─────────────────────────────────────────────────────────────────────┐
│                         五个入口（同一后端）                          │
│  ① 桌面壳 pywebview（Windows 桌面程序，双击启动+自动拉起后端）★主窗口 │
│  ② Web UI（浏览器 http://127.0.0.1:8800，同一界面，开发/远程备用）   │
│  ③ QwenPaw agent-skills（主入口，/kb-* 命令）                        │
│  ④ MCP server（任意 agent：Claude Code / Codex / 其他）             │
│  ⑤ Obsidian 薄插件（二期：侧边栏提问 + 引用跳原文）                  │
└───────────────┬─────────────────────────────────────────────────────┘
                │ HTTP(REST) + WebSocket / MCP / skill 调用
┌───────────────▼─────────────────────────────────────────────────────┐
│                    Python 后端（FastAPI，127.0.0.1:8800）            │
│  ┌────────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌─────────┐ │
│  │ 入馆清洗器  │→│  编目器   │→│价值评估器 │→│ 蒸馏流水线 │→│ 检索问答器 ││
│  │ ingest     │ │ catalog  │ │ assess    │ │ distill  │ │ ask/rag  │ │
│  └────────────┘ └──────────┘ └──────────┘ └──────────┘ └─────────┘ │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ 采购员（collect/recommend/daily report）│ 档案员（archive/backup）│  │
│  │ 对话引擎（chat/stream/archive-to-book）│ 索引器（index/cow）      │  │
│  └──────────────────────────────────────────────────────────────┘  │
│  存储：SQLite(FTS5 词法) + LanceDB(向量) + 文件系统(vault)          │
└───────────────┬─────────────────────────────────────────────────────┘
                │ 读/写（单写入者事务，见 6.10）
┌───────────────▼─────────────────────────────────────────────────────┐
│   Obsidian vault = 真源（普通 Markdown 文件，可被 Obsidian 打开）     │
│   books/楼层/房间/书架/ · catalog/ · skills/ · pending/ · archive/   │
│   profile.md · schema.md · .meta/（机器元数据）                     │
└─────────────────────────────────────────────────────────────────────┘
                │ 调用时数据经过（免费 API 边界，见第 10 章）
┌───────────────▼─────────────────────────────────────────────────────┐
│   ModelScope（OpenAI 兼容）：DeepSeek-V4-Flash / GLM-5.2 /           │
│   Qwen3-Embedding-0.6B；私密内容走本地 Ollama（隔离）                │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 分层与模块清单

| 层 | 模块 | 职责 | 关键类/文件（实现时） |
|---|---|---|---|
| 入口层 | `api_rest.py` | REST 端点（见 §9.2） | FastAPI router |
| 入口层 | `api_ws.py` | WebSocket 聊天/进度推送 | WS 协议见 §9.3 |
| 入口层 | `mcp_server.py` | MCP 工具暴露（见 §8.3） | stdio transport |
| 入口层 | `kb_skills/` | QwenPaw SKILL.md 入口（见 §8.2） | markdown + 脚本 |
| 核心层 | `ingest/` | 入馆清洗：格式解析→文本→不可变副本 | `cleaner.py` / `parsers/` |
| 核心层 | `catalog/` | 图书卡片生成、楼层/房间/书架建议 | `card_generator.py` |
| 核心层 | `assess/` | 蒸馏价值评估 | `value_assessor.py` |
| 核心层 | `distill/` | 调 cangjie-skill 的蒸馏编排 | `pipeline.py` |
| 核心层 | `retrieval/` | 混合检索 + copy-on-write 索引 | `indexer.py` / `searcher.py` |
| 核心层 | `router/` | 技能路由 | `skill_router.py` |
| 核心层 | `ask/` | 问答编排（定位→概览→路由→作答） | `orchestrator.py` |
| 核心层 | `purchase/` | 采集器 + 荐书评估 + 配额 + 日报 | `collector.py` / `quota.py` |
| 核心层 | `archive/` | 归档/删除/恢复/备份 | `archivist.py` |
| 核心层 | `chat/` | 对话会话 + 归档为书 | `conversation.py` |
| 核心层 | `agent/` | 多角色 agent 定义与调度 | `roles.py` |
| 基础层 | `db/` | SQLite 访问层（schema 见 §5.3） | `schema.sql` / `repo.py` |
| 基础层 | `vec/` | LanceDB 封装 | `vector_store.py` |
| 基础层 | `llm/` | 模型客户端（ModelScope/Ollama 双通道） | `client.py` / `providers.py` |
| 基础层 | `ledger/` | 操作日志 + 撤销（事务写入） | `action_ledger.py` |
| 基础层 | `config/` | 设置（托管滑杆/API key/采集规则） | `settings.json` |
| 前端 | `web/` | Vue 3 SPA（静态资源由后端托管） | `frontend/` |

### 2.3 关键取舍（依据调研报告）

| 决策 | 理由 |
|---|---|
| 后端独立进程而非 Obsidian 插件内嵌 | 可跑 Python 生态；其他 agent（MCP）可接入；不被插件沙箱限制 |
| vault 即真源（普通文件） | Obsidian 原生可打开；不依赖私有缓存；claude-obsidian 已验证 |
| 单写入者 + 事务 | 避免多 agent 并行写竞态（调研"应避免"清单第 6 条） |
| 不依赖已归档项目 | Reor / obsidian-mcp-tools / smart-composer 不作为依赖 |
| 不依赖 Claude Code 宿主 | claude-obsidian 只参考理念，宿主换 QwenPaw + ModelScope |
| 桌面壳优先于 Obsidian 插件（P5） | 主人 2026-08-14 确认：先做 pywebview 桌面壳满足"像软件"，Obsidian 薄插件二期再议 |

---

## 3. 技术选型明细

### 3.1 后端

| 项 | 选型 | 版本/说明 |
|---|---|---|
| 语言 | Python | 3.11+（Windows 11 原生或 WSL2 内运行，见 3.5） |
| Web 框架 | FastAPI + uvicorn | 一个进程、一个端口 `127.0.0.1:8800`，托管静态前端 |
| 异步 | asyncio | 聊天流式、采集、索引均异步 |
| 词法索引 | SQLite + FTS5 | Python 内置 sqlite3 编译需含 FTS5（3.11 自带） |
| 向量索引 | LanceDB | 本机嵌入式、无服务进程；写入磁盘目录 |
| 嵌入向量维度 | 1024 | Qwen3-Embedding-0.6B 输出维度 |
| 任务调度 | 内置 APScheduler 或 QwenPaw cron | 每日 08:00 采购；采集/索引周期任务 |
| 解析库 | markdown/`pypdf`/`trafilatura`/`ebooklib` | 见 3.4 |
| 搜索爬取 | `feedparser`(RSS) + `httpx` + readability | 公开源，无 API key |
| 测试 | pytest | 管线单测 + 验收脚本 |

### 3.2 LLM / Embedding（ModelScope 免费，OpenAI 兼容）

| 用途 | 主选模型 | 备选 | 说明 |
|---|---|---|---|
| 问答/对话 | `deepseek-ai/DeepSeek-V4-Flash-0731` | `ZhipuAI/GLM-5.2` | 快、便宜；GLM 用于复杂推理 |
| 编目/卡片生成 | `deepseek-ai/DeepSeek-V4-Flash-0731` | `ZhipuAI/GLM-5.2` | 结构化 JSON 输出 |
| 蒸馏价值评估 | `deepseek-ai/DeepSeek-V4-Flash-0731` | `ZhipuAI/GLM-5.2` | 输出评估 JSON |
| 蒸馏流水线 | `ZhipuAI/GLM-5.2` | `Qwen/Qwen3.5-27B` | 长文本、多 agent 提取、结构化产物，选更强模型 |
| 技能路由裁决 | `deepseek-ai/DeepSeek-V4-Flash-0731` | — | 多技能冲突时裁决 |
| Embedding | `Qwen/Qwen3-Embedding-0.6B` | `Qwen3-Reranker-0.6B`（P4 rerank） | 1024 维，中文友好 |
| 私密内容（隔离） | 本地 Ollama（如 `qwen2.5:7b`） | — | 见第 10 章；不可用时降级为"跳过 AI 处理，仅本地索引" |

**调用约定**：
- 统一 `llm/client.py` 抽象，`chat(messages, model, json_mode)` / `embed(texts)`。
- 模型名在 `config/settings.json` 可改（UI 设置页提供下拉）。
- 所有 LLM 调用带 `reason` 参数记录用途（进 action ledger 与日报）。

### 3.3 前端

| 项 | 选型 | 说明 |
|---|---|---|
| 框架 | Vue 3 + Vite + TypeScript | SPA，静态构建产物由 FastAPI 托管 |
| UI 库 | Element Plus（或 Naive UI） | 表单/抽屉/卡片成熟组件 |
| 图表 | ECharts | 占星室星空图（graph 系列）、健康度仪表盘、藏书分布饼图 |
| 状态 | Pinia | 全局（当前楼层/补书室计数/通知） |
| 通信 | fetch + WebSocket | REST 业务操作；WS 流式聊天/进度/通知 |
| 编辑器/渲染 | markdown-it + highlight.js | 阅览室原文渲染、引用跳转 |
| 桌面壳 | pywebview（P5） | Windows 桌面程序：WebView2 加载本地 `web/dist`，双击启动 + 自动拉起/关闭 FastAPI 后端，托盘常驻；WebView2 缺失时降级浏览器打开 |

### 3.4 多格式解析（入馆清洗）

| 来源 | 格式 | 解析器 | MVP 支持 |
|---|---|---|---|
| 电子书 | PDF | `pypdf`（文本层） | ✅ P0 |
| 电子书 | EPUB | `ebooklib` → 章节 html → `trafilatura` | ✅ P1 |
| 技术文档 | Markdown/纯文本 | 直接读取 | ✅ P0 |
| 网页/公众号 | HTML | `trafilatura`（正文抽取，兼容微信文章） | ✅ P0 |
| 聊天记录 | JSON/TXT（如 douyin-chat-export 导出） | 自定义 parser → 按人分房间 | ✅ P1 |
| 视频转写 | SRT/VTT/转写稿 | 直接读取字幕文本；**默认不主动抓视频** | P2+ |
| 音频/视频原文件 | — | 需 ASR（whisper），成本高，默认不做 | 视需要 |

### 3.5 运行环境

- Windows 11 原生跑 Python 服务（开发期最快路径）；WSL2/Docker 作为备选（已具备条件，见 MEMORY.md 经验）。
- 数据目录：`D:\qwenpaw的工作空间\ai图书馆\data\`（SQLite + LanceDB + 日志）。
- vault 目录：`D:\qwenpaw的工作空间\ai图书馆\vault\`（Obsidian 可直接打开该文件夹）。

---

## 4. 命名与编码规范

- 文件/目录：小写 + 连字符（`kebab-case`），中文名仅用于 vault 内楼层/房间/书架（Obsidian 友好）。
- ID：`bk_`（书）、`fl_`（楼层）、`rm_`（房间）、`sh_`（书架）、`sk_`（技能）、`cv_`（对话）、`ar_`（档案）、`act_`（操作）。UUID4 简写（取 12 位）。
- 书 slug：由书名清洗生成，仅 `[a-z0-9-]`。
- 时间：ISO 8601 + 时区 `Asia/Shanghai`，存 UTC 转本地展示。
- JSON 字段命名：`snake_case`。

---

## 5. 数据模型与 vault 目录结构

### 5.1 vault 目录树（真源 = 普通文件）

```
vault/
├── schema.md                  # 结构与约定（人机共同演化，SwarmVault 理念）
├── profile.md                 # 兴趣画像/借书证（frontmatter 见 5.4）
├── README.md                  # 图书馆导览（人类可读）
├── books/                     # 馆藏（按楼层/房间/书架，可自定义）
│   ├── 1F-电子书/             # 楼层目录名：<编号>-<楼层名>
│   │   ├── .floor.json        # 楼层元数据（机器可读）
│   │   ├── README.md          # 楼层说明（人类可读）
│   │   ├── 人工智能/          # 房间（语义主题）
│   │   │   ├── .room.json
│   │   │   ├── LLM与Agent/    # 书架
│   │   │   │   ├── .shelf.json
│   │   │   │   └── 书名/      # 书 = 目录（含 book.md + 章节文件）
│   │   │   │       └── book.md
│   │   │   └── 机器学习/
│   │   └── 哲学/
│   ├── 2F-网页公众号/
│   ├── 3F-聊天记录/           # 房间按人
│   └── 4F-视频转写/
├── catalog/                   # 图书卡片（平行于 books/ 的索引层）
│   └── bk_<id>.md             # 每本书一张卡片（格式见 5.5）
├── skills/                    # 蒸馏产物
│   └── <book-slug>/SKILL.md   # cangjie-skill 输出目录结构
├── pending/                   # 采购候选（推荐清单的落盘，供 Obsidian 可见）
│   └── 2026-08-13.md          # 当日推荐清单（日报卡片）
├── archive/                   # 档案馆
│   ├── raw/                   # 原始不可变副本（内容寻址）
│   ├── deleted/               # 主人删除的书（30 天可恢复）
│   ├── daily-reports/         # 历史日报
│   ├── distill-logs/          # 蒸馏过程记录（candidates/rejected 审计）
│   └── backups/               # 备份导出（zip）
├── conversations/             # 对话归档（主人确认后从补书室上架）
└── .meta/                     # 系统内部索引状态（勿手动编辑）
    ├── index_state.json       # copy-on-write 索引修订状态
    └── vault_watch.db         # 文件变更跟踪（hash 缓存）
```

**设计要点**：
- `books/` 是**呈现层**（主人和 Obsidian 看的），`catalog/` 是**索引层**（AI 读的）。两者通过 `book_id` 关联。
- 书入馆时原始文件复制进 `archive/raw/<hash>` 作为不可变副本；`books/` 下放**清洗后的可读 Markdown**（正文 + 元信息）。
- 楼层/房间/书架目录自带 `.json` 元数据 + `README.md`，增删改全部经后端（保证一致），主人也可以在 Obsidian 里直接编辑——后端检测变更后同步（单向：文件是源）。

### 5.2 楼层元数据（`.floor.json`）

```json
{
  "floor_id": "fl_1f2a3b4c5d6e",
  "name": "电子书",
  "code": "1F",
  "media_type": "pdf|epub|ebook",
  "description": "PDF/EPUB 电子书",
  "order": 1,
  "custom": false,
  "created_at": "2026-08-13T10:00:00+08:00",
  "rooms": ["人工智能", "哲学"]
}
```

默认 4 个内置楼层：`1F 电子书`、`2F 网页公众号`、`3F 聊天记录`（房间=按人）、`4F 视频转写`。楼层/房间/书架均可增删改名（设置页）。

### 5.3 SQLite 数据模型（核心表）

> FTS5 用外部内容表方式建虚拟表；向量存 LanceDB（`chunks` 表存 chunk 文本与元数据，向量 id 用 `chunk_id`）。

**floors / rooms / shelves**（目录的 DB 镜像）

```sql
CREATE TABLE floors (
  floor_id   TEXT PRIMARY KEY,        -- fl_*
  name       TEXT NOT NULL,
  code       TEXT,                    -- 1F
  media_type TEXT,                    -- pdf/epub/web/chat/video/other
  description TEXT,
  ord        INTEGER DEFAULT 0,
  custom     INTEGER DEFAULT 0,
  created_at TEXT
);
CREATE TABLE rooms (
  room_id    TEXT PRIMARY KEY,        -- rm_*
  floor_id   TEXT REFERENCES floors(floor_id),
  name       TEXT NOT NULL,
  description TEXT,
  ord        INTEGER DEFAULT 0,
  created_at TEXT
);
CREATE TABLE shelves (
  shelf_id   TEXT PRIMARY KEY,        -- sh_*
  room_id    TEXT REFERENCES rooms(room_id),
  name       TEXT NOT NULL,
  description TEXT,
  ord        INTEGER DEFAULT 0,
  created_at TEXT
);
```

**books**（书的状态机主体）

```sql
CREATE TABLE books (
  book_id     TEXT PRIMARY KEY,       -- bk_*
  title       TEXT NOT NULL,
  author      TEXT,
  slug        TEXT,
  media_type  TEXT,                   -- pdf/epub/web/chat/video/other
  source_uri  TEXT,                   -- 来源（本地路径/URL/对话导出）
  content_hash TEXT,                  -- 内容寻址（sha256）
  raw_path    TEXT,                   -- archive/raw/<hash> 不可变副本
  vault_path  TEXT,                   -- books/... 呈现路径（可空=未上架）
  card_path   TEXT,                   -- catalog/bk_*.md
  status      TEXT NOT NULL DEFAULT 'incoming',
              -- incoming|cleaning|cataloging|assessing|reviewing|shelved|archived|deleted
  suggest_floor TEXT,  suggest_room TEXT,  suggest_shelf TEXT,  -- 管理员建议（JSON）
  confirm_by  TEXT,                   -- 主人确认人（NULL=未确认）
  private     INTEGER DEFAULT 0,      -- 1=私密，永不发 API
  tags        TEXT,                   -- JSON array，虚拟书架聚合用
  meta        TEXT,                   -- JSON（来源信息/解析信息/页码等）
  created_at  TEXT,
  updated_at  TEXT
);
```

**chunks**（chunk 文本 + 元数据；向量在 LanceDB）

```sql
CREATE TABLE chunks (
  chunk_id  TEXT PRIMARY KEY,         -- ck_*
  book_id   TEXT REFERENCES books(book_id),
  section   TEXT,                     -- 章节/页码/时间戳
  seq       INTEGER,                  -- 书内顺序
  content   TEXT NOT NULL,
  token_cnt INTEGER,
  vector_id TEXT                      -- LanceDB 主键
);
CREATE VIRTUAL TABLE chunks_fts USING fts5(
  content, content='chunks', content_rowid='rowid'
);   -- 外部内容表；rowid 与 chunks.rowid 对应
```

**catalog_cards**（图书卡片，生成后也进向量索引）

```sql
CREATE TABLE catalog_cards (
  book_id      TEXT PRIMARY KEY REFERENCES books(book_id),
  summary      TEXT,                  -- 200 字摘要
  chapters     TEXT,                  -- JSON [{title, summary, ref}]
  concepts     TEXT,                  -- JSON [{term, definition, ref}]
  distill_value INTEGER,              -- 0-100 蒸馏价值分
  distill_reason TEXT,                -- 为什么值得/不值得蒸馏
  skills       TEXT,                  -- JSON [{skill_id, name, status}]
  generated_at TEXT,
  model        TEXT                   -- 生成用的模型
);
```

**skills**（蒸馏产物注册表）

```sql
CREATE TABLE skills (
  skill_id     TEXT PRIMARY KEY,      -- sk_*
  book_id      TEXT REFERENCES books(book_id),
  name         TEXT NOT NULL,
  slug         TEXT,
  path         TEXT,                  -- skills/<book-slug>/<skill-slug>/SKILL.md
  description  TEXT,                  -- trigger 条件（进向量索引做路由）
  status       TEXT DEFAULT 'draft',
               -- draft|reviewing|approved|rejected|installed
  reject_count INTEGER DEFAULT 0,     -- 连续拒绝次数（≥5 自动阻塞）
  last_reject_reason TEXT,
  test_prompts TEXT,                  -- JSON（darwin 兼容）
  created_at TEXT, updated_at TEXT
);
```

**conversations / messages**（管理员对话）

```sql
CREATE TABLE conversations (
  cv_id        TEXT PRIMARY KEY,
  title        TEXT,
  archived_book_id TEXT,              -- 归档为书后关联
  created_at TEXT, updated_at TEXT
);
CREATE TABLE messages (
  msg_id        TEXT PRIMARY KEY,
  cv_id         TEXT REFERENCES conversations(cv_id),
  role          TEXT,                 -- user|assistant|system
  content       TEXT,
  refs          TEXT,                 -- JSON [[wikilink]] 引用列表
  private       INTEGER DEFAULT 0,
  created_at    TEXT
);
```

**actions**（操作账本 = 主人权利机制核心）

```sql
CREATE TABLE actions (
  act_id      TEXT PRIMARY KEY,       -- act_*
  agent       TEXT,                   -- admin|purchaser|archivist|distiller|system|owner
  action_type TEXT,                   -- classify|distill|shelve|archive|delete|purchase|ingest|...
  target_type TEXT, target_id TEXT,   -- book/skill/conversation/...
  params      TEXT,                   -- JSON（动作参数）
  undo_params TEXT,                   -- JSON（撤销所需逆操作）
  status      TEXT DEFAULT 'done',    -- done|undone|failed
  reason      TEXT,                   -- 动作理由（进日报）
  created_at  TEXT
);
```

**purchase / reports / profile**

```sql
CREATE TABLE recommendations (
  rec_id      TEXT PRIMARY KEY,
  date        TEXT,                   -- 2026-08-13
  title       TEXT, url TEXT, source TEXT,
  score       REAL,
  reasons     TEXT,                   -- JSON 评分理由（主题/可信度/质量/可蒸馏性）
  status      TEXT DEFAULT 'pending', -- pending|collected|ignored|not_interested
  feedback_note TEXT,
  created_at  TEXT
);
CREATE TABLE daily_reports (
  report_id TEXT PRIMARY KEY,
  date      TEXT,
  rtype     TEXT,                     -- purchase|ingest|distill|system
  content   TEXT,                     -- JSON（卡片渲染数据）
  created_at TEXT
);
CREATE TABLE profile (
  key   TEXT PRIMARY KEY,             -- theme_dist|direction_pool|prefs|history
  value TEXT                          -- JSON
);
```

**index_state**（copy-on-write 索引修订状态，另存 `.meta/index_state.json`，DB 里留摘要表）

```sql
CREATE TABLE index_state (
  revision   INTEGER PRIMARY KEY,     -- 当前修订号
  active     INTEGER DEFAULT 1,       -- 是否已激活
  status     TEXT,                    -- building|active|stale|rebuild-required
  changed_book_ids TEXT,              -- JSON（本次修订变更的书）
  built_at   TEXT
);
```

### 5.4 profile.md（借书证）frontmatter

```yaml
---
type: profile
name: 主人借书证
updated: 2026-08-13
themes:
  人工智能: 31
  哲学: 20
direction_pool:            # 采购方向池（来自提问历史+热门）
  - { topic: 自行车维修, weight: 3, source: question, first_seen: 2026-08-10 }
  - { topic: LLM Agent, weight: 5, source: hot }
prefs:
  default_mode: conservative   # conservative|auto
  auto_score_threshold: 85
  max_daily_purchase: 5
  no_video_unless_hot: true
---
正文：自由格式兴趣说明（主人可手写）
```

### 5.5 图书卡片格式（catalog/bk_<id>.md）

```markdown
---
type: book-card
book_id: bk_xxx
title: 《…》
author: …
media_type: pdf
floor: 1F 电子书
room: 人工智能
shelf: LLM与Agent
status: reviewing
private: false
distill_value: 82
ingested: 2026-08-13
---

# 《…》图书卡片

## 摘要
（200 字左右，模型生成）

## 章节结构
1. …（章节标题 / 页码）—— 一句摘要
2. …

## 关键概念
- **术语**：定义（出处 [[wikilink]]）

## 蒸馏价值评估
- 分数：82/100
- 理由：方法论密度高、有可执行框架，建议蒸馏
- （评估模型 + 日期）

## 关联技能
- [ ] sk_xxx 《…》→ SKILL.md（待审阅）
```

---

## 6. 核心流程设计（状态机）

### 6.0 总览：书的一生

```
                    ┌─────────────────────────────────────────────┐
                    │                补书室（唯一入口）              │
 ① 主人拖拽/扔书 ───┤                                             │
 ② 采购收藏 ────────┤→ incoming → cleaning → cataloging →        │
 ③ 对话归档 ────────┤     （清洗）     （卡片+分类建议）             │
                    └─────────────────────────────────────────────┘
                                   │
                    assessing（蒸馏价值评估）
                                   │
              ┌────────────────────┴────────────────────┐
              │            reviewing（主人审阅）           │
              │  建议区：管理员已给分类/蒸馏建议            │
              │  待定区：AI 无法判断，主人亲自指定          │
              │  操作：确认上架 / 改位置 / 拒绝蒸馏 / 放着  │
              └────────────────────┬────────────────────┘
                                   │ 主人确认
              ┌────────────────────▼────────────────────┐
              │  shelved（上架楼层）→ 可随时被主人移走      │
              │    └─ 蒸馏价值高 → distilling → skill      │
              │         reviewing(人审) → approved/installed│
              │         rejected(附原因)→ 回炉/阻塞        │
              └────────────────────┬────────────────────┘
                                   │ 主人操作
              ┌────────────────────▼────────────────────┐
              │  archived（建议归档）→ deleted（仅主人可删） │
              │         → archive/deleted 可恢复 30 天     │
              └─────────────────────────────────────────┘
```

### 6.1 入馆流程（ingest）

**触发**：Web UI 拖拽/选择文件、`/kb-ingest` 命令、采购收藏、对话归档。

**步骤**：
1. 接收文件/URL/文本 → 写入暂存目录 `data/tmp/`。
2. 解析（按格式路由，§3.4）→ 提取纯文本 + 元信息（标题/作者/来源）。
3. **不可变副本**：内容 `sha256` 作为 `content_hash`，复制原始文件到 `archive/raw/<hash>`（保留原名信息在 `.meta` 记录）。
4. 清洗：去广告/导航（网页）、规范化换行、去重复空白、识别章节标题（markdown 标题层级）。
5. 写 `books/` 呈现层前先登记 DB：`status=cleaning`。
6. 清洗完成 → `status=cataloging`，进入补书室（DB 状态 `reviewing` 前的中间态由 `updated_at` + 流水线字段追踪）。

**幂等**：同 `content_hash` 已存在 → 跳过复制，标记"重复入馆"，提示主人已存在的书。

**失败**：解析失败 → 补书室"待定区" + 错误信息 + 建议格式。

### 6.1.1 批量入馆（P5，主人 2026-08-14 确认）

**规则（硬约束）**：
- 一次最多 **10 本**；每次上传前必须先选**格式**（PDF/EPUB/MD/TXT/HTML…），**一次只能上传同一格式**的书籍（混格式直接拒绝并提示）。
- 前端：入馆对话框 = 格式下拉（必选）+ 多文件选择/拖拽（>10 本提示截断）；同格式校验在提交前完成。
- 后端：`POST /api/ingest/batch`（form: `files` 多文件 + `format` + `private`）。

**流程**：
1. 校验：文件数 ≤10；扩展名/格式与所选 `format` 一致；否则整批 400 拒绝。
2. 批量去重：一次性计算所有文件 sha256，重复的标记 `duplicate`（不阻断其余文件）。
3. 批量清洗登记：逐本走 §6.1 清洗与 `archive/raw` 不可变副本（幂等 hash 去重）。
4. **批量索引提速**：所有新书合并后一次性批处理 embedding（API 批 64 条/次），LanceDB/FTS 批量写入——把 N 次 API 往返降为 N/64。
5. **异步化**：清洗登记完成后立即返回逐本状态（`{book_id, title, status, duplicate, error}`），索引放后台任务 + WS `progress` 广播（复用 P3 通知机制）。
6. **429/额度兜底**：embedding 失败的书先落词法索引（FTS5），向量后台补；不阻断入馆。

### 6.2 编目流程（catalog）

**触发**：入馆清洗完成后自动（或主人手动）。

**步骤**：
1. LLM 生成：摘要（200 字）、章节结构、关键概念（§5.5 卡片格式），输出 JSON。
2. 生成分类建议：`suggest_floor/room/shelf`（按楼层=来源媒介 直接确定楼层；房间=语义主题由 LLM 建议，匹配已有房间或建议新建）。
3. 写入 `catalog/bk_<id>.md`（**草稿**，标 `status: reviewing`）。
4. 卡片内容进向量索引（chunk 化），但书未上架前检索时标注"补书室未归位"。
5. 写入 action ledger（`classify`，可撤销=改回原分类建议为空）。

**半托管差异**：
- 半托管（默认）：建议生成后停在 `reviewing`，等主人确认。
- 全托管：建议自动生效上架（仍记录 + 可撤销 + 日报汇报）。
- 全手动：只生成卡片，不生成分类建议（或建议标"仅供参考"），主人亲自分类。

### 6.3 蒸馏价值评估（assess）

**触发**：编目完成后，对每本书评估一次（可手动重评）。

**输入**：卡片 + 正文抽样（前 20% + 章节标题）。
**输出**：
```json
{ "distill_value": 82,
  "category": "methodology|reference|narrative|data",
  "reason": "方法论密度高、有可执行框架…",
  "suggested": true }
```
**决策**：`category=methodology` 且 `distill_value >= 60` → 建议蒸馏；否则只编目。**最终由主人在补书室确认**（"值得蒸馏"勾选）。

### 6.4 蒸馏流程（distill，接 cangjie-skill）

**触发**：主人确认"开始蒸馏"（或全托管下自动）。

**步骤**：
1. 调用工作区 `cangjie-skill`（技能已存在：`skills/cangjie-skill/SKILL.md`），执行其 5 阶段流水线：
   - 阶段 0：整书理解 → `BOOK_OVERVIEW.md`（需主人确认骨架，可跳过）
   - 阶段 1：5 个提取器并行 → `candidates/`
   - 阶段 1.5：三重验证 → `verified.md` + `rejected/`
   - 阶段 2：RIA++ 构造 SKILL.md
   - 阶段 3：Zettelkasten 链接 → `INDEX.md`/`GLOSSARY.md`
   - 阶段 4：darwin 兼容压力测试 → `test-prompts.json`
   - 阶段 5：交付 → 安装到 `vault/skills/<book-slug>/`
2. 每阶段产出 → 更新 `PIPELINE_STATE.md`（断点续跑）。
3. 每个 SKILL.md 生成 → `skills.status=reviewing`，进"待审阅技能"队列。
4. 过程审计：candidates/rejected/测试结果复制到 `archive/distill-logs/<book-slug>/`。

**人审（P5 硬约束）**：
- 主人在 UI"技能审阅"卡片看到：SKILL.md 全文 + test-prompts + 测试结果。
- 动作：批准（→ `installed`，注册进技能库索引）/ 拒绝（附原因 → `rejected`，原因带进下次重蒸 prompt；`reject_count++`）/ 修改后批准。
- `reject_count >= 5` → 自动阻塞该书的蒸馏（提示主人需人工干预，防死循环，参考 obsidian-llm-wiki 反馈环）。

### 6.5 检索问答流程（ask，含技能路由）

```
提问（Web UI / /kb-ask / MCP）
   │
   ▼
① 定位：混合检索（FTS5 词法 + LanceDB 向量，§7）→ top-k chunks
         + 卡片检索（摘要/章节）→ 命中 1-3 本书
   │
   ▼
② 概览：组装"书级上下文"（卡片摘要 + 命中章节 + 原文 chunk）
   │
   ▼
③ 技能路由：问题 embedding → 技能库向量检索（skills.description）
         → 命中 sk_* → 加载 SKILL.md 作为 system prompt 补充
         → 多技能冲突 → LLM 裁决（router）
   │
   ▼
④ 作答：LLM 生成回答，要求带 [[wikilink]] 引用（书卡片/章节锚点）
   │
   ▼
⑤ 可选：执行技能产出文档（如按框架生成清单/报告）
   │
   ▼
⑥ 归档：问题主题 → 沉淀进 profile.direction_pool（采购方向池）
   │
   ▼
⑦ 用户可一键"存入图书馆" → 对话归档为书 → 补书室
```

**引用溯源**：回答中的 `[[wikilink]]` 指向 `catalog/bk_<id>` 或 `books/.../<书名>.md#<章节>`；前端可点击跳转阅览室原文。`refs` 字段存 JSON 供溯源审计。

### 6.6 采购流程（purchase，每日 08:00）

**步骤**：
1. **配额计算**（AI 可解释，理由进日报）：
   - 统计藏书主题分布（profile.themes）。
   - 按比例把当日 5 格分给主题。
   - 藏书刚变化：新增书属已有主题 → 不调整；属全新主题 → 记"新兴趣信号"，连续 ≥3 次才纳入配额。
   - 当日提问方向（direction_pool 高权重）插队优先。
2. **采集**：方向池 + 热门公开源（arXiv / HN / 知乎热榜 / RSS 订阅，无需 key）。默认不抓视频；非常热门才抓标题+简介评估。
3. **荐书评估**（`score 0-100`）：
   - 主题相关度（与 profile.themes/direction_pool 匹配）
   - 来源可信度（域名/频道白名单）
   - 内容质量（标题/简介/结构信号）
   - 可蒸馏性（方法论类加分）
4. 生成 `pending/2026-08-13.md` 推荐清单 + 每条评分理由 + `recommendations` 表。
5. **保守模式（默认）**：只出清单，主人点"收藏"才入馆（走 §6.1）；"忽略"/"不感兴趣"写回 profile（负反馈学习）。
   **自动模式**：score ≥ 85 直接入馆（仍记录+可撤销），低分进清单。
6. 生成当日采购日报（抓取明细 + 筛选理由 + 配额执行率）。

### 6.7 对话归档为书

1. 主人对某段对话点"存入图书馆"。
2. 对话导出为 Markdown（消息 + 引用），登记 `conversations.archived_book_id`。
3. 走 §6.1 入馆（media_type=chat，来源=对话）→ 补书室 → 三楼聊天记录（房间按人/按主题建议）。

### 6.8 归档流程（archivist）

- **建议归档**：AI 检测到书长期未读/内容重复 → 建议归档（**不自动执行**，除非全托管）。
- **主人删除**：仅主人可删；书移入 `archive/deleted/`，`books.status=deleted`，30 天可恢复；30 天后自动清理（或提示主人确认彻底删除）。
- **备份**：`archive/backups/` 周期导出 vault + SQLite + LanceDB 为 zip。
- 所有归档动作写 action ledger（可撤销 = 恢复）。

### 6.9 操作账本与撤销（ledger，P1 硬约束）

**单写入者事务**：所有写 vault/DB 的操作集中在 `ledger/` 执行：
1. 动作开始 → `actions` 插入 `status=doing`。
2. 执行写操作（文件 + DB 在同一事务批次，失败回滚）。
3. 成功 → `status=done`（记录 `undo_params`）；失败 → `failed` + 回滚。
4. **撤销**：读取 `undo_params` 执行逆操作 → `status=undone`。

**并发规则**：同一时刻只有一个写入事务（asyncio lock）；MCP/Web/UI 全部走同一入口，杜绝竞态（调研"应避免"第 6 条）。

---

## 7. 索引与检索系统

### 7.1 索引管线（indexer，P0 核心）

**流程**：扫描 → chunk → embedding → LanceDB + FTS5。

1. **扫描**：遍历 vault `books/` + `catalog/` + `skills/`，计算每文件 hash（`.meta/vault_watch.db` 缓存），找出 changed/new/deleted。
2. **chunk 策略**：
   - 按 markdown 标题分节；节超长再按段落/字数切分。
   - 目标块 500-800 token（中文约 800-1200 字），重叠 10%（跨节不重叠）。
   - 元数据：book_id、section、seq、页码/时间戳。
3. **embedding**：Qwen3-Embedding-0.6B（批 64，本地缓存已嵌 hash 避免重复调用）。
4. **写索引**（copy-on-write，参考 ObsidianRAG）：
   - 新修订号 `revision+1`；只重嵌变更 chunk（旧 chunk 标记 superseded，不删）。
   - LanceDB：同一 revision 用版本/命名空间区分（`revision_<n>` 表或 dataset version）。
   - FTS5：更新外部内容表（事务）。
   - **校验**：嵌入完成后抽查 N 条可检索 → 通过才 `index_state.status=active`（旧修订作为 reader lease 保留到查询结束）。
5. **状态机**：`missing → building → active`；检测到 vault 变更 → `stale`；索引损坏/重建 → `rebuild-required`（全量重建）。

### 7.2 混合检索（searcher）

```
query
 ├─ 词法路：FTS5 MATCH（分词：中文按字符 ngram？→ MVP 用简单 jieba 或 FTS5 unicode61 + 空格，见 7.3）
 ├─ 向量路：query embedding → LanceDB top-k
 └─ 融合：RRF（Reciprocal Rank Fusion）合并 → 按书聚合 → 书级得分 + chunk 级明细
输出：{books: [{book_id, score, title, card_summary, hit_chunks:[...]}]}
```

- 检索范围可过滤：全库 / 指定楼层 / 指定书 / 补书室（未归位）。
- 卡片（catalog/）与正文（chunks）**分开索引**，概览阶段优先取卡片。
- P4：加 rerank（Qwen3-Reranker-0.6B）微调排序。

### 7.3 中文分词（MVP 决策）

- FTS5 原生 `unicode61` tokenizer 对中文按整句 token 处理效果差。
- **MVP**：chunk 写入时用 `jieba` 预分词（空格连接）再进 FTS5；查询同样预分词。简单、零成本、够用。
- **备选**（若效果不佳）：ngram tokenizer（`trigram`）直接建索引，牺牲索引大小换召回。
- 验收标准：100 篇中文笔记中，含特定术语的查询 top-10 召回率 ≥ 90%（§12 验收）。

---

## 8. 多角色 Agent 设计

### 8.1 角色定义（role 层，共享同一后端）

| 角色 | 职责 | 触发 | 写入权限 |
|---|---|---|---|
| 管理员 admin | 编目、分类建议、索引、问答、技能路由 | 入馆事件/提问 | 建议 + 卡片草稿 |
| 采购员 purchaser | 采集、评估、推荐、日报、配额 | 每日 08:00/手动 | 推荐清单 + pending/ |
| 档案员 archivist | 归档、备份、删除恢复、审计 | 周期/主人指令 | archive/ 操作（不删书） |
| 蒸馏师 distiller | 调 cangjie-skill 蒸馏 | 主人确认 | skills/ 草稿 |
| 系统 system | 索引、清洗、通知 | 事件 | 索引/暂存 |

- 所有角色写操作 → `actions.agent` 记录角色。
- 角色只建议不执行（半托管），执行权在主人。

### 8.2 QwenPaw agent-skills 入口（主入口）

在 `kb_skills/` 提供标准 SKILL.md（供 QwenPaw 加载）：

| 命令 | SKILL.md | 说明 |
|---|---|---|
| `/kb-ingest` | 扔书入馆：`/kb-ingest <path|url|text>` | 走 §6.1，返回 book_id + 补书室状态 |
| `/kb-ask` | 检索问答：`/kb-ask <问题>` | 走 §6.5，返回回答 + 引用 |
| `/kb-distill` | 蒸馏：`/kb-distill <book_id>` | 走 §6.4，返回 pipeline 状态 |
| `/kb-catalog` | 重编目：`/kb-catalog <book_id>` | 重生成卡片 |
| `/kb-purchase` | 采购：`/kb-purchase [today]` | 今日推荐 + 日报 |
| `/kb-status` | 健康度：`/kb-status` | 仪表盘数据 |
| `/kb-review` | 审阅队列：`/kb-review [skill\|book]` | 待审阅列表 |

### 8.3 MCP server 工具清单（供任意 agent）

| 工具 | 说明 | 等价命令 |
|---|---|---|
| `kb_list_books(status?, floor?)` | 列书 | — |
| `kb_get_card(book_id)` | 取卡片 | — |
| `kb_search(query, top_k)` | 混合检索 | — |
| `kb_ask(question, stream=false)` | 问答（带引用） | /kb-ask |
| `kb_ingest(path_or_url)` | 入馆 | /kb-ingest |
| `kb_classify_suggest(book_id)` | 生成分类建议 | — |
| `kb_confirm_shelving(book_id, floor, room, shelf)` | 主人确认上架 | — |
| `kb_distill(book_id)` | 蒸馏 | /kb-distill |
| `kb_list_skills(status?)` | 技能库 | — |
| `kb_review_skill(skill_id, action, reason)` | 审阅技能 | — |
| `kb_archive(book_id, reason)` | 建议归档 | — |
| `kb_restore(book_id)` | 恢复（30 天内） | — |
| `kb_dashboard()` | 健康度 | /kb-status |

> 注意：MCP 中 `kb_confirm_shelving` / `kb_restore` 等**主人操作类**工具应做调用方校验（仅在 UI 会话或确认过的主入口允许），防止其他 agent 越权——主人权利硬约束的工程化体现。

### 8.4 与 Obsidian 薄插件（二期）的接口

- Local REST API 插件或自定义插件：读取 vault（Obsidian 原生渲染）+ 调用后端 REST/WS。
- 薄插件职责：侧边栏提问框、点击 `[[wikilink]]` 跳原文、补书室通知徽章。
- 二期范围，不阻塞 P0-P3。

---

---

## 9. 交互协议（REST + WebSocket）

### 9.1 Web UI 布局（空间隐喻，浏览器 127.0.0.1:8800）

```
┌──────────────────────────────────────────────────────────────────────┐
│ 顶部导航： 🏛大厅 │ 📖楼层 │ 🌌占星室 │ 📖阅览室 │ 💬管理员 │ 🛒采购员 │ 🗄档案馆 │ ⚙设置 │
├──────────────────────────────────────────────────────────────────────┤
│ 当前页面内容区                                                        │
│                                                                      │
│ 全局元素：                                                            │
│  · 右上角：补书室入口（徽章显示待分类数）＋ 健康度迷你仪表              │
│  · 右侧悬浮：待审阅技能队列（P2 后）                                   │
│  · 底部：WebSocket 状态指示 + 进行中任务进度条                         │
└──────────────────────────────────────────────────────────────────────┘
```

**各空间布局**：

| 空间 | 布局要点 |
|---|---|
| 🏛 大厅 | 顶部 3 卡片行（健康度仪表：待分类书数/待审阅技能数/索引状态/采购配额执行率/本周入馆趋势）；建筑鸟瞰（楼层缩略图，点击跳楼层）；今日日报卡片；快捷操作（扔书入馆/打开补书室）；"继续阅读"模块（P5：最近阅读书列表） |
| 📖 楼层 | 左侧楼层列表（含补书室入口）；中间房间/书架树；右侧书列表（卡片：封面占位+书名+作者+标签+状态徽章）；支持按标签/主题切"虚拟书架"视图 |
| 🌌 占星室 | ECharts graph：书/技能/主题/档案/对话节点；补书室的书=未归位星（虚线圈）；**书↔书语义边（P5）**：卡片级向量两两相似 + 规则边（同房间/同标签/引用），top-k 截断防毛线球，按关系类型着色/线宽映射相似度；筛选（类型/时间着色）；"在 Obsidian Graph View 中打开"按钮（二期） |
| 💬 管理员 | 左：对话历史（今天/昨天/按主题分组）；右：聊天（流式输出 + [[引用]] 可点击）；输入框支持 @书 / #技能；下方 Tab：索引（全站搜索：按时间/书名/内容/语义） |
| 🛒 采购员 | 今日配额卡片（配额理由展开）；推荐清单（卡片：来源/评分/理由 + ✅收藏 ❌忽略 🙅不感兴趣）；历史日报列表（可展开）；对话输入（"帮我找点 Rust 的内容"） |
| 🗄 档案馆 | Tab：原始副本 / 已删除（30 天倒计时 + 恢复按钮）/ 历史日报 / 蒸馏过程记录 / 备份（导出/导入） |
| ⚙ 设置 | API 配置（ModelScope key、Ollama 地址）；采集规则（每日条数、视频开关、来源白名单）；托管程度滑杆（全托管/半托管/全手动）；楼层房间编辑（增删改名排序）；画像编辑（借书证） |
| 📖 阅览室 | **顶层入口（P5）**：最近阅读/最近入馆书列表，点书进入三栏阅读页（章节导航/正文/相关面板）；书卡片"阅读"按钮直达；**离线优先**读 vault 原文，无额度/断网可读（见 §10 降级）；原文件查看（PDF.js，P5） |

### 9.2 REST API 端点（v1，前缀 `/api`）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/ingest` | 入馆（multipart 文件 / JSON url / 文本） |
| POST | `/api/ingest/batch` | 批量入馆（P5：同格式 ≤10 本/次，form: files+format+private，返回逐本状态；索引异步） |
| GET | `/api/books` | 列书（query: status/floor/room/shelf/tag/q） |
| GET | `/api/books/{book_id}` | 书详情 + 卡片 |
| POST | `/api/books/{book_id}/classify` | 生成分类建议（admin） |
| POST | `/api/books/{book_id}/confirm` | 主人确认上架（body: floor/room/shelf） |
| POST | `/api/books/{book_id}/reject-distill` | 拒绝蒸馏 |
| POST | `/api/books/{book_id}/archive` | 建议归档（admin）/ 删除（owner） |
| POST | `/api/books/{book_id}/restore` | 恢复（30 天内） |
| GET | `/api/books/{book_id}/content` | 原文（分节） |
| GET | `/api/floors` / POST `/api/floors` | 楼层 CRUD（设置页） |
| GET | `/api/rooms?floor_id=` / POST `/api/rooms` | 房间 CRUD |
| GET | `/api/shelves?room_id=` / POST `/api/shelves` | 书架 CRUD |
| POST | `/api/search` | 混合检索（body: query/filters/top_k） |
| POST | `/api/ask` | 问答（SSE 流式，见 9.3） |
| GET | `/api/skills` / POST `/api/skills/{id}/review` | 技能列表 / 审阅 |
| POST | `/api/distill/{book_id}` | 启动蒸馏 |
| GET | `/api/purchase/today` | 今日配额 + 推荐 |
| POST | `/api/purchase/{rec_id}/collect` | 收藏入馆 |
| POST | `/api/purchase/{rec_id}/feedback` | 忽略/不感兴趣反馈 |
| GET | `/api/daily-reports?date=` | 日报 |
| GET | `/api/conversations` / GET `/api/conversations/{id}` | 对话历史 |
| POST | `/api/conversations/{id}/archive` | 对话归档为书 |
| GET | `/api/dashboard` | 健康度 |
| GET | `/api/profile` / PUT `/api/profile` | 画像 |
| GET | `/api/actions` | 操作账本（撤销入口） |
| POST | `/api/actions/{id}/undo` | 撤销 |
| GET | `/api/settings` / PUT `/api/settings` | 设置 |
| WS | `/ws/chat` | 聊天 + 进度 + 通知（9.3） |

### 9.3 WebSocket 消息协议（`/ws/chat`）

客户端→服务端：
```json
{ "type": "chat", "cv_id": "cv_xxx", "content": "提问", "refs_ok": true }
{ "type": "ask_stream", "content": "…" }          // 同 chat，走 §6.5
{ "type": "subscribe", "channel": "book:<id>" }    // 订阅某书状态变更
{ "type": "ping" }
```

服务端→客户端：
```json
{ "type": "chat_delta", "cv_id": "cv_xxx", "delta": "逐字/逐句" }
{ "type": "chat_done", "cv_id": "cv_xxx", "refs": ["[[catalog/bk_xxx]]"] }
{ "type": "progress", "task": "ingest|index|distill|purchase", "book_id": "bk_xxx",
  "stage": "cleaning", "percent": 45, "message": "解析 PDF…" }
{ "type": "notification", "level": "info|warn|action", "title": "有新书待分类",
  "payload": { "book_id": "bk_xxx", "href": "/books/1F" } }
{ "type": "report", "rtype": "purchase", "date": "2026-08-13", "content": {…} }
{ "type": "pong" }
```

### 9.4 权限与越权防护

- 后端仅监听 `127.0.0.1`（不对外网）。
- 主人操作类端点（confirm/restore/delete/settings）与 MCP 主人工具做同源校验（仅允许来自 Web UI 会话或带主人确认令牌的调用）。
- 所有写操作记 `actions`（§6.9）。

---

## 10. 隐私与安全边界

| 边界 | 规则 | 实现 |
|---|---|---|
| 存储 | 全部本机：SQLite + LanceDB + vault 文件 | 数据目录在开发根目录，无云端同步 |
| 私密内容 | 标记 `private=1` → **永不发送任何 API**（ModelScope/Ollama 都不发），仅本地索引 + 本地词法检索；向量索引也跳过（或仅本地） | ingest 时主人可勾选；聊天中检测到敏感信息提示主人标记；设置页默认策略 |
| 免费 API 明示 | ModelScope 调用时文本经过阿里云服务器 → UI 设置页 + 入馆提示条明示 | 全局横幅："免费模型调用会经手 ModelScope，请勿投喂私密内容" |
| 本地隔离通道 | 私密内容处理走本地 Ollama（如 qwen2.5:7b） | `llm/providers.py` 双通道；Ollama 不可用时降级：跳过 AI 处理，仅索引 |
| API key | ModelScope key 存本机配置文件（不记入记忆/日志）；提供 UI 输入 | 参照 MEMORY.md 经验：`~/.qwenpaw.secret/…` 或 `data/secrets.json`（权限 600） |
| 日志 | 日志不记录消息正文、不记录 key | logging 配置脱敏过滤器 |
| 对话 | 对话记录默认本机；可标记私密不参与检索 | `messages.private` |

**降级策略**：无 API key / 无网 → 系统仍可做：本地索引、FTS5 检索、主人手动分类、阅览室阅读（离线可用）；AI 功能（卡片/建议/问答）显示"模型不可用"。

---

## 11. MVP 范围与验收标准（P0–P5）

### P0 地基：索引管线

- [ ] 项目骨架：FastAPI + SQLite + LanceDB + 配置加载 + 启动脚本（`start.ps1`/`start.sh`）
- [ ] 入馆清洗（MD/TXT/PDF/HTML）
- [ ] chunk + jieba 分词
- [ ] embedding（Qwen3-Embedding-0.6B，带本地缓存）
- [ ] copy-on-write 增量索引 + FTS5 写入
- [ ] 混合检索（RRF 融合）
- **验收**：100 篇中文笔记 ≤ 60 秒完成首次索引；改 1 篇笔记增量重建 ≤ 3 秒；查询含特定术语 top-10 召回率 ≥ 90%；可一键全量重建；索引损坏可检测（rebuild-required）。

### P1 编目闭环

- [ ] 图书卡片生成（摘要/章节/概念）+ catalog/ 落盘
- [ ] 分类建议（楼层固定 + 房间/书架 LLM 建议）
- [ ] 补书室（建议区/待定区）+ 主人确认上架 + 撤销
- [ ] 基础问答带 `[[wikilink]]` 引用 + 阅览室（原文阅读）
- **验收**：1-3 本书入馆→卡片→确认上架全流程 ≤ 5 分钟人工操作；"这本书讲了什么"能答并定位原文；问答引用可点击跳原文。

### P2 蒸馏闭环

- [ ] 接 cangjie-skill 流水线（断点续跑 + 审计日志）
- [ ] 技能审阅 UI（批准/拒绝附原因/阻塞逻辑）
- [ ] 技能路由（description 向量命中 → 注入 SKILL.md）
- **验收**：1 本书蒸馏出 ≥2 个通过测试的 SKILL.md；提问命中相关技能并按框架作答；拒绝 5 次自动阻塞生效。

### P3 Web UI MVP（四空间）

- [ ] 🏛大厅（健康度 + 日报 + 快捷操作）
- [ ] 📖楼层（树 + 书列表 + 虚拟书架）
- [ ] 💬管理员（对话历史 + 索引搜索 + 流式聊天）
- [ ] 🛒采购员（配额卡片 + 推荐清单 + 反馈 + 历史日报）
- [ ] ⚙设置（托管滑杆 / API / 采集规则 / 楼层编辑）
- **验收**：仅用鼠标完成"扔书→确认上架→提问→收藏采购"全流程，全程无需命令行；WS 通知在补书室有新书时弹出。

### P4 打磨

- [x] 🌌占星室（星空图 + 未归位星）——已实现（2026-08-14）
- [x] 🗄档案馆（原始副本/已删除恢复/日报/蒸馏日志/备份导出）——已实现（2026-08-14）
- [x] 📖阅览室完善（高亮 + 相关技能联动）——已实现（2026-08-14）
- [ ] rerank（Qwen3-Reranker-0.6B）
- [ ] 性能优化（大 vault 索引；批量入馆移至 P5）
- [ ] Obsidian 薄插件（二期可选，移至 P5 末位）

### P5 桌面化 + 批量入馆 + 离线阅读 + 星图增强（2026-08-14 主人确认追加）

- [ ] 桌面壳：pywebview 封装 `web/dist` → Windows 桌面程序（双击启动、自动拉起/关闭后端、托盘常驻；WebView2 缺失降级浏览器）
- [ ] 批量入馆：`/api/ingest/batch`（同格式 ≤10 本/次 + 格式选择器；批量去重 → 批量 embedding 批处理 → 异步索引 + WS 进度；429 先词法后补向量）
- [ ] 无额度读原文：content 端点优先读 vault 文件（未上架读 `archive/raw`）；原文件查看（PDF.js / 浏览器内置 viewer）；UI 明示模型降级
- [x] 阅览室顶层入口：顶部导航 + 书卡片"阅读"按钮 + 大厅"继续阅读"（**2026-08-14 完成，git `625bb78`，143 测试**）
- [ ] 星图书↔书语义边：卡片级向量两两相似度 + 规则边（同房间/同标签/引用）+ top-k 截断 + 本地缓存/词法兜底
- **验收**：桌面壳双击即用；一次拖 10 本 PDF 全部入馆且 WS 进度可见；断网/无额度仍可打开任意书原文；星图可见书与书的相关连线

---

## 12. 开发顺序与里程碑

```
M0 脚手架（0.5 周）：项目结构 + 配置 + 启动脚本 + CI（pytest 骨架）
  ↓
M1 P0 索引管线（1.5 周）：入馆清洗 → 索引 → 检索（验收见 P0）
  ↓
M2 P1 编目闭环（1.5 周）：卡片 + 补书室 + 问答（验收见 P1）
  ↓
M3 P2 蒸馏闭环（1.5 周）：cangjie 接入 + 审阅 + 路由（验收见 P2）
  ↓
M4 P3 Web UI（2 周）：四空间 + 采购（验收见 P3）
  ↓
M5 P4 打磨（2 周）：占星室/档案馆/阅览室/优化（2026-08-14 已完成，实际由 P4-1~6 落地）
    ↓
M6 P5 桌面化（2026-08-14 主人确认追加）：桌面壳 → 批量入馆 → 离线阅读 → 星图增强
```

**里程碑检查点**：每个 M 完成 → 跑对应验收脚本 + 主人体验确认 → 进入下一步。

---

## 13. 风险与备选方案

| 风险 | 影响 | 缓解/备选 |
|---|---|---|
| FTS5 中文分词效果差 | P0 验收不达标 | 方案 A：jieba 预分词（首选）；方案 B：trigram tokenizer；方案 C：加重向量路权重 |
| Qwen3-Embedding-0.6B 免费接口限流/不可用 | 索引卡住 | 本地缓存 + 失败重试；备选 embedding（bge-m3 via Ollama） |
| GLM-5.2/DeepSeek 免费接口质量不足（蒸馏） | 蒸馏产物质量低 | 蒸馏走 GLM-5.2 + 人工审阅兜底；降级为只编目不蒸馏 |
| 采集源失效（无 key、热门源变动） | 采购空转 | 采集器插件化（source adapters），源失败跳过并记日报；方向池兜底 |
| vault 与 DB 不一致（主人直接在 Obsidian 编辑） | 索引漂移 | 扫描阶段以文件为准同步 DB；冲突时以文件为真源 + 记录警告 |
| 大书蒸馏 API 成本/时长 | 用户等待长 | 分阶段续跑（PIPELINE_STATE.md）+ 进度通知；MVP 先蒸 1 本书验证 |
| 私密误发 API | 隐私泄漏 | 默认策略保守（标记才发）；敏感词检测提示；设置页可强制"全部本地" |
| Windows 路径/编码问题 | 偶发解析失败 | 统一 UTF-8；路径规范化（vault 中文目录名经测试） |
| 多 agent 并发写 | 竞态 | 单写入者 + 事务 + action ledger（§6.9） |
| 项目范围膨胀 | 交付拖期 | 严格 P0-P4 边界；P4 内容可砍，P0-P2 不动；P5 按里程碑逐项验收 |
| pywebview 桌面壳环境依赖 | Win11 需 WebView2 运行时（系统自带） | 启动时检测缺失 → 提示下载 / 降级为浏览器打开 |
| 批量 embedding 触发 429 | 批量入馆索引中断 | 批处理 64 条 + 失败落词法索引，向量后台补（P5 兜底） |

---

## 附录 A：词汇表

| 词 | 含义 |
|---|---|
| 补书室 | 新书唯一入口：三条来路 → 建议区/待定区 → 主人确认上架 |
| 上架 | 主人确认后书进入楼层正式馆藏（shelved） |
| 图书卡片 | catalog/bk_*.md：摘要/章节/概念/蒸馏价值/技能清单 |
| 楼层/房间/书架 | 分类三级：来源媒介 / 语义主题 / 细分主题；可自定义 |
| 虚拟书架 | 按标签/主题自动聚合的视图，书可同时在楼层和个人书架 |
| 托管滑杆 | 全托管 / 半托管（默认）/ 全手动：AI 建议→自动执行的度 |
| 操作账本 | actions 表：所有 AI 动作的记录 + 撤销依据 |
| 借书证 | profile.md 画像：藏书分布/方向池/偏好，采购员决策依据 |
| 未归位星 | 占星室里处于补书室（未确认上架）的书 |
| 桌面壳 | pywebview 封装的 Windows 桌面程序：双击启动、自动拉起本地后端（P5） |
| 批量入馆 | 一次上传 ≤10 本、同格式书籍：批量去重 + 批量 embedding + 异步索引（P5） |
| 书↔书语义边 | 占星室中按内容相似度连接两本书的边（P5） |

## 附录 B：cangjie-skill 接入接口约定（P2）

蒸馏师调用 cangjie-skill 时的适配层 `distill/adapter.py`：
- 输入：`vault/books/.../` 清洗后文本路径 + 元信息（书名/作者/来源）。
- 工作目录：`vault/skills/<book-slug>/`（即 cangjie 的 `books/<slug>/` 输出根）。
- 每阶段完成 → 回调进度（写 PIPELINE_STATE.md + 发 WS progress）。
- 阶段 0/1.5 需主人确认 → 暂停并推送通知（`action_required`）。
- 阶段 4 测试 → 结果写 `test-results.md`；未过回炉（不表面修补）。
- 阶段 5 交付 → 把 `<skill-slug>/SKILL.md` 注册进 `skills` 表 + 技能库索引（description 进向量）。

---

*本文档 v0.1 为设计稿；2026-08-14 主人审阅确认后冻结为 v1.0（P0-P4 已实现），同日追加 v0.2（P5 桌面化 + 批量入馆 + 离线阅读 + 星图增强）。变更需更新版本号与变更记录。*

