# P5 桌面化 + 批量入馆 + 离线阅读 + 星图增强任务清单（2026-08-14 启动）

> 前置：P4 打磨全部完成（140 测试，git `9f9e2e8`）。P5 主人新需求 2026-08-14 确认，设计见详细设计文档 v0.2 §11 P5。
> 建议顺序：P5-4（阅览室入口）→ P5-3（离线读原文）→ P5-2（批量入馆）→ P5-5（星图增强）→ P5-1（桌面壳），可按主人偏好调整。
> 验收方式：pytest + 浏览器手工验收；ModelScope 额度恢复后补 P2 --real（与 P5 无关，独立事项）。

## 任务分解（按序实施，每项完成 → pytest + 验收 + git commit/push）

### P5-4 📖 阅览室顶层入口 ✅（2026-08-14 完成，git `625bb78`）
- [x] 后端：books 表加 `last_read_at`（旧库 ALTER 迁移）；`GET /api/books?recent_read=true&limit=N` 按最近阅读倒序（排除删除书）；`GET /api/books/{id}/content` 读取即记录阅读时间（`repo.mark_book_read`，毫秒精度，不影响 updated_at）
- [x] 前端：顶部导航加"📖 阅览室"（`/reading`，占星室后）；新建 `Reading.vue`（最近阅读 + 最近入馆）；`BookCard.vue` 加"📖 阅读"按钮（仅已上架书）；大厅加"继续阅读"模块（chip 直达 + 全部 →）
- [x] tests/test_reading.py +3（读取标记/时间排序/排除删除+limit）；143 pytest 全绿
- [x] 浏览器实测通过；git `625bb78`（代码）+ `30d22fe`（文档）已 push

### P5-3 📖 无额度读原文（离线优先）✅（2026-08-15 完成）
- [x] content 端点优先读 vault 文件；未上架/未编目书读 `archive/raw` 原始文件（当前无 chunks 读不了）——三级兜底：vault book.md → chunks 拼接 → `archive/raw` 原始副本（按 media_type 路由解析器：pdf/markdown/html/text）；返回 `source` 字段（vault|chunks|raw）+ `raw_available`
- [x] 原文件查看：`GET /api/books/{id}/raw-file` 返回原始副本二进制（media_type→MIME 推断，`inline` 内嵌）；前端"📄 查看原文件"模态 iframe（浏览器内置 viewer 渲染 PDF）+ 新标签页链接
- [x] UI 明示模型降级：无卡片时卡片区显示"⚠️ 模型不可用或尚未生成 —— 卡片/摘要暂缺。正文离线照常可读"；`source=raw` 时正文标题旁显示"📦 离线原文"徽标
- [x] 测试：tests/test_offline_reading.py +9（vault/chunks/raw 三级来源、PDF raw 兜底解析、content 不调 LLM、raw-file MIME/字节/404）；152 pytest 全绿
- [x] 浏览器实测通过（127.0.0.1:8800）：已上架《机器学习基础》读 vault 正文照常；PDF 未上架《小蘑菇》"📦 离线原文"徽标 + 全文从原始副本读出 + "查看原文件"模态内 Chrome PDF viewer 原生渲染（embed）；无卡片书显示"模型不可用"提示

### P5-2 📥 批量入馆（≤10 本/次、同格式）✅（2026-08-15 完成，git `待填`）
- [x] `POST /api/ingest/batch`（form: files 多文件 + format + private）；文件数 >10 或混格式 → 整批 400 拒绝
- [x] 批量 sha256 去重（重复标记 duplicate，不阻断其余）
- [x] 批量 embedding（API 批 64 条/次）+ LanceDB/FTS 批量写入（N 次 API 往返降为 N/64）
- [x] 异步化：清洗登记完成后立即返回逐本状态 `{book_id, title, status, duplicate, error}`，索引后台任务 + WS progress 广播
- [x] 429/额度兜底：embedding 失败的书先落词法索引（FTS5），向量后台补（vector_pending=1），不阻断入馆
- [x] 前端：入馆对话框 = 格式下拉（必选）+ 多文件选择/拖拽（>10 本提示截断）+ 同格式校验提交前完成
- [x] 测试：tests/test_ingest_batch.py +14；166 pytest 全绿
- [x] 顺手修复预存在 flaky 测试：`test_recent_read_ordered_by_time` 在 Windows 时钟粒度下偶发失败（同毫秒并列）→ `repo.mark_book_read` 加单调推进守卫（同进程内时间戳严格递增），隔离跑 5/5 稳定

### P5-5 🌌 星图书↔书语义边
- [ ] `/api/starmap` 增加 book↔book 边：卡片级向量两两相似 + 规则边（同房间/同标签/引用）
- [ ] top-k 截断防毛线球；按关系类型着色/线宽映射相似度
- [ ] 本地缓存/词法兜底（embedding 不可用时降级）
- [ ] 测试：tests/test_starmap.py 扩展

### P5-1 🖥 桌面壳（pywebview）
- [ ] pywebview 封装 `web/dist` → Windows 桌面程序
- [ ] 双击启动 + 自动拉起/关闭 FastAPI 后端（127.0.0.1:8800）+ 托盘常驻
- [ ] WebView2 缺失检测 → 提示下载 / 降级浏览器打开
- [ ] 打包脚本 / 启动器

## 验收残留数据说明（2026-08-15 P5-2 实测后更新）

- 已上架：机器学习基础（1F 电子书/机器学习/入门）
- 补书室待定区：小蘑菇、悉达多、Hacker News 热榜、arXiv 最新论文精选、**批量测试书 A-E（5 本，P5-2 批量入馆验收产物，可直接删）**
- 已删除（档案馆可恢复）：机器学习进阶、**3 本乱码标题书（第一批 curl 直传误用 GBK 编码所致，P5-2 实测后软删）**
- 最近阅读记录：机器学习基础（2026-08-14 22:18，本次实测触发）、小蘑菇（22:17）
- 测试文件 `data/manual_accept_book.md`、`data/p52_test/`（仅剩 batch_dialog_result.png 截图）等为验收产物，可删
