# P3 Web UI MVP · 具体任务清单

> 日期：2026-08-14
> 依据：`详细设计文档.md` §11 P3 / §9.1 布局 / §9.2 REST / §9.3 WS 协议 / §5.3 表
> 前置：P2 蒸馏闭环已验收（71 测试全绿，git `P2 提交后`）
> 验收：**仅用鼠标完成"扔书→确认上架→提问→收藏采购"全流程，全程无需命令行；WS 通知在补书室有新书时弹出。**

---

## 总体目标

把 P0-P2 的 REST 能力包上一层图书馆空间隐喻 Web UI（Vue 3 + Vite SPA，FastAPI 托管同端口 8800），并补齐后端缺口：入馆上传、健康度仪表、采购员（推荐/收藏/反馈/日报）、对话落库 + 历史、设置读写 + 楼层 CRUD、WebSocket（通知 + 流式聊天 + 蒸馏进度）。主人主权硬约束沿用：所有写操作走 action ledger、AI 只建议主人确认。

## 任务分解（按依赖排序）

### T1 · schema 扩展（采购/日报/画像 + 设置）
- [ ] `app/db/schema.py` 新增表（设计文档 §5.3）：
  - `recommendations`：rec_id / date / title / url / source / score / reason / status(pending|collected|ignored|not_interested) / feedback_note / created_at
  - `daily_reports`：report_id / date / rtype(purchase|ingest|distill|system) / content(JSON) / created_at
  - `profile`：单行画像（themes JSON / direction_pool JSON / prefs JSON / updated）
- [ ] 旧库兼容：仅 CREATE TABLE IF NOT EXISTS；books 表补 `distill_status` 已有
- [ ] `app/db/repo.py` 补 CRUD：recommendations insert/list/by_date/update_status、daily_reports insert/list、profile get/save

### T2 · 入馆 API（`app/api/ingest.py`，§9.2 POST /api/ingest）
- [ ] multipart 文件上传 → `ingest_file`（复用 cleaner）→ 登记 books(status=incoming) → 自动跑索引
- [ ] JSON 文本/URL 入馆（text 字段 / url 抓取占位，无 key 时报 501 说明）
- [ ] 重复内容检测（content_hash 已存在 → 返回已存在书，不重复入库）
- [ ] 入馆后发 WS 通知 `{type:"notice", event:"book_ingested", book_id, title}`
- [ ] action ledger 记录 `ingest`（agent=system/owner）
- [ ] 测试：上传 md 文件 → 补书室出现；重复文件不重复入库

### T3 · 健康度仪表（`app/api/dashboard.py`，§9.2 GET /api/dashboard）
- [ ] 统计：待分类书数（status=reviewing）、待审阅技能数（status=reviewing）、索引状态（revision/status/chunks）、采购配额执行率（今日 recommendations 状态占比）、本周入馆趋势（近 7 天 books.created_at 计数）
- [ ] 楼层鸟瞰：每层藏书数（vault_path 前缀匹配）
- [ ] 今日日报卡片（latest daily_reports）
- [ ] 测试：空库 + 造数后字段齐全

### T4 · 采购员 API（`app/api/purchase.py`，§9.2）
- [ ] `GET /api/purchase/today`：今日配额卡片（理由）+ 推荐清单（pending）；无推荐时可手动"生成今日推荐"（内置热门源种子 + 方向池启发式，无搜索 key 用公开热榜接口可选）
- [ ] `POST /api/purchase/{rec_id}/collect`：收藏入馆 → 创建书（走 T2 入馆管线，status=incoming）→ rec status=collected + action ledger(purchase, agent=purchaser/owner)
- [ ] `POST /api/purchase/{rec_id}/feedback`：ignore / not_interested + 反馈写回 profile 负反馈
- [ ] `GET /api/daily-reports?date=`：日报列表
- [ ] 配额算法（§6.6）：按藏书主题分布分 5 格；全新主题连续 ≥3 次才纳入（简化：先按主题权重启发式，理由附上）
- [ ] 测试：生成推荐 → 收藏 → 书入补书室 → 反馈 ignore

### T5 · 对话落库 + 历史（`app/api/conversations.py`，§9.2）
- [ ] `POST /api/ask` 升级：落库 conversations + messages（role=user/assistant + refs JSON）
- [ ] `GET /api/conversations`：列表（今天/昨天/按主题分组所需字段：cv_id/title/updated_at/msg_count）
- [ ] `GET /api/conversations/{cv_id}`：全部消息
- [ ] `POST /api/conversations/{cv_id}/archive`：对话归档为书（导出 md → 走入馆管线，media_type=chat）
- [ ] 测试：提问 → 对话落库 → 列表可见 → 归档为书成功

### T6 · 设置 API + 楼层 CRUD（`app/api/settings.py` + floors.py 扩展）
- [ ] `GET /api/settings`：读 settings.json + profile → 返回 UI 可编辑子集（API 配置脱敏、采集规则、托管程度、楼层房间书架）
- [ ] `PUT /api/settings`：写回（server.paths 不可改；modelscope key 空值表示不改；写 settings.json）
- [ ] floors.py 扩展 CRUD：POST/PUT/DELETE /api/floors、/api/rooms、/api/shelves（含 action ledger 记录 owner 操作；删除有书的楼层拒绝）
- [ ] 测试：改设置写回文件；建/改名/删楼层

### T7 · WebSocket 通知 + 流式聊天（`app/api/ws.py`，§9.3 /ws/chat）
- [ ] WS 连接管理（ConnectionManager：connect/disconnect/broadcast/send_to）
- [ ] `{type:"notice", event, ...}`：book_ingested（补书室新书）、distill_progress、skill_review_ready
- [ ] `{type:"chat"}`：接收提问 → 复用 qa.ask（流式降级：先发 `ask_start` → 完整回答 `ask_done`，LLM 逐字流式留 P4；回答含 refs）
- [ ] `{type:"ask_stream"}` 兼容：同 chat
- [ ] `{type:"ping"}` → `{type:"pong"}`
- [ ] 蒸馏进度：distill pipeline 阶段变更时广播 progress（注入 ConnectionManager）
- [ ] 测试：manager 广播、ask 走 WS 返回回答

### T8 · 静态托管 + SPA fallback（main.py）
- [ ] FastAPI 挂载 `web/dist`（若存在）为静态目录，`/` 返回 index.html
- [ ] 未命中 API 的路由 fallback 到 index.html（SPA 刷新不 404）
- [ ] 启动脚本 start.ps1/start.sh 增加前端构建说明

### T9 · 前端骨架（web/，Vue 3 + Vite + Pinia + vue-router + markdown-it）
- [ ] `web/` 独立 package.json + vite.config（proxy /api + /ws → 127.0.0.1:8800，dev server 5173）
- [ ] 布局：顶部导航（🏛大厅 📖楼层 🌌占星室占位 💬管理员 🛒采购员 ⚙设置）+ WS 状态指示 + 通知 toast
- [ ] Pinia store：auth 不需要；library（健康度/楼层/补书室计数/WS 连接）
- [ ] 组件：Toast 通知（WS notice 弹出）、BookCard、StatusBadge、SectionTabs

### T10 · 页面：大厅（web/src/views/Hall.vue）
- [ ] 健康度仪表卡片（待分类书数/待审技能数/索引状态/配额执行率/本周趋势）
- [ ] 建筑鸟瞰（楼层缩略图，点击跳楼层）+ 快捷操作（扔书入馆/打开补书室）
- [ ] 今日日报卡片
- [ ] 扔书入馆（拖拽/选择文件 → POST /api/ingest → 成功后 WS 通知）

### T11 · 页面：楼层 + 补书室（web/src/views/Floors.vue）
- [ ] 左侧楼层列表（含补书室入口徽章）
- [ ] 中间房间/书架树；右侧书列表（卡片 + 状态徽章 + 虚拟书架标签视图）
- [ ] 补书室：建议区（分类建议 + 确认上架/改位置）+ 待定区（无建议）
- [ ] 书详情抽屉：卡片 + 原文（/content）+ 分类确认 + 蒸馏启动/确认

### T12 · 页面：管理员（web/src/views/Admin.vue）
- [ ] 左：对话历史（今天/昨天分组）；右：聊天窗口（流式输出 + [[引用]] 可点击跳原文）
- [ ] 下方 Tab：索引搜索（/api/search 结果列表）+ 操作账本（/api/actions + 撤销按钮）
- [ ] 提问走 WS /api/ask（或 WS chat），消息落库

### T13 · 页面：采购员（web/src/views/Purchaser.vue）
- [ ] 今日配额卡片（理由可展开）+ 推荐清单（✅收藏 ❌忽略 🙅不感兴趣）
- [ ] 历史日报列表（可展开）
- [ ] 手动"生成今日推荐"按钮（调 /api/purchase/generate）

### T14 · 页面：设置（web/src/views/Settings.vue）
- [ ] API 配置（ModelScope key 输入——显示掩码，Ollama 地址/开关）
- [ ] 采集规则（每日条数、视频开关）
- [ ] 托管程度滑杆（全托管/半托管/全手动，写 profile.prefs）
- [ ] 楼层房间编辑（增删改名排序）
- [ ] 技能审阅页并入（/api/skills 队列 + approve/reject/unblock + SKILL.md 预览）
- [ ] 蒸馏确认页并入（书籍 distill_status=awaiting → confirm-stage 确认/跳过/取消）

### T15 · 测试（tests/，P3 新增）
- [ ] `test_ingest_api.py`：上传/重复/文本入馆
- [ ] `test_dashboard.py`：统计字段齐全
- [ ] `test_purchase.py`：推荐/收藏/反馈/日报/配额
- [ ] `test_conversations.py`：ask 落库/列表/归档
- [ ] `test_settings_api.py`：GET/PUT 设置、楼层 CRUD
- [ ] `test_ws.py`：manager 广播 + ask
- [ ] 全部跑 `pytest`，旧 71 个不回归

### T16 · 验收 + 提交
- [ ] `scripts/acceptance_p3.py`：起服务（TestClient）→ 上传书 → 补书室 → 分类 → 上架 → 提问 → 生成推荐 → 收藏 → 反馈 → WS 通知收到 → 设置改楼层；全程 HTTP 无命令行
- [ ] 手动浏览器验收（127.0.0.1:8800）：扔书 → 确认上架 → 提问 → 收藏采购 全鼠标流程
- [ ] 更新 `交接文档.md`（§4 状态、目录树、验收数据）
- [ ] git commit + push（ai-library-obsidian 公开仓库）

---

## 设计取舍（P3 范围内，写死避免返工）

| 项 | 决定 |
|---|---|
| 前端技术 | Vue 3 + Vite SPA（Pinia + vue-router + markdown-it）；不用 TypeScript（小团队快速迭代） |
| 开发期代理 | vite dev 5173 → proxy /api 与 /ws 到 8800；生产由 FastAPI 直接托管 web/dist |
| 流式聊天 | WS `/ws/chat`：先 `ask_start` → 完整回答 `ask_done`（LLM 逐字流式留 P4）；REST /api/ask 同步兜底 |
| WS 通知 | ConnectionManager 单例挂在 app.state；广播 notice（book_ingested / distill_progress / skill_review_ready） |
| 采购推荐 | 无搜索 key → 内置公开热门源种子（arXiv 热榜 RSS 等，可配开关）+ 方向池启发式；评分规则透明进 reason；手动"生成今日推荐"按钮 |
| 配额算法 | §6.6 简化版：藏书主题分布（cards.tags）→ 按比例分 5 格 → 理由字符串；全新主题信号计数存 profile.direction_pool |
| 对话落库 | /api/ask 同步写 conversations+messages；`archive` 导出 md 走 ingest 管线（media_type=chat） |
| 设置存储 | settings.json（服务器配置）+ profile 表（托管程度/画像）；key 回显掩码，空值=不修改 |
| 楼层 CRUD | 有书的楼层/房间/书架禁止删除；owner 操作记 action ledger（agent=owner） |
| 技能审阅/蒸馏确认 | 作为设置页 Tab 并入（P2 过渡页 /skills/review 保留但不推荐使用） |
| 占星室 | P3 只放导航占位（P4 实现） |
| 静态托管 | 仅当 web/dist/index.html 存在时挂载；否则 / 返回"前端未构建"提示 |
| 测试 | 全部离线（FakeEmbed/FakeLLM/FakeDistiller）；WS 用 TestClient websocket_connect |
| 验收 | acceptance_p3.py 全 HTTP 断言（TestClient 模拟鼠标操作序列），浏览器手动验收记录截图 |

---

## 验收步骤（用户确认后执行）

1. `pytest` 全绿（旧 71 + P3 新增全部通过，无回归）
2. `python scripts/acceptance_p3.py`：模拟全鼠标流程通过
3. 手动浏览器验收：`start.ps1` 起服务 → 打开 127.0.0.1:8800 → 扔书入馆 → 补书室出现（WS toast）→ 分类建议 → 确认上架 → 提问（管理员）→ 采购员生成推荐 → 收藏入馆 → 反馈；全程鼠标
4. 更新 `交接文档.md` + git commit + push
