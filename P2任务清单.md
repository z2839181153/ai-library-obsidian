# P2 蒸馏闭环 · 具体任务清单

> 日期：2026-08-14
> 依据：`详细设计文档.md` §11 P2 / §6.3 / §6.4 / §6.5③ / §5.3 skills 表 / 附录 B（cangjie-skill 接入接口约定）
> 前置：P1 编目闭环已验收（49 测试全绿，git `P1 提交后`）
> 验收：1 本书蒸馏出 ≥2 个通过测试的 SKILL.md；提问命中相关技能并按框架作答；拒绝 5 次自动阻塞生效

---

## 总体目标

把"方法论类书"从**编目**推进到**技能闭环**：主人确认开始蒸馏 → 后端接 cangjie-skill 流水线（断点续跑 + 审计日志）→ 产出可执行 SKILL.md + 压力测试 → 主人审阅（批准/拒绝附原因/5 次自动阻塞）→ 批准后注册进技能库向量索引 → 问答时路由命中并注入 SKILL.md 按框架作答。

主人主权硬约束贯穿始终：蒸馏/审阅/阻塞全部经 action ledger 记录；拒绝原因可追溯；阻塞可由主人解除。

## 任务分解（按依赖排序）

### T1 · schema 扩展 + 技能库索引集合
- [x] `app/db/schema.py` 新增 `skills` 表（设计文档 §5.3 原文，补充 blocked 状态）：
  - `skill_id`(sk_*) / `book_id` / `name` / `slug` / `path`(skills/<book-slug>/<skill-slug>/SKILL.md) / `description`(trigger 条件，进向量索引做路由) / `status`(draft|reviewing|approved|rejected|installed|blocked) / `reject_count`(连续拒绝次数，≥5 自动阻塞) / `last_reject_reason` / `test_prompts`(JSON，darwin 兼容) / `created_at` / `updated_at`
- [x] 旧库迁移：仅 `CREATE TABLE IF NOT EXISTS`，不动现有表；`catalog_cards.skills` 字段（JSON [{skill_id, name, status}]）已预留，P2 回填
- [x] `app/db/repo.py` 补 CRUD：skills upsert/get/list(by status/book)、reject_count 递增 + reason 更新、status 更新
- [x] 技能库向量索引：新 LanceDB collection `skills`（doc = skill_id + description + 书名 + SKILL.md 摘要，vector = embed(description)），放 `app/router/skill_index.py`（复用 embeddings 客户端）

### T2 · 蒸馏产物扫描解析（`app/distill/artifacts.py`）
- [x] 扫 `vault/skills/<book-slug>/` → 检测 cangjie 规范产物：PIPELINE_STATE.md / BOOK_OVERVIEW.md / candidates/ / verified.md / rejected/ / `<skill-slug>/SKILL.md` / `test-prompts.json` / `test-results.md` / INDEX.md / GLOSSARY.md / DIGEST.md
- [x] 解析 SKILL.md frontmatter（name/description）+ 正文六段（R/I/A1/A2/E/B）；解析 test-prompts.json 并校验 darwin 格式 + **必须含诱饵（should_not_trigger）**，缺失标 warning（质量红线）
- [x] 输出注册数据：生成 `sk_*` id、name、slug、path、description、test_prompts（供 T6 注册）

### T3 · 蒸馏状态机 + 断点续跑（`app/distill/pipeline.py`）
- [x] 状态机（对齐 cangjie 5 阶段）：`idle → stage0(reading) → awaiting_confirmation(0) → stage1(extracting) → stage1_5(verifying) → awaiting_confirmation(1.5) → stage2(building) → stage3(linking) → stage4(testing) → stage5(delivering) → done / failed / blocked`
- [x] 每阶段完成 → 更新 `vault/skills/<book-slug>/PIPELINE_STATE.md`（cangjie 规范 checklist 格式，断点续跑唯一事实源）；启动时读它从最后阶段续跑
- [x] 阶段 0/1.5 主人确认：状态停在 `awaiting_confirmation`，`POST /api/distill/{book_id}/confirm-stage`（确认 / 跳过 / 取消）——设计文档 §6.4"阶段 0 需主人确认骨架（可跳过）、阶段 1.5 需主人轻确认"
- [x] 进度查询：`GET /api/distill/{book_id}/status`（轮询；WS 进度推送留 P3，附录 B 的 WS progress 降级为轮询）
- [x] 审计复制：每阶段产物（candidates/rejected/测试结果/SKILL.md）复制到 `archive/distill-logs/<book-slug>/`
- [x] 触发条件（§6.3 决策）：书已上架 + 卡片 `category=methodology` 且 `distill_value>=60` 才允许 `POST /api/distill/{book_id}/start`；否则拒绝并说明理由
- [x] action ledger 记录：`distill_start` / `distill_stage` / `distill_block` / `skill_approve` / `skill_reject` / `skill_unblock`（agent=distiller/owner）

### T4 · FakeDistiller（`app/distill/executor_fake.py`，测试/离线）
- [x] 按状态机推进，产出**结构对齐 cangjie 规范**的假产物：BOOK_OVERVIEW.md + candidates/（5 类）+ verified.md + 2 个 SKILL.md（六段齐全）+ test-prompts.json（含诱饵）+ test-results.md + INDEX.md + GLOSSARY.md + DIGEST.md
- [x] 可注入失败/超时/缺诱饵场景（测异常分支）；确定性输出，pytest 不依赖真实 API

### T5 · LLMDistiller 内置执行器（`app/distill/executor_llm.py`，真实蒸馏）
- [x] 串行执行 cangjie 5 阶段（官方降级方案：无 sub-agent 时串行），模型用 `settings.modelscope.distill_model`（GLM-5.2）
- [x] **prompt/模板单一事实源**：`settings` 新增 `distill.cangjie_skill_dir`（默认指向 `C:\Users\Z\.qwenpaw\workspaces\yh7ECT\skills\cangjie-skill`），执行器运行时读取其 `extractors/*.md`（5 个提取器 prompt）与 `templates/*.md`（BOOK_OVERVIEW/SKILL/INDEX/DIGEST/test-prompts 模板）；cangjie 更新即生效
- [x] 阶段 0：Adler 四步（methodology/01）→ BOOK_OVERVIEW.md，正文超上下文按章节分块读（methodology/02）
- [x] 阶段 1：5 个 extractor 串行调用 → `candidates/<type>.md`
- [x] 阶段 1.5：三重验证 V1 跨域 / V2 预测力 / V3 独特性（LLM 判定）→ `verified.md` + `rejected/`（附原因，保留审计）
- [x] 阶段 2：RIA++ 构造（methodology/04 + SKILL.md.template）→ 每个通过单元一个 SKILL.md（六段：R/I/A1/A2/E/B）
- [x] 阶段 3：Zettelkasten 链接（methodology/05）→ INDEX.md + GLOSSARY.md，回填 A2"与相邻 skill 区分"
- [x] 阶段 4：按模板生成 test-prompts.json（3–5 should_trigger + 2–3 诱饵**含跨 skill 混淆** + 1–3 edge）；LLM 盲测（隐藏 type/expected 让模型判是否触发）→ test-results.md，**标注 fallback 自测**（无独立 sub-agent）；通过率 100% 接受 / ≥80% 分析 / <80% 回炉阶段 2
- [x] 阶段 5：DIGEST.md 精华长文 + 交付到 `vault/skills/<book-slug>/`
- [x] 断点续跑：每阶段产物落盘 + PIPELINE_STATE.md 更新，中断后从最后阶段续

### T6 · 技能审阅 API + 阻塞逻辑（`app/api/skills.py`）
- [x] `GET /api/skills?status=reviewing` → 待审阅队列（含来源书卡片）
- [x] `GET /api/skills/{skill_id}` → SKILL.md 全文 + test-prompts + test-results + 来源书
- [x] `POST /api/skills/{skill_id}/approve` → status=installed，**注册进技能库向量索引**（description embedding upsert）
- [x] `POST /api/skills/{skill_id}/reject`（body: reason）→ status=rejected，`reject_count++`，reason 记录（重蒸时带进 prompt）
- [x] 阻塞逻辑：`reject_count >= 5` → skill.status=blocked + 该书蒸馏任务标记 blocked（再次 start 被拦截，提示主人人工干预）；`POST /api/skills/{skill_id}/unblock`（主人解除，重置计数）
- [x] 极简审阅页 `GET /skills-review`（单个 HTMLResponse 页，无 Vue）：技能列表 + SKILL.md 全文渲染 + 测试结果 + 批准/拒绝按钮——P3 完整 Web UI 前的过渡，P3 并入
- [x] action ledger 全记录（approve/reject/unblock 均记 + 可撤销：approve 的 undo=回 reviewing 并移除索引）

### T7 · 技能路由 + 问答注入（`app/router/skill_router.py` + `app/core/qa.py` 接入）
- [x] `SkillRouter.retrieve(query, top_k)`：query embedding → `skills` collection 检索 → 分数 ≥ 阈值（可配）命中
- [x] 命中 1 个 → 注入；命中 ≥2 → LLM 裁决（chat_json 选最相关，可配置"全部注入"）——对应 §6.5 ③"多技能冲突 → LLM 裁决"
- [x] 注入方式：SKILL.md 全文（超长截断）追加进问答 system prompt（"馆内已加载技能《X》：请优先按此框架作答"）
- [x] `qa.py` 流程升级：定位 → 概览 → **技能路由** → 作答；返回新增 `used_skills: [{skill_id, name}]`；无命中行为与 P1 一致
- [x] 索引维护：approved → upsert；rejected/blocked → 移除或标记不参与路由
- [x] 无 key 降级：路由不依赖 LLM 裁决（纯向量分数），问答降级逻辑保持 P1 行为

### T8 · 测试（tests/）
- [x] `test_schema_p2.py`：skills 表存在/字段/默认状态；旧表不受影响
- [x] `test_artifacts.py`：产物扫描解析、SKILL.md 六段校验、test-prompts 缺诱饵 → warning
- [x] `test_pipeline_fake.py`：FakeDistiller 全流程状态推进、阶段确认暂停/继续/跳过、断点续跑、审计复制、失败场景
- [x] `test_review.py`：批准→installed+索引注册；拒绝→rejected+计数+原因；5 次→blocked+书阻塞；unblock；undo
- [x] `test_skill_router.py`：命中注入、阈值过滤、多技能裁决、rejected/blocked 技能不注入
- [x] `test_qa_p2.py`：问答带技能注入（验证 system prompt 含 SKILL.md 内容）、used_skills 字段、无 key 降级仍可用
- [x] `test_api_p2.py`：新端点冒烟（distill start/status/confirm-stage、skills CRUD、skills-review 页）
- [x] 全部跑 `pytest`，确保旧 49 个不回归

### T9 · P2 验收 + 提交
- [x] `scripts/acceptance_p2.py`：默认 Fake 离线全流程（构造 1 本 methodology 书 → start → 蒸馏 → ≥2 个 SKILL.md → approve → 索引 → 路由命中 → 问答按框架 → 拒绝 5 次 → blocked），`--real` 走真实 API
- [x] **LLM 切 DeepSeek 官方**（2026-08-15）：ModelScope 免费额度耗尽（GLM 空响应），改接 `https://api.deepseek.com/v1`（模型 `deepseek-v4-flash`）；chat/distill 走 DeepSeek（secrets.deepseek_api_key），embedding 保持 ModelScope 免费（独立 `embed_base_url`/`embed_api_key` 配置，见 T5 下方"DeepSeek 接入改动"）
- [x] **混合预验收 `--real-llm` 通过**（2026-08-15，LLM=真实 DeepSeek / embedding=Fake）：10 步全绿，16 个技能注册，耗时 1798.9s；验证 DeepSeek 蒸馏链路端到端可用
- [ ] **全真 `--real` 验收**：待 ModelScope embedding 额度恢复（当日 0 点）后补跑一条命令即可（LLM=DeepSeek / embedding=ModelScope 真）
- [x] 更新 `交接文档.md`（§4 状态、目录树、验收数据）
- [x] git commit + push（ai-library-obsidian 公开仓库）

### T5.5 · DeepSeek 接入改动（2026-08-15，随 T5/T9 一并提交）
- [x] `app/config.py`：`ModelScopeConfig` 新增 `embed_base_url`/`embed_api_key`（embedding 独立配置，空=跟随 chat 配置）；密钥加载：`api_key` ← env/secrets.deepseek_api_key/modelscope_api_key（chat/distill），`embed_api_key` ← settings/env/secrets.modelscope_api_key（embedding）
- [x] `app/llm/embeddings.py`：client 用 `embed_base_url`/`embed_api_key`（缺省回退 chat 配置）
- [x] `app/api/settings.py`：GET/PUT 支持 embed_base_url/embed_api_key（掩码展示）；`api_key` 写 secrets.deepseek_api_key，`embed_api_key` 写 secrets.modelscope_api_key
- [x] `config/settings.json`：`base_url` → DeepSeek 官方、`chat_model`/`distill_model` → `deepseek-v4-flash`、`embed_base_url` → ModelScope
- [x] 实测修复 3 个 DeepSeek 接入暴露的问题：
  1. **stage1_5 验证 JSON 截断**（单批 15 候选输出 429 字符即停，`_parse_verdicts` 解析失败 → verified 空 → 0 技能）：改为分批验证（每批 ≤3）+ `_parse_verdicts` 截断容错（逐对象解析）+ 未判定候选保守放行
  2. **SKILL.md 格式不规范**（DeepSeek 常用 ```markdown 围栏包裹全文、六段标题写 `## R 原文引用`、frontmatter 漏闭合 `---`）：stage2 输出 `_strip_fence()` 剥离围栏；prompt 硬性要求 `## R — 原文引用` 标题与裸输出；`artifacts._find_sections` 放宽匹配 `## X`；`_parse_frontmatter` 容错漏闭合
  3. **慢响应超时**（DeepSeek 生成约 8 字符/s，6 条判定需 70s+ 撞 60s 默认 timeout）：LLMDistiller 的 ChatClient `timeout=150s`

---

## 设计取舍（P2 范围内，写死避免返工）

| 项 | 决定 |
|---|---|
| 蒸馏执行引擎 | **后端内置 LLM 执行器**（串行 5 阶段，模型=settings.modelscope.distill_model）+ FakeDistiller 供测试；外部 QwenPaw 蒸馏师 agent 模式留 adapter 接口（附录 B），P3/二期接 MCP 再启用 |
| LLM 提供方 | 2026-08-15 起 chat/distill 走 **DeepSeek 官方 API**（`https://api.deepseek.com/v1`，`deepseek-v4-flash`，secrets.deepseek_api_key）；embedding 独立走 **ModelScope 免费**（`embed_base_url`，secrets.modelscope_api_key）。原因：ModelScope 免费额度每日 0 点恢复但当天易耗尽（GLM 空响应/429），DeepSeek 官方付费稳定 |
| 三重验证（stage1_5） | 分批（每批 ≤3 候选）+ JSON 截断容错解析 + 未判定候选保守放行（防链路卡死）；DeepSeek 长 JSON 输出易截断/慢（约 8 字符/s），单批过大实测 60s 超时 |
| SKILL.md 格式 | stage2 输出剥离 ```markdown 围栏；六段标题 `## X —`（扫描器兼容 `## X 标题`）；frontmatter 漏闭合容错；prompt 硬性约束 + 输出后清洗双保险 |
| sub-agent 并行 | 后端无 sub-agent 能力 → 阶段 1 用 5 个 extractor prompt 串行（cangjie 官方降级方案）；阶段 4 用 LLM 盲测并在 test-results.md 标注 fallback 自测 |
| prompt/模板来源 | 直接引用 cangjie-skill 目录（settings 新增 `distill.cangjie_skill_dir`），**不复制进项目**，保持单一事实源 |
| 阶段 0/1.5 主人确认 | 状态停在 `awaiting_confirmation` + API 确认/跳过/取消；确认 UI 留 P3（过渡期命令行/API 操作） |
| 审阅 UI | P2 极简 HTML 单页（HTMLResponse，无 Vue）；P3 并入完整 Web UI |
| 技能库索引 | 独立 LanceDB collection `skills`，doc = description + 书名 + SKILL.md 摘要；命中阈值可配（默认 0.5，真实验收后校准） |
| 多技能冲突 | 命中 ≥2 时 LLM 裁决 top-1；无 key 时降级为按分数取 top-1（裁决不阻塞问答） |
| 注入方式 | SKILL.md 全文（截断阈值 ~8000 字符）追加进问答 system prompt；used_skills 随回答返回 |
| 阻塞粒度 | skill 级 `reject_count>=5` → blocked + 该书蒸馏任务 blocked；unblock 由主人，重置计数 |
| WS 进度推送 | P2 用轮询 `GET /api/distill/{book_id}/status`；WS 留 P3（附录 B 的 WS progress 降级为轮询） |
| 审计 | actions 表新增 distill 相关 action_type（agent=distiller/owner）+ `archive/distill-logs/<slug>/` 全量复制 |
| 重蒸 | 拒绝原因带进下次重蒸 prompt；重蒸保留历史版本（新 SKILL.md 用新 sk_* id），不覆盖已 installed |
| 蒸馏触发 | 仅已上架 + `category=methodology` + `distill_value>=60` 可 start（§6.3 决策）；全托管差异留 P3 设置页 |
| 长文本处理 | 正文超上下文 → 按章节分块喂 extractor（methodology/02 分块策略），最终候选池合并去重 |

---

## 验收步骤（用户确认后执行）

1. `pytest` 全绿（旧 49 + P2 新增全部通过，无回归）
2. `python scripts/acceptance_p2.py`（Fake 离线）：构造 1 本 methodology 书 → 蒸馏 → ≥2 个 SKILL.md 全部 registering → 批准 1 个 → installed + 索引 → 提问命中技能 → 回答按框架 + `used_skills` 非空 → 对另一技能连续拒绝 5 次 → blocked 生效（再次 start 被拦截）→ unblock 恢复
3. 真实验收 `python scripts/acceptance_p2.py --real`（需 secrets.json 就位）：GLM-5.2 蒸馏 1 本真实方法论书 → ≥2 个通过测试的 SKILL.md（test-results 通过率 ≥80%，缺诱饵打回）；记录耗时与质量基线（对标 P1 卡片基线的记录方式）
4. 提问命中验证：问一个命中技能场景的问题 → 回答按技能框架作答且带 `[[catalog/bk_<id>]]` 引用 + `used_skills` 字段
5. 阻塞验证：`POST /api/skills/{skill_id}/reject` 连拒 5 次 → skill 与书均 blocked，`POST /api/distill/{book_id}/start` 返回 409 拦截提示
6. 更新 `交接文档.md` + git commit + push
