# P1 编目闭环 · 具体任务清单

> 日期：2026-08-14
> 依据：`详细设计文档.md` §11 P1 / §5.3 / §5.5 / §6.0–6.3 / §6.5 / §6.9 / §9.2
> 前置：P0 索引管线已验收（git `4dd88de`，22 测试全绿）
> 验收：1-3 本书入馆→卡片→确认上架全流程 ≤ 5 分钟人工操作；"这本书讲了什么"能答并定位原文；问答引用可点击跳原文

---

## 总体目标

把"书"从索引层推进到**编目闭环**：入馆 → LLM 生成图书卡片 → 分类建议 → 补书室等待 → 主人确认上架（可撤销）→ 检索问答带 `[[wikilink]]` 引用 + 原文阅读。

主人主权硬约束贯穿始终：AI 只出建议、不写死；确认/撤销全部走 action ledger。

## 任务分解（按依赖排序）

### T1 · 数据库 schema 扩展 + 默认楼层种子
- [x] `app/db/schema.py` 新增表（设计文档 §5.3 原文）：
  - `floors` / `rooms` / `shelves`（目录的 DB 镜像）
  - `catalog_cards`（卡片：summary/chapters/concepts/distill_value/distill_reason/generated_at/model）
  - `actions`（操作账本：agent/action_type/target/params/undo_params/status/reason/created_at）
- [x] 旧库迁移：`books` 表字段已齐（suggest_floor/room/shelf/confirm_by 已有），无需改；仅新增表用 `IF NOT EXISTS`
- [x] 默认 4 楼层种子（仅当 floors 为空时插入）：`1F 电子书`(pdf/epub/ebook) / `2F 网页公众号`(web/html) / `3F 聊天记录`(chat) / `4F 视频转写`(video)
- [x] `app/db/repo.py` 补 CRUD：floors/rooms/shelves 查询与插入、catalog_cards upsert/get、actions insert/list/get/undo 更新

### T2 · LLM Chat 客户端（新）
- [x] `app/llm/chat.py`：OpenAI 兼容 chat 客户端（复用 config.modelscope）
  - `chat(messages, json_mode=False) -> str`：DeepSeek-V4-Flash，失败重试 2 次，超时保护
  - `chat_json(prompt, schema_hint) -> dict`：解析模型 JSON 输出（去 ```json 围栏，容错）
  - 无 API key → 抛 `LLMUnavailable`（上层降级，与 embedding 客户端风格一致）
- [x] `tests/conftest.py` 增加 `FakeLLM`：确定性伪 LLM（读入内容返回固定 JSON 模板），测试不依赖真实 API

### T3 · 图书卡片生成器（`app/core/card_generator.py`）
- [x] 输入：`book_id`；取该书 chunks（正文抽样 = 前 20% + 全部章节标题）做 LLM 输入，控制 token 量
- [x] LLM 生成 JSON（设计文档 §5.5 卡片字段）：
  - 书名 / 一句话简介 / 200 字摘要 / 章节结构 [{title, summary, ref}] / 关键概念 [{term, definition, ref}] / 标签数组
  - 蒸馏价值评估：`distill_value`(0-100) + `category`(methodology|reference|narrative|data) + `reason`
- [x] 落盘 `catalog/bk_<id>.md`（frontmatter + 正文，status: reviewing，格式见 §5.5）
- [x] 写入 `catalog_cards` 表 + `books.card_path`；action ledger 记录 `classify`
- [x] 私密书（private=1）→ 跳过 LLM，卡片标记"模型不可用（私密）"
- [x] 幂等：卡片已存在且书未变 → 跳过；可 `force=True` 重新生成

### T4 · 分类建议（`app/core/classifier.py`，或并入 card_generator）
- [x] 楼层 = 来源媒介固定映射（media_type → 楼层 code），不经过 LLM
- [x] 房间/书架 = LLM 建议：结合已有房间/书架列表（prompt 里列出，优先匹配已有；否则建议新建），输出 suggest_room/suggest_shelf
- [x] 写入 `books.suggest_floor/suggest_room/suggest_shelf`
- [x] 全托管/半托管差异留 P3 设置页，P1 固定"半托管（默认）：停在 reviewing 等主人确认"

### T5 · 补书室 API（`app/api/books.py`）
- [x] `GET /api/books?status=reviewing` → 补书室列表（建议区：有建议；待定区：AI 无法判断/解析失败）
- [x] `GET /api/books/{book_id}` → 书详情 + 卡片内容 + 分类建议
- [x] `POST /api/books/{book_id}/classify` → 手动触发 T3+T4（入馆后自动也走这里）

### T6 · 确认上架 + 撤销（`app/core/shelving.py` + `app/api/actions.py`）
- [x] `POST /api/books/{book_id}/confirm`（body: floor/room/shelf，可覆盖建议）
  - 校验楼层存在；房间/书架不存在 → 自动创建（.json + README.md）
  - 创建 `books/<楼层>/<房间>/<书架>/<书名>/` 目录，把清洗后正文写为 `book.md`（章节分文件可选，P1 先单 book.md）
  - 更新 books：`vault_path`、`status=shelved`、`confirm_by`
  - action ledger：`shelve` + `undo_params`（记录原位置/原状态）
- [x] `GET /api/actions` + `POST /api/actions/{id}/undo`：
  - undo shelve → 移回补书室（status=reviewing，vault_path 清空）
  - undo classify → 清空分类建议
- [x] 单写入锁：所有写操作经 ledger 串行（asyncio.Lock 或简单进程内锁，P1 单进程够用）

### T7 · 基础问答（`app/core/qa.py` + `POST /api/ask`）
- [x] 流程（§6.5 前四步）：
  1. 定位：复用 searcher 混合检索 top-k chunks + 命中书卡片摘要 → 确定 1-3 本书
  2. 概览：组装书级上下文（卡片摘要 + 命中章节 + 原文 chunk 片段，截断 token）
  3. 作答：LLM 生成回答，**要求带 `[[catalog/bk_<id>]]` 引用**
  4. 返回：`{answer, refs: [{book_id, title, link, snippet}]}`；同轮回写 conversation（可选，P1 至少返回）
- [x] 无 key 降级：仅返回检索到的原文片段（不生成回答），标注"模型不可用"
- [x] 技能路由（§6.5 ③）留 P2，P1 不接

### T8 · 阅览室（原文阅读）
- [x] `GET /api/books/{book_id}/content` → 按 chunk seq 返回全文分节（shelved 书读 vault_path，未上架读 chunks）

### T9 · 测试（tests/）
- [x] `test_schema_p1.py`：新表存在、默认楼层种子
- [x] `test_cards.py`：FakeLLM 下卡片生成/落盘/幂等/私密跳过
- [x] `test_shelving.py`：确认上架建目录、覆盖建议、undo 回补书室
- [x] `test_qa.py`：问答返回 answer+refs、无 key 降级
- [x] `test_actions.py`：ledger 记录与撤销
- [x] `test_api.py` 扩展：新端点冒烟
- [x] 全部跑 `pytest`，确保旧 22 个不回归

### T10 · P1 验收 + 提交
- [x] `scripts/acceptance_p1.py`：生成 1-3 本真实书 → 入馆 → 卡片（--real 走真 LLM）→ 确认上架 → 问"这本书讲了什么" → 校验引用格式与原文定位；统计全流程耗时
- [x] 真实验收（需 data/secrets.json 就位）：记录卡片质量基线（P1 风险点）
- [x] 更新 `交接文档.md`（§4 状态、目录树、验收数据）
- [x] git commit + push（ai-library-obsidian 公开仓库）

---

## 设计取舍（P1 范围内，写死避免返工）

| 项 | 决定 |
|---|---|
| `/api/ask` 流式 | P1 **同步返回**（answer+refs）；SSE 流式随 P3 Web UI 按 §9.3 协议落地 |
| 章节文件 | P1 单 `book.md`（正文含章节标题），章节分文件留 P2/P4 |
| 全托管/半托管 | P1 固定半托管（停在 reviewing）；滑杆留 P3 设置页 |
| 技能路由 | P2 接；P1 问答不注入 SKILL.md |
| conversations 表 | P1 建表但不实现对话存储（`/api/ask` 不强制落库），P3 聊天用 |
| 楼层 CRUD API | P1 只实现 `GET /api/floors` + 种子；增删改留 P3 设置页 |
| LLM 超时 | 卡片/问答单次调用设 timeout（默认 60s），避免免费 API 卡死 |
